# Artemis — Feature Backlog

Raw idea inbox. **Throw anything here the moment it occurs to you** — half-formed is fine, no
structure required, no commitment to build it. This is the feeder: SP0 and later planning drain
items from here into `REQUIREMENTS.md`, the roadmap, and specs. Capturing ≠ committing.

**How to add an idea:**
- Just add a `- ` line under **Inbox** (hand-edit), **or**
- In any Artemis session say **"backlog: <idea>"** and I'll append it (dated).

When an item gets pulled into the planning corpus, move it to **Triaged** with a pointer so the
trail isn't lost.

---

## Inbox (unsorted — dump here)

<!-- Format is loose. Optional tags in [brackets] if useful, e.g. [integration] [second-brain] [voice]. Example:
- Auto-summarise overnight emails into a morning briefing  [integration][assistant]
-->

- **✅ DESIGNED 2026-06-25 → ADR-031 (was: ⭐ PLANNING TOPIC) — Artemis agentic capability: executor + harness + host computer-use.** Functional-design discussion DONE (decisions A–G); refines ADR-024+022. One unified executor runtime · Rung 0→4 host-computer-use ladder · blast-radius authority · coding subsystem = borrow OpenHands SDK under Artemis planner/inbox/gate/router · 2 owner-approved privacy exceptions. Design: `docs/findings/agentic-runtime-design-decisions.md`; research: `docs/research/2026-06-25-agentic-harnesses/`. Spec series post-spoke-wave. _(Original framing below kept for trail.)_ Fold the scattered agentic pieces into one planning discussion: **ADR-024 Task Executor (M9)** + **ADR-022 harness** + **`agent-loop-reliability` guardrails** + the existing "autonomous agents on local tokens" item below, EXTENDED to the genuinely-new surface — **Artemis operating the host computer it lives on** (shell/files/apps/desktop). Owner steer: true agenticness ≈ (M9 executor + harness) **combined with** host computer-use; needs discussion. Full framing + the 8 questions for planning (start at concrete-first-use-case → authority/sandbox model → executor-vs-autonomous-agent consolidation): **`docs/findings/agentic-computer-use-harness-discussion.md`**. NB the live APEX harness (this very build session) is a working plan→dispatch→verify→commit + GATE + sandbox reference to borrow from. Run as a functional-design discussion (apex-plan), ADR before specs.  [aci][assistant][capability][agentic]  (2026-06-25)
- **Agentic coding/runtime UI** (deferred from the 2026-06-25 ADR-031 design) — 4 new surfaces folding into the **locked travel-zoom map** (not a new app): (1) background-task monitor (status/progress/pause-resume-cancel) · (2) plan-preview (ADR-024 trigger) · (3) **pause-to-ask-you answer surface** (visual half of `AskOwnerTool`/agent-inbox — load-bearing) · (4) build/task review (diffs/files/tests/commit) — **must include VISUAL review for UI-building tasks: mockup-preview before coding + rendered-screenshot/live-preview after** (borrow Playwright/imagegen/run; even headless via ntfy-image). Reuses Ask-pop-up (kick-off) · ntfy/M6-c (notify) · GATE-b (approvals). Distinct UI/UX discussion (apex-ui-ux-design × client direction); design when the agentic build nears.  [client][ui][agentic]  (2026-06-25)
- **Agentic self-verification of the activation/owner-manual tails** — the Artemis Task Executor (M9/ADR-024, atop the ADR-031 agentic runtime) should **run the owner-manual activation runbook automatically** as a self-test suite, instead of the owner walking it by hand. Targets the steps in `docs/technical/setup/owner-manual-activation-runbook.md`: brain `/healthz` + heartbeat, owner-private unlock, client build/launch, the DER signature handshake (4b), the overlay Ask round-trip (5), and Google go-live (6). **Triage tensions:** the Hello-gated steps need a real console-window gesture (a headless agent can't supply a PIN/biometric → either a one-time human-in-loop unlock the agent reuses, or run the non-gesture subset); deterministic **read-back** verification (never agent-self-judged — reuse the ADR-024 verification stance); GATE posture for anything external-effect (Google OAuth). Natural first concrete use-case for the autonomous-agent item below ("define what an autonomous agent *does* here"). Pairs with the host computer-use ladder (ADR-031). _(Surfaced 2026-06-28 while manually running the activation tails — found 2 real blockers a self-test would have caught: a misrouted `artemis-brain` entry point + a Mac `data_root` default failing the Windows DPAPI guard.)_  [aci][agentic][testing][capability]  (2026-06-28)
- **⛔ BUILD GAP (found 2026-06-28, blocks the overlay demo): client has no pairing UI wired in.** The Tauri client boots at `connection.state="unpaired"`; `App.tsx:131` renders `null` until `connectedLocked`/`unlocked`, and **nothing in the running app renders a pairing surface or calls `pairDevice()`** (`auth/pairing.ts:45` — referenced only by its own test). So first launch = permanent blank screen, no way to pair from the UI. **Scope to unblock:** (1) build a pairing screen (render on `unpaired`/`disconnected`; collect the brain's pairing code; call `pairDevice` with the PairingError → toast states already defined; likely a recovery-passphrase entry too per ADR-005) — view-layer, but needs the brain-side pairing-code source confirmed; (2) THEN debug the **never-exercised live auth handshake** (`auth_pair`/`auth_connect`/`auth_unlock` + the DER signature path = the gated `CLIENT-auth` Task 7 / runbook Step 4b; handoff flagged a possible X9.63-vs-SPKI encoding fork). Blocks runbook Steps 3→4→5 (incl. the headline game-overlay Ask demo). Needs a planning→build cycle, not an ops fix. _(3rd never-run-end-to-end gap found while manually walking the activation runbook — reinforces the agentic self-verification item above.)_  [client][ui][auth][agentic]  (2026-06-28)
- **⛔ BUILD GAP (found 2026-06-28): `artemis-google-auth login` registers no scopes → bails.** `cli.py login` computes `sorted(required_scopes())` but imports no connector modules, so the scope registry is empty → `"no scopes: pass --scope or ensure a connector registered scopes"` (cli.py:59-62). Root cause = **inconsistent scope registration**: `modules/calendar` registers at module **import** (top-level call), but `modules/gmail/module.py` registers only *inside* `build_gmail_manifest()` (`register_gmail_scope()`, line 25) — so the brain (which composes manifests) has the scopes, the standalone CLI never does. **Fix:** make the CLI import + trigger connector scope registration before `login` (and/or move gmail's `register_google_scopes` to module top-level for parity with calendar). **Workaround (ops):** `artemis-google-auth login --scope https://www.googleapis.com/auth/gmail.readonly [--scope …]`. **Also stale:** the go-live runbook says "least-privilege read-only," but calendar registers `calendar.events` (write); confirm intended scope set. _(4th never-run-end-to-end gap found walking the activation runbook — see the agentic self-verification item above.)_  [google][auth][build]  (2026-06-28)
- **⛔ ROBUSTNESS BUG (found 2026-06-28, first live observe run): `QuarantinedReader.read()` crashes the whole batch on a single model transport error.** The `self._model.complete(...)` call (`untrusted/quarantine.py:132-140`) sits **outside** the `try/except` (which starts at line 143 and only catches JSON-*parse* errors). So an httpx `HTTPStatusError` (e.g. Ollama `400 Bad Request`) from `raise_for_status()` (`model_adapters.py:85`) propagates **uncaught** → `poll_once` → the entire `artemis-dev-email-rules --once` run dies. For the untrusted-email reader specifically, one oversized/odd email kills the poll. **Fix:** wrap the model call too; on a transport/HTTP/timeout error degrade that message to `parse_failed`/blank `Extract` (same as the JSON path) or skip-and-continue. **Live evidence:** 7 emails degraded cleanly (`Quarantined extract parse failed (JSONDecodeError)`), the 8th threw `400` and crashed.  [security][robustness][rag][build]  (2026-06-28)
- **⚠ MODEL-QUALITY (found 2026-06-28): local `qwen3:4b` via Ollama returns prose, not strict JSON, for the quarantine reader.** The adapter requests `response_format: {type: json_schema, strict: true}` (`model_adapters.py:71-79`), but **Ollama's OpenAI-compat endpoint ignores it** → every email's read returned non-JSON → `JSONDecodeError` degrade (7/7 before the crash). Structured-output is unreliable. **Options:** use Ollama's native `format` param (top-level `format: <json-schema>` / `format: "json"`, not the OpenAI `response_format` shape) via an Ollama-aware adapter path; or prompt-coax + tolerant parse; or a different reader model. This is the exact tuning signal the observe harness exists to surface (R5d classifier + reader). **Also probable:** the `400` was **context-length overflow** — a long email body pushed the prompt past `qwen3:4b`'s default `num_ctx`; raising `num_ctx` (Modelfile/options) or truncating the laundered body before the reader would help. _(These two + the 4 earlier gaps = 6 first-run breakages found walking the activation runbook; all unit-built, none run end-to-end → reinforces the agentic self-verification item above.)_  [ai-systems][rag][model-tuning]  (2026-06-28)
- Quote of the day — user adds quotes, assistant serves/finds them (daily or on demand)  [second-brain][assistant]  (2026-06-02)
- Acoustic-gesture activation — finger-snap + clap "turns Artemis full on" (wake/activate the assistant via a sound gesture, not a wake-word). Sits on the M5 audio sidecar; needs a sound-event classifier alongside STT/EOU. Decide: what "full on" means (wake from idle? full attention mode?) + false-trigger handling.  [voice][assistant]  (2026-06-16)
- **Migrate the standalone Codex build driver → APEX mechanic A.** APEX ADR-027 made Codex the primary coder *inside* the apex-code harness (an `APEX_MODE=coding` Opus host dispatches `codex exec -p apex-coder` per task; Opus inline-fallback on quota-out — gets verify/family-aware-review/handoff/checkpoint for free). Artemis still drives builds via the standalone `docs/bring-up/CODEX-BUILD-RUNBOOK.md` (per-spec, outside apex-code — ADR-027's "interim option B until it migrates to A"). Migration = align Artemis status.md (`coder_models: [codex]`, retire flash/pro), reconcile Artemis ADR-026 with APEX ADR-027, redirect/retire the runbook to mechanic A, then validate one real build. Needs an Artemis planning session (its full build context). Parallel-Codex within a wave is also now available (APEX ADR-028) if Artemis waves have independent tasks.  [build][infra]  (2026-06-23)

<!-- From the 2026-06-05 repo-studies pass — detail + verified sources in docs/research/repo-studies-prior-art.md -->
- Graph-RAG tier for the brain (symbol/entity knowledge-graph + multi-hop) alongside vector search — compare codegraph-ai/CodeGraph ONNX-memory vs current brain-architecture  [second-brain][rag]  (2026-06-05)
- Incremental fingerprint indexing + committed graph artifact for ingest (re-index only changed files) — from Understand-Anything  [second-brain][rag]  (2026-06-05)
- Enterprise-search query path: decompose → parallel multi-source → confidence-scored synthesis → digest (budget-guarded; HIGH cost) — from anthropics knowledge-work-plugins  [second-brain][assistant]  (2026-06-05)
- **✅ DESIGNED 2026-06-29 → `docs/findings/anti-hallucination-answer-layer-design-2026-06-29.md` — Anti-hallucination brain invariants** (gap-tagging [MATERIAL GAP], multi-index citation verify, claim-audit gate; from academic-research-skills). Design = **tiered**: deterministic cheap floor (gap-tag-on-empty/thin retrieval + citation-presence validation in `_rag_messages`, no model dependency) **dev-buildable now**; model-driven claim-audit + cross-document contradiction detection (reuses the DR-c grounded-synthesis pattern) **parked for the Mac** behind an escalation trigger. Absorbs the cross-doc contradiction deferred from threads A & B. (SHA-256 resumption ledger = separate, not in this design.) Spec breakdown in the doc; not yet specced.  [second-brain][rag]  (2026-06-05)
- Two-tier memory (hot-cache + deep store) — reconcile with memory-engine-research.md  [second-brain]  (2026-06-05)
- Document handling: pdf/docx/pptx/xlsx skills + interactive pdf-viewer — install as plugins at build  [assistant][integration]  (2026-06-05)
- Eval Artemis's own AI quality with withpi encoder-scorer (deterministic, Promptfoo type:pi)  [assistant][eval]  (2026-06-05)
- Assistant persona/tone prior-art: Inflection Pi (empathetic personal assistant)  [assistant]  (2026-06-05)

<!-- [2026-06-13] Future-proofing items surfaced by the local-LLM expansion research (detail: docs/research/2026-06-13-local-llm-expansion/_SYNTHESIS-PLAN.md §6) -->
- **Home-lab / local-inference future-proofing (hardware + items) → lives in its own BANK**, not here. Build checklist, hardware items, path-specific accessories, and the EXP-a/EXP-b future specs are all in `docs/research/2026-06-13-local-llm-expansion/README.md` (§"Future hardware items & build checklist"). Activated when a hardware trigger fires.  [hardware][aci]  (2026-06-13)

<!-- [2026-06-13] UI-polish thread parked back to backlog (was an In-Flight scoping row; no decisions taken). Functional client UI is already specced (CLIENT-a..f). Two genuine gaps below + an undecided discussion-mode (visual mockups vs words+wireframes vs mix). Scoping context: docs/technical/architecture/app-flow.md. -->
- **Visual identity / design system** — no design tokens or concrete "Athena-style" aesthetic; client screens are stock SwiftUI. Define tokens (colour/type/spacing/material), dark mode, the menu-bar/hotkey panel look. Touches all surfaces — do before more screens.  [ui][design-system]  (2026-06-13)
- **Domain-spoke screens** — Calendar/Tasks/Email/Finance have no dedicated client UI (chat-only by current design); give each domain a visual home. Larger surface; depends on the design-system tokens existing first.  [ui]  (2026-06-13)

<!-- From the 2026-06-08 social-media RAG/voice filter pass (dedup'd against locked SP0 specs) -->
- Spotlighting + CaMeL + delimiter discipline for untrusted web content → fold into the **Deep-Research engine spec** (still to draft; already an open question)  [rag][security]  (2026-06-08)
- Versioned retrieval — tag ingest chunks `processing/verified/searchable`, expose only `verified` to retrieval → refines **M3-a** beyond plain idempotency (relates to the incremental-indexing item above)  [rag]  (2026-06-08)
- Summary-first tiered retrieval ("context stinginess") — store raw + a summary, retrieve the summary first, pull full transcript only when needed → **M4** auto-inject + tool design; fits lean profile (relates to two-tier memory item)  [second-brain][assistant]  (2026-06-08)
- Exact-match identifiers (IDs/dates/invoice numbers) fail in pure vector search — make sure **M3-b** keyword path explicitly handles structured tokens  [rag]  (2026-06-08)
- RAGAS/DeepEval RAG eval dimensions (faithfulness, answer-relevancy, context-recall) + red-teaming as the knowledge-core eval rubric → complements the withpi eval item above  [eval]  (2026-06-08)

<!-- From the 2026-06-09 cross-module-links 4-agent dive (detail: docs/research/cross-module-links.md) — prior-art links Artemis wouldn't have designed itself -->
<!-- [2026-06-11] RELATIONSHIP/PERSONAL-CRM CLUSTER discussed → core converged onto an on-demand **Person Briefing** (open-threads + facts; auto-detect threads from comms + manual log; deliberately bounded — "not crazy"). The 4 items below are now the **opt-in extras** layered around that core. Design note: docs/findings/person-briefing-discussion.md. Discussed, NOT specced. -->
- **Person Briefing (core, DISCUSSED 2026-06-11)** — on-demand "brief me on X" → non-obvious brief (open threads/promises + stored facts; skips identity/history). Auto-detect threads from comms at ask-time (dismissable) + manual log. Bounded/passive by default. Reuses M4 + Gmail(`artemis.untrusted`). → `docs/findings/person-briefing-discussion.md`  [memory][comms][assistant]
- **Gift-budget pipeline** — birthday (Calendar) → gift idea (Person/Memory) → budget line (Finance) → shopping item; a 4-hop cross-domain chain  [integration][finance][calendar]  (2026-06-09)
- **Person↔debt edge** — "I owe X $50 / X owes me" as a bidirectional Person↔Finance link (Monica pattern)  [finance][memory]  (2026-06-09)
- **Unlinked-mention detection** — a contact/project name in an email/note/task without an explicit link → suggested connection (Obsidian/Logseq); applies across Email+Notes+Tasks+Journal  [memory][second-brain]  (2026-06-09)
- **Relationship time-decay / reconnection prompts** — surface "haven't talked to X in a while" from comms-frequency patterns, not fixed reminders (Mesh)  [memory][comms]  (2026-06-09)
- **Task-deadline-vs-meeting conflict check** — when scheduling, flag if an attendee (or you) has a hard task deadline that day (task↔calendar↔person); no surveyed tool does this — a differentiator  [calendar][productivity]  (2026-06-09)
- **News-on-contact → pre-meeting brief** — public update/job-change about an attendee surfaced before a meeting (Person→News→Calendar, Mesh)  [comms][calendar][news]  (2026-06-09)
- **Goal entity + goal-cascade** — yearly Goal → project → weekly task → daily habit; Artemis has no Goal node (Habits/Goals deferred) — the missing rollup (Tana)  [productivity]  (2026-06-09)
- **Health↔productivity correlation** — "low-energy/poor-sleep days correlate with missed tasks" cross-link  [health][productivity]  (2026-06-09)
- **Camera receipt-OCR** — photo receipt → transaction extraction; closes the cash-transaction gap email/manual can't (folds into the camera-module discussion)  [vision][finance]  (2026-06-09)
- **Place/Location entity** — unhomed shared entity (Calendar location · Travel · Maps connector); needed as later spokes add location links  [architecture]  (2026-06-09)

<!-- [2026-06-18] From the "5 Levels of an AI Second Brain" video fit-eval (verdict + detail: docs/findings/prebuild-test-review-findings.md § Video fit-eval). Artemis already spans/exceeds all 5 levels; these two are the genuine sharpening ideas. -->
- **✅ DESIGNED 2026-06-29 → `docs/findings/query-shape-retrieval-design-2026-06-29.md` — Whole-document & aggregate retrieval gap.** Design = a **query-shape router** in front of retrieval with hybrid detection (deterministic gate + constrained 1-of-N LLM fallback) → routes: pinpoint (today) · whole-doc via **build-time RAPTOR summary tree** (reserved `ChunkRecord.{node_level,is_summary,parent_chunk_id}`, background-built) · aggregate via **domain-tool dispatch + honest-decline**. Dev-buildable behind fakes; Option-C tiered whole-file-read + real summary quality are Mac-gated. Spec breakdown in the doc; not yet specced. _(Original framing:)_ vector chunk-retrieval gives *wrong/incomplete* answers for two query shapes: faithful "summarise the WHOLE doc" (top-k chunks ≠ the whole file) and aggregates over structured data ("which week had the highest sales?" grabs one chunk, answers wrong). Refines the **summary-first tiered retrieval** + **exact-match identifiers / structured tokens** items.  [rag][second-brain]  (2026-06-18)
- **Structured-document extraction (aggregate over free-text docs)** — deferred half of the query-shape design (D3, b): extract structured fields from documents at ingest + a text-to-SQL-style query path so aggregates work over arbitrary docs that were never put in a spoke table. Heavy new subsystem; the query-shape router honest-declines this case until built. Only worth it if free-text-document aggregation becomes a real need (domain aggregates already covered via spoke tools).  [rag][second-brain]  (2026-06-29)
- **Active knowledge elicitation ("grill me")** — a proactive **interview-the-owner** capability to extract evergreen knowledge from the owner's head into M4 memory/entities (the video's sharpest non-technical point: the bottleneck is *getting knowledge in*, not retrieval). No such path today — M7-c curiosity is **web-grounded only** (needs ≥2 external sources), never owner-grounded. Natural fit for the voice-first surface (M5) + M4 write path; relates to the Person-Briefing core.  [memory][assistant][voice]  (2026-06-18)

<!-- [2026-06-20] From the "agentic OS perfect-memory" 4-pillar video fit-eval (verdict + dedup detail: ~/apex/docs/research/agentic-os-video-audit.md § sixth-video addendum). Pillars 1–3 already span Artemis's second-brain corpus; pillar 4 (team scoping) out of scope (single-owner/local-first, ADR-001/002). One genuine sharpening: -->
- **✅ DESIGNED 2026-06-29 → `docs/findings/supersession-recency-grounding-design-2026-06-29.md` — Supersession-on-recall (temporal-freshness citation).** Design = two layers: **passive recency** (deterministically surface `valid_from`/source dates on facts AND chunks into the answer prompt — they're dropped today) + **active "still current"** for memory facts only (bitemporal `idx_facts_one_current` guarantees it for free); documents get dates but no active contradiction hunt. Substrate: facts ready (date just dropped at render); chunks need small additive `fetched_at` plumbing. Cross-document contradiction detection deferred to thread C (anti-hallucination). Spec breakdown in the doc; not yet specced. _(Original:)_ when the brain surfaces a past decision/fact, confirm whether anything *newer* superseded it; today's citation items are *source*-grounding, not *recency*-grounding.  [rag][memory]  (2026-06-20)

<!-- [2026-06-21] Owner ask: autonomous agents running on local/internal LLM tokens inside Artemis. CAPTURE — fit-eval + design deferred. -->
- **Autonomous agents on internal/local LLM tokens (Mini-resident)** — Artemis runs its own **autonomous agents** powered by the **local/internal model serving** (DeepSeek-coding and/or the MLX responder + future expansion-box big-context model) rather than burning cloud/subscription tokens. The idea: long-running, self-directed agents that live *inside* Artemis on the Mac Mini, doing multi-step work (research, monitoring, capability-building, spoke automation) on free local inference. **Distinct from** today's two agent ideas already in the corpus — (a) the *teacher/distill* pipeline (`distill-datagen-pipeline`, offline data-gen) and (b) M7 recipes (declarative automations) — this is **general autonomous agency** as a runtime capability. **Design tensions to resolve at triage:** scope/authority of an autonomous agent vs the locked **internal-reversible autonomy boundary** + GATE (an autonomous agent that can take external-effect actions is a much bigger trust surface than a reaction); the **agent-loop reliability** guardrails (idempotent · bounded · clean-state · externally-verified — see `docs/research/2026-06-16-agent-loop-reliability.md` + memory) must apply; **which model serves them** (DeepSeek-coding local vs MLX responder vs the parked expansion box — relates to the home-lab/local-LLM expansion BANK, `docs/research/2026-06-13-local-llm-expansion/`); token/compute budgeting now that "tokens are free but compute/RAM isn't"; and whether this is an **ACI capability-lane** item (`docs/research/homelab-control-plane.md`) or a new first-class subsystem. **Triage step 0:** define what an "autonomous agent" *does* here (a concrete first use-case) before designing the harness.  [aci][assistant][capability]  (2026-06-21)

<!-- [2026-06-18] Owner ask: explore Devin desktop on the coding side. CAPTURE only — fit-eval deferred. -->
- **Devin desktop on the coding side** — explore incorporating Cognition's Devin (autonomous AI software engineer; source: trycognition.com / Cognition Labs) into Artemis's coding workflow. **Two distinct targets to disambiguate at triage:** (a) the *meta-build* side — Devin as an alternative/additional autonomous **coder backend in APEX coding mode** (executes whole `docs/changes/` specs), overlapping the existing DeepSeek pipeline (waves/tier-policy/verify-loop); or (b) an Artemis **coding *capability*** (a "delegate-this-coding-task" spoke Artemis orchestrates — no such spoke today; nearest = ACI/homelab control-plane + desk-vision build-assistant). **Key tension:** Artemis is locked local-first / privacy-walled (ADR-001/002); classic Devin is a *cloud* autonomous engineer → cuts against the posture (softer for the build corpus = planning docs/specs, harder for anything touching user data). **Triage step 0: verify what "Devin desktop" actually is/does** — a genuinely local desktop client changes the privacy math materially vs the cloud product. Then fit-eval per the standing "external content = fit-eval, not just capture" routine.  [aci][tooling][build]  (2026-06-18)



## Triaged (pulled into SP0 / requirements)

_Item → where it went (REQUIREMENTS.md section / ADR / spec). Keeps the trail._

- **Camera/vision module** (2026-06-09) → dedicated discussion 2026-06-11 **reframed** it from a home-cameras spoke into a **vision build-assistant** (overhead desk-vision HUD + voice-first guided builds) → **ADR-014** (DESIGNED, deferred; capability ladder Rung 0→3). Mini-local, NOT a home-cameras spoke, no ACI Phase-4. Full design + research: `docs/findings/desk-vision-hud-deep-dive.md`.

<!-- - Morning email briefing → REQUIREMENTS.md §Assistant; spec: docs/changes/morning-brief.md -->
