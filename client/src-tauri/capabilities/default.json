{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main Artemis window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
