# Handoff — 2026-07-04 (session 11, planning+AFK coding): ADR-048 curated machinery + spine live smoke

Morning: Forks 1+2 decided (ADR-048), data-spine live smoke PASSED (real 22-Aug event through
sync→ingest→store→local-read→app). Afternoon (owner AFK, greenlit "finish them all"): all three
curated-machinery specs drafted (Opus) → reviewed → built (Codex) → host-verified → committed
(`3ca8a58` → `e2af8da` → `0f61074`, archived to done/). What changed → `git log`; state →
`docs/status.md`. This handoff = what the build learned.

## Decisions Made
- **Owner:** Fork 1 = A→C (ADR-048); Fork 2 = defer briefing, dogfood curated saves first.
- **Synced-domain guard design (autonomous, from data-review BLOCK):** curated CRUD refused on any
  domain with non-`curate`-source rows (dynamic, no registry) **+ static-roster names reserved even
  on an empty store** (closes pre-first-sync name-squat). Freshness bypass for curated domains rides
  the same discriminant.
- **Referent-stash semantics (autonomous):** stash = last NON-EMPTY list the owner saw
  (`stash_results` no-ops on empty) — a tracking query or empty read doesn't clobber a live referent.
- **Calibration-driven amendment (autonomous):** spec-1's empty-domain→degrade rule relaxed for
  referent ops (it was collapsing legitimate "save the second one" to none). Referent-save with no
  target → "Save it where?" refusal; forgets are domain-optional under exactly-one-or-refuse.
- **Review proportionality (autonomous):** spec 2 got dual review (security+data — 2 BLOCKs, both
  folded pre-build); spec 3 got single data review (deterministic read-side, no new trust boundary).
- Codex dispatch effort: spec 2 at `model_reasoning_effort=high` (7-file trust-boundary spec), others medium.

## Specialist Flags
- **Runtime-only provenance discriminant (data review, accepted):** curated-vs-synced rests on the
  `source` column with no schema-level enforcement — deliberate under ADR-046 #7. Revisit if a
  second writer class ever appears.
- **Tracing gap (ai-systems, house-wide, pre-existing):** no prompt/tokens/latency instrumentation
  on ANY model-facing call (intent, read, curate). Not a regression; candidate for a project-level
  observability spec.
- **Calibration corpus is hand-authored** (24 cases; owner prefers real captured data) — treat as a
  v1 smoke; rebuild the golden set from real usage once curated saves accumulate.

## Blocked Actions
- none

## What's Next (discoveries)
- **The machinery's last live leg is untested:** everything is unit/host-verified + the extractor is
  live-calibrated (22/24; adversarial idioms 9/9), but no end-to-end "add a task" has run through the
  real app. Brain was restarted from HEAD (machinery live); **client needs Ctrl+R**. First dogfood:
  "add a task: …" → "what are my tasks" → "forget the … one" → "what are you tracking for me?".
- **Watch item (calibration):** reminder-phrasings extract `domain=calendar` → the synced guard
  refuses with a steer-to-tasks reply. If that annoys in dogfood, teach the extractor prompt that
  reminders → tasks.
- **Codex sandbox env skips tests:** its in-sandbox pytest showed 13 skips vs the host's 6
  (env-dependent tests) — reaffirms host re-verify as the only arbiter. Also: Codex tried
  `uv sync --group agentic` per stale AGENTS.md (v1 doc, rewrite already queued).
- **Session-10 metric drift:** "mypy 82 files" in prior handoffs ≠ canonical `uv run mypy` (169
  files, config `files=["src","evals","tests"]`) — the gap hid 3 untyped defs in
  `test_calendar_sync.py` (fixed in `3ca8a58`). Use the canonical invocation in future verify counts.
- **Dispatch mechanics note:** a `codex exec` detached with `&` inside a backgrounded compound loses
  its completion notification (worked around with a file-watcher on the `-o` final). Dispatch Codex
  as its own `run_in_background` call.
