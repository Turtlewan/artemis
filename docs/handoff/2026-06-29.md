# Handoff — 2026-06-29 (agency v0 + proactivity build session, spanned from 2026-06-28)

_Read-once inbox of what this session learned that git + status.md can't reconstruct. What changed → git log; build status / queue → status.md; specs finished → done/._

## Decisions Made
- **Agency scoped to Option 2 (due-date), B1 suggest/accept-only, no GATE** — pre-flight grounding caught that the built `suggestion.accept` can't time-block an *existing* task (the `suggestions` table has no task ref / no `scheduled_block`); time-block would need a schema change. Owner chose due-date. "Never auto" is enforced **structurally** (the schedule write is not a reactive tool; only the owner-route accept reaches it), so no GATE was composed. Locked in `DECISIONS-LOG.md` § Agency & Proactivity.
- **Did NOT apply the agency review's FLAG-1 (reject→500) blindly** — verified against source: `reject_suggestion` is a silent idempotent no-op (0 rows on unknown), not a `KeyError`→500. The review mis-assumed. Reject kept idempotent by design.
- **Idempotency fix = claim-first** (`UPDATE … WHERE status='pending'` before `create_task`); reject made conditional-on-pending too (blocks accept→reject overwrite). Chose idempotent-by-design over adding 404s.
- **B (proactivity) scoped to finance-only; calendar deferred** — registering the calendar manifest would need OAuth-gated store construction AND would expose `calendar.create` (external effect) reactively. Deferred to Google-spoke go-live. Finance is local (no external effect); productivity hooks already live from the agency build's `tasks_manifest` registration.
- **Builds + review ran on the Codex pool to conserve Max** — Codex (gpt-5.5) built both specs; review used a Codex multi-lens ensemble + bounded Opus adjudication. Validated and parked in `~/.claude/skills/apex-code/REVIEW-ENSEMBLE-DESIGN.md` (keep BOTH models — coverage was near-disjoint).

## Specialist Flags (non-obvious gotchas)
- **THE load-bearing security invariant for this whole area:** registering a module manifest registers its *tools* into the model's reactive registry, and `brain.py:266` dispatches them with **no approval gate**. So the "never auto" / "no un-gated external action" guarantee depends entirely on **`include_write_surface=False`** keeping write tools out of the registry. This pattern now gates agency (tasks) + proactivity (finance) and MUST gate calendar when it lands (its `calendar.create` is genuinely external-effect). Any future "register module X" spec must consider this first.
- **Codex sandbox can't run vitest** (esbuild reads `../..` → outside the repo-confined sandbox). Host must run client `npm test` after a Codex client build — Codex wrote a passing-looking test it couldn't execute; host found + fixed a React-input-setter bug in it.
- **winsdk teardown flaky recurred** (`0xc0000374` heap-corruption at suite teardown, exit 0, tests pass) — same known issue status.md tracks; green on clean re-run; unrelated to changes.

## Blocked Actions
- none. (Deferred *levers* — GATE/agentic executor, calendar hooks, free-form "schedule X", time-block scheduling — are deliberate scope choices, recorded in DECISIONS-LOG, not blocks.)

## What's Next (discoveries)
- **The deep-review backlog is the highest-value next work.** Two read-only surveys found that NONE of the built corpus has had an ultra-grade review, and the dominant review-debt theme is **concurrency / race correctness in the always-on background + auth** (P0: auth counter-monotonicity race in `devices.json` [no lock], `tier1_queue` mutate-before-persist races, memory bitemporal write races, staging state-machine flip). Full ranked list in `REVIEW-ENSEMBLE-DESIGN.md`. The validated ensemble harness is now the cheap (off-Max) tool to audit these.
- **The review-ensemble works** — it caught a real idempotency race on agency (fixed) and correctly cleared B. Worth wiring as an opt-in apex-code step (`review_ensemble: true`) for high-risk specs.
- **Agency test-hardening** (3 low items the agency correctness lens flagged: exact-equality registry assertion, full back-compat set, accept/reject unknown-id tests) — B's build already adopted the stronger pattern; agency's tests could be retro-fitted.
- **C draft** (`client-detail-action-wiring`) still pending — guard/wire the `complete`/`reschedule` dead buttons.

---

# Handoff — 2026-06-29 (cont.) — KCQ knowledge-core quality wave + 2 hardening specs BUILT

_8 specs built (Codex gpt-5.5, Opus host orchestration + cross-model review), all host-verified full `uv run mypy` + full `uv run pytest -q`, committed. Specs archived to done/. What changed → git log; queue → status.md._

## Decisions Made
- **kcq-4/5/6 committed as ONE commit** (`7af0dbd`), not three. All three interleave in `brain._rag_messages` on the same lines, and `git add -p` is unavailable in this shell (non-interactive), so per-spec hunk-splitting of `brain.py` was impossible without risking a broken intermediate commit. They are a single serial chain (recency + anti-hallucination answer-layer) verified together. All three spec files still archived individually to done/. **Review needed? No** — clean technical necessity, recorded here.
- **Two host-side `ruff format` fixes applied** to `memory/store.py` (kcq-4) and `tests/test_citation_check.py` (kcq-6). Codex's *scoped* `ruff format --check` (touched-files-only) passed, but the host's full-tree check flagged them. Pure formatting, zero logic change. **Lesson:** Codex's scoped format check can disagree with the full-tree check — host must always run full `ruff format --check .`.
- **`staging-at-most-once` built + committed now despite its "gated to Google-spoke go-live" status note.** The note is *operational* (the at-most-once guarantee only bites once a twin performs a real external effect, i.e. at go-live) — the code itself (terminal `FAILED` status + no-rollback-after-dispatch) is pure staging-service logic, dev-buildable and unit-tested now. Shipping it early is safe (no behaviour change for in-process twins) and removes a go-live blocker.

## Specialist Flags (non-obvious gotchas)
- **`harden-background-invariants` directly closes the prior session's P0 deep-review finding.** The auth counter-monotonicity race (`devices.json`, no lock) and the `tier1_queue` mutate-before-persist race that the deep-review backlog flagged as the single highest-value debt are now closed by-construction (cross-platform advisory `file_lock` + `threading.Lock`). The `tier1_queue` drain takes the lock ONLY for the snapshot and releases before the loop — `_remove`/`_fail` self-acquire, so a non-reentrant Lock does not deadlock (verified in review). Memory bitemporal + the broader race backlog remain.
- **Build was parallelised across the file-disjoint specs.** staging + harden ran as concurrent background `codex exec` subprocesses alongside the kcq chain and the host's full-pytest runs (all file-disjoint, and pytest collects at start so mid-run edits don't perturb it). 3 full-pytest runs total at wave boundaries (kcq-1/2 → 965; kcq-3+staging+harden → 975; kcq-4/5/6 → ~984/990), each ~15 min — the real wall-clock cost of this build.
- **Codex's restricted sandbox does not run the full pytest** (and was told not to) — it ran targeted spec tests + `uv run mypy` only; the host ran every full-suite verification. Per the standing host-verify rule.

## Blocked Actions
- none.

## What's Next (discoveries)
- **Live shape-aware retrieval is now wired but unexercised on real data.** kcq-2's summary-tree build is a `pre_tick_step` that only runs when unlocked with real ingested docs; kcq-3's WHOLE_DOC route falls back to pinpoint until summary nodes exist. First real signal needs an on-box run with ingested documents (owner-paced).
- **kcq AGGREGATE route is an honest-decline + domain hint, not a real aggregator** (by design, C1 cheap floor). The actual "route to the Finance/Calendar/Tasks query tool" wiring is a separate future step — the hint text names the tool but nothing dispatches to it yet.
- **Anti-hallucination floor is the cheap deterministic tier only** (gap-tag + citation-presence). The model-driven claim-audit + cross-doc contradiction tier is Mac-parked (design doc Decision C1, heavy tier) — out of scope, not built.
- **The remaining deep-review backlog** (memory bitemporal write races, plus the rest in `REVIEW-ENSEMBLE-DESIGN.md`) is still the highest-value next work now that the auth/tier1 P0s are closed.

---

# Handoff — 2026-06-29 (cont. 2) — FIRST LIVE CLIENT↔BRAIN BRING-UP + email-keystone test

Owner drove the first real end-to-end client↔brain session on the Windows box. **Full gap list +
detail: `docs/findings/client-live-bringup-gaps-2026-06-29.md` (Findings 1–8).** Headlines:

## Decisions Made
- **Pairing→connect→unlock WORKS live** — the handshake that previously died at `session/complete 401`
  now reaches connected/unlocked on real hardware (closes the D3 / CLIENT-auth Task-7-4b live tail).
- **Applied 3 fixes + 1 dev-unblock this session** (committed): NeuralWeb unplaced-domain crash
  (`c6a8e57`), neural-pulse alignment + card hover emphasis (`ab0f19b`), loopback rate-limit exemption
  (`ef313d1`, brain — effect on restart). **Ask hotkey fix (Alt+Space→Ctrl+Alt+Space) is STAGED, not
  applied** — editing `lib.rs` forces a tauri-dev rebuild mid-poke; apply at next client restart.
- **Email-extraction keystone tested directly (no OAuth)** via the dev harness with fake Gmail + real
  local model. **Pipeline is built & correct** (email→task + email→calendar-event reactions fire in
  observe; `test_dev_email_rules.py` green) **but `qwen3:4b` can't produce the structured JSON** the
  extraction needs (JSONDecodeError → degrades to nothing). Keystone is blocked on **model capability,
  not code**.

## Specialist Flags
- **The keystone's real blocker = local-model structured output.** `OpenAIModelPort` sends an OpenAI
  `response_schema` that Ollama's qwen3:4b ignores → prose → JSONDecodeError. Fix = Ollama-native
  `format: json` / `response_format` + `num_ctx`, or a stronger model. Highest-value pre-req for the
  live email demo. (Finding 8.)
- **Client is a built shell with live data wired for only 1/11 domains** (`app_review_pending`); the
  other 10 read-commands don't exist (Finding 5) → cards show "Data not yet available."
- **Brain `default_layout()` mismatches the client** on taxonomy + coordinate space → empty map until
  the in-app **Reset layout** button reseeds (Finding 3).

## Blocked Actions
- none hard-blocked. The findings are planning work, not blocks. Several are BIG forks (auth/packaging/
  data-contract) deliberately NOT hot-patched.

## What's Next (discoveries)
- Owner's plan: **run the email/OAuth tests tomorrow, then planning mode to fix the 8 findings.**
- Tomorrow's email pass = (1) fix local-model JSON output → (2) see real extraction → task + calendar
  → (3) live Gmail via OAuth (`artemis-google-auth login`; re-surface OAuth steps at exercise time).
- A "launch & demo cleanly" workstream surfaced (Finding 7): dummy-data demo mode, single-`.exe`
  launch, default/auto pairing, theme override. Recommend specc'ing as one demo-readiness milestone.
