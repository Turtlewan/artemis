# Handoff — 2026-06-24 (coding)

Long coding day. Earlier: M6 delivery chain (M6-chain-prereqs → M6-b → M6-c) + M7 self-improvement line (M7-a1/a2/a3/b) + M4-c-2 — all built & committed (see `git log` + status.md In-Flight; the M6-chain blocker that paused the *prior* sub-session was resolved in planning and is now COMPLETE). This continuation session ("resume build") began the **cluster build** per `BUILD-ORDER.md`, serial via Codex, host-re-verified + committed each. Landed: **Wave F0** (X3-runtime-config, M6-wake-trigger, M8-d-a-areas-drop, M8-d-a2-projects) + **M8-a Google-auth foundation** + **M8-d-c1 wake-digest hooks** + **CAL-a Calendar read/find-time/prefs (CalPrefs folded)**. Baseline green @ `ac3d9a1`, 355 tests.

## Decisions Made

- **Cadence (owner directive this session):** once a build is greenlit, run autonomously through the waves — do NOT checkpoint per-wave; halt only for a genuine blocker/fork or to report at the end. Dispatch stayed **serial** (owner's F0 choice) even though F0/F1 are file-disjoint.
- **M8-d-a `store.py` fold-in.** The areas-drop spec's Files-table omitted `store.py`, but the M8-d-a build (07c63e2) had added it as a `ProductivityStore` facade; the spec's own whole-dir sweep gate required cleaning it. Folded in as a mechanical mirror of the repository edits. Review-needed ⚠️ only as a roadmap note (build-introduced files won't appear in amendment Files-tables).
- **M8-d-a2 fq-id naming resolved against the live registry** (`fq = {manifest.name}.{tool.name}`, registry.py:55). Spec delegated this to live-code resolution. Chose: bare last-segment task/project tool names, but `suggestion.*` keeps its prefix (else `create`/`list` collide with task tools on the `tasks` manifest) → `projects.create` / `tasks.create` / `tasks.suggestion.create`. Matches the spec's documented fq-id map. **F1's M8-d-b/c1 will inherit this scheme.**
- **Two spec miscounts corrected to live reality:** areas-drop tool count = **22** (Files-table's "30→27" was a typo; Task 3 + acceptance criteria said 22); projects-split projects-manifest = **6** tools incl. `project.tasks` (done-when's "5" undercounted, but the spec's own §Tool partition lists project.tasks on projects).
- **M8-d-c1 reconciled `test_productivity_core` assertion** (`tasks.proactive_hooks == []` → `len == 3`) — forced by the spec's own required end-state; one line, outside the spec's listed scope.
- **M8-a deps installed host-side** (`uv add google-auth google-auth-oauthlib google-api-python-client`), NOT inside the Codex sandbox (Windows artifact-ownership rule). pip-audit clean for the 3 Google libs. Task 7 (live OAuth + keyed SQLCipher) gated.
- **CAL-a `# noqa: N802` on `calendarList`** — mirrors the Google API method name `service.calendarList()`; renaming breaks the API surface.

## Specialist Flags

- **M8-a is auth/secrets domain.** In-spec security invariants were honored + host-spot-checked (token-repr redaction, no hardcoded `redirect_uris`, `key.as_hex()` local-only, `invalid_grant`→reauth, CSRF state). **Recommend `/code-review ultra` before any real OAuth deployment** (billed, owner-triggered) — the live consent/token path (Task 7) is gated and untested on dev.
- **CAL-a CalPrefs focus-window biases slot RANKING only**, not the underlying find_time band algorithm — confirm that matches the intended UX on-hardware.
- **Pre-existing dependency CVEs** surfaced by pip-audit (starlette, torch, yt-dlp) — unrelated to this session's deps, predate it. Planning should schedule a dep-bump pass.

## Blocked Actions

- **M3-a docling dependency decision — PARKED by owner choice.** The cluster's critical path (Gmail M8-b1 → Finance → sensitivity-P → CAL-d) needs **M3-a's IngestPipeline**. M3-a code is dev-buildable as written (docling lazy-imported behind `DocumentParser`+`FakeParser`, real parse = Task-7 gated). The open call is M3-a's `uv add docling` — docling is a heavy torch-scale ML dep on the 8GB box; In-Flight leaning = make it an **extra** so dev stays lean. Owner chose **"build the M3-a-independent set first"** and settle docling when only the M3-a-gated chain remains. Full capture: `docs/progress/cluster-build-2026-06-24.md`.

## What's Next (discoveries)

- **`BUILD-ORDER.md`'s "F1 = 4 amendments" framing is misleading** — the Gmail/Calendar/hook BASE modules were never built, so the cluster actually proceeds by building those bases dev-first and FOLDING the F1 deltas in (each F1 spec says "or this folds into its build"). The "prereq layer" (M8-a/M8-b1/M3-a/b) was unbuilt at session start. Update the roadmap's mental model: cluster = build-bases-with-folds, not amend-existing.
- **The "stub-never-built" pattern persists** (noted in the prior handoff): `hooks.py`, the entire `calendar/` module, etc. were CREATE not modify. Pre-flight every original-corpus spec against the live tree before dispatch.
- **CAL specs are large + slow** — CAL-a is 2515 lines and **hit the 10-min Codex timeout** (but had finished writing; recover by re-verifying the tree, not re-dispatching). Budget a longer timeout for CAL-b/CAL-c, or split.
- **Remaining M3-a-independent buildable set (resume here):** `CAL-b` (write+attendee-gate→GATE-a, built) → `CAL-c` (overlay + 7 hooks) → `M8-d-b` (time-blocking + focus-slot; needs CAL-a/b + adds the richer `tasks_manifest` signature the M8-d-c1 fold deferred) · `M4-d-2` (writepath resolve; touches brain/gateway) · `OBS-a` (new obs/ pkg; touches brain.py/distill.py). Then resolve docling → Gmail/Finance/sensitivity/CAL-d.
- **Codex build method that worked:** serial `codex exec -p apex-coder`, host installs deps as normal user (never in-sandbox), host runs the full recipe (`mypy --strict src tests` / ruff / full pytest) + commits. Stale `/Users/artemis-build/` paths adapted in-place via the prompt.

---

## Continuation 2 (later 2026-06-24) — M3-a-independent set COMPLETE

Drained the entire "Remaining M3-a-independent buildable set" listed above. Serial Codex (`apex-coder`), host-re-verified + committed each. **Baseline green @ `7006b15`, 401 tests.**
Shipped: **CAL-c** (f66701c — overlay + 7 §D hooks, 377) → **M8-d-b seam** (38b67eb — `calendar.schedule_task` + `tasks.schedule` + Task↔Event link, 388) → **M8-d-b focus-slot-pick** (a830043 — focus-window slot bias, 390) → **M4-d-2** (f6616c8 — subject→PERSON write-path wiring + `resolve_entity` tool, 395) → **OBS-a** (3e77934 — JSON logging + redaction + `ObservabilitySink` + error capture + brain/distill taps, 401).

### Decisions Made (continuation 2)
- **Bare ToolSpec names enforced as the live convention (B9)** across CAL-c + M8-d-b — overrode the frozen specs' literal dotted `name="calendar.schedule_task"`/`"task.schedule"`; the registry composes fq ids. Confirmed against CAL-a/CAL-c live manifests.
- **M8-d-b: optional injection params to protect out-of-scope callers.** The frozen seam's signature changes to `make_calendar_manifest`/`tasks_manifest` would have broken ~10 existing test callers (`tasks_manifest(store)`, `make_calendar_manifest(tools, write_tools)`) that are NOT in the spec's file scope. Made the new params (`schedule_task_fn`, `schedule_fn`, `write_tools`) **optional (default None)** — additive, callers untouched, the new tool appears only when wired. Tool-count assertions made **relative** (the spec's `30→31` is pre-Areas-drop/pre-split).
- **M4-d-2: live `SqliteMemoryStore` exposes no `conn`/`person_id`/`repository` accessors** (the spec's `memory_manifest(store)`/`store.conn` can't work) and `build_write_path` was already reconciled to 3-arg `(repo, embedder, model)`. Resolved in-scope: added read-only `conn`/`person_id` **properties to `BitemporalRepository`** and made **`memory_manifest(repo)`** repo-based; gateway registers `memory_manifest(repo)` using the in-scope `repo`+`OWNER_PERSON_ID`. No `store.py`/`entities.py` edits.
- **OBS-a: distill is a `DistillService` class, not the spec's assumed module-function.** Injected `obs: ObservabilitySink = NullSink()` on `DistillService.__init__` (backward-compatible) with the `on_escalation`/`on_error` taps inside the method; tests build `DistillService(..., obs=spy)`. Brain `_router.route` is async → route-decision tap placed in the success path after the route try/except.

### Specialist Flags (continuation 2)
- **M4-d-2 ships an owner-private cross-module read tool** (`memory.resolve_entity`). Security guards host-spot-checked: two-layer module guard (`Literal["memory"]` + runtime assert), sanitized `EntityNotFound("entity not found")` (no id echo / probe side-channel), PII-safe logging (exception **type name only**, never `ef.subject`/`ef.object`), registration-gated (no store ⇒ not registered). The spec's standing apex-security FLAG holds: `data_scope`/`action_risk` are **advisory** in M1 — the effective wall is registration-gating + keyed-store binding; **dispatch-time scope enforcement is the deferred M2/M5 item** — confirm when M2/M5 land.
- **OBS-a is the no-PII redaction core.** Guards spot-checked (content-key drops at all levels, redact-before-store, append-only `os.fsync`, primitives-only Protocol, non-raising `CompositeSink`). The `redact()` base64/hex length heuristic is **defence-in-depth only** (documented) — the primary controls are key-name matching + the no-content Protocol.

### Blocked Actions (continuation 2)
- **M3-a docling fork is now the SOLE critical-path blocker** — the independent set is drained, so every remaining cluster spec (Gmail M8-b1/b2 → Finance → sensitivity-P → CAL-d) sits behind the docling-as-extra-vs-core dep decision. This is a **PLANNING/OWNER** call (heavy torch-scale dep on the 8GB box) — route to planning to resolve, or pick an off-critical-path spec below.

### What's Next (discoveries, continuation 2)
- **OBS-b is now UNBLOCKED** — OBS-a shipped the `ObservabilitySink` Protocol + `CompositeSink`, so OBS-b (SQLCipher telemetry backend persisting token/cost/latency via a concrete sink) is buildable now (needs only OBS-a + the M2 sqlcipher seam, both done). Natural completion of the observability story.
- **M7-c (curiosity loop) is buildable** but needs `uv add tldextract` (mutates pyproject/uv.lock → build serially, host-side dep install). Reads M7-a2's OBS escalation tap via Protocol; builds against fakes.
- **`FactRow.subject_entity_id` was pre-staged** (existed before M4-d-2 built — from M4-d-1/reservations) — the real M4-d-2 deliverable was threading it through `add`/`update`/SELECTs + `facts_for_entity`, not adding the field. Expect more pre-staged columns/fields in the M4 family.
- **So the next coding choice is:** (a) **OBS-b** (finish observability, fully unblocked), (b) **M7-c** (needs a dep-add), or (c) switch to **planning** to resolve the docling fork and reopen the Gmail→Finance→sensitivity critical path. No coding-mode blocker on (a)/(b).

---

## Continuation 3 (later 2026-06-24) — M7-c → OBS-b (observability story closed)

Owner chose **Build OBS-b** — but OBS-b imports `artemis.curiosity.gaps` (the M7-c `TelemetrySource` contract), which was **unbuilt**. Surfaced the discovered dependency; owner chose **Build M7-c → OBS-b**. Both shipped (serial Codex, host-verified). **Baseline green @ `462d737`, 422 tests.**
Shipped: **M7-c** (9d2541c — Curiosity Loop: idle gap-scan → curriculum → Researcher-stub + grounding gate → token-capped staging → owner-gated commit; + `uv add tldextract`, 416) → **OBS-b** (a1342dd — SQLCipher telemetry store + `TracingModelPort` token/cost/latency + tier-aware `CostModel` + `TelemetrySink`/`SqliteTelemetrySource`, 422).

### Decisions Made (continuation 3)
- **OBS-b's real dependency was M7-c, not just OBS-a.** OBS-a shipped the `ObservabilitySink` *write* Protocol; OBS-b also needs M7-c's `TelemetrySource` *read* Protocol + `scan_gaps`/`Gap` (for its end-to-end test). Resolved by building M7-c first (owner-approved). Correct order is M7-c (defines the contract + builds against fakes) → OBS-b (implements `SqliteTelemetrySource`).
- **`tldextract` added** (5.3.1, + filelock + requests-file) host-side, pip-audit clean for the new deps. The 27 pre-existing CVEs (starlette/torch/yt-dlp) are unchanged.
- **M7-c: heartbeat.py left untouched** — `make_curiosity_hook` is self-contained in `curiosity/__init__.py` (the spec's heartbeat edit was "only if needed").
- **OBS-b: `TracingModelPort` matched the live `ModelPort`** (live `complete` has no `stream` arg; streaming is a separate `complete_stream`) rather than the spec's stale `complete(..., stream=...)` signature.

### Specialist Flags (continuation 3)
- **M7-c grounding gate is the anti-collapse control** (≥2 independent external reachable sources, never self-generated; owner-gated commit only). M7-c ships only the **stub** Researcher + the gate — the real Deep-Research engine (spotlighting + CaMeL on untrusted web) lands separately and carries the egress security wrapping. Curiosity-distilled recipes enter as CANDIDATE through M7-b's owner gate; `curiosity_tick` never writes the live store (verified by test). Task 7 (live web egress) gated.
- **OBS-b store is owner-private SQLCipher** (no plaintext telemetry file); tracer WARNING paths log only `role[:64]` + `error_type` (no message/response/exc-string); `TelemetrySink.on_route_decision` records ONLY a route row (the reviewed duplicate-escalation bug stays fixed). Verified by the test suite.

### What's Next (discoveries / follow-ups, continuation 3)
- **Two minor planning follow-ups raised this session (both 1-line spec amendments, deferred — not overridden mid-build):**
  1. **tldextract 5.3.1 deprecates `.registered_domain`** (→ `.top_domain_under_public_suffix`). M7-c's `research.py:registrable_domain` uses the spec-mandated `.registered_domain` → ~18 DeprecationWarnings per test run. Migrate the one line in a spec amendment.
  2. **OBS-b `tier_for` predates the `codex` adapter** — codex currently maps to `LOCAL` (else branch). Flat-rate cost is 0 either way, but codex tokens aren't tagged as the SUBSCRIPTION quota signal in `usage_summary`. Add `codex`→SUBSCRIPTION (same as `claude-cli`) in a spec amendment.
- **Observability is now end-to-end buildable** (OBS-a Protocol + error pillar; OBS-b store + tracer + cost + concrete sink/source; M7-c consumes the source). The remaining wiring (`CompositeSink` + `TracingModelPort` at the composition root) is OBS-b's GATED Task 7 (on-Mini, real ModelPort + DEK).
- **The M3-a docling fork is STILL the sole critical-path blocker** for the main cluster (Gmail M8-b1/b2 → Finance → sensitivity-P → CAL-d). Nothing this session touched it. Next coding wave on the critical path needs that planning/owner decision first.
- **Next off-critical-path independent options** (if continuing in coding mode): the remaining M7 line is complete; M5 voice is HW-gated; CLIENT specs await the Tauri rewrite (ADR-028, planning-pending). So the natural next move is **planning** — resolve docling, and/or the Tauri client spec-rewrite.

## Continuation 4 (later 2026-06-24) — CLIENT Tauri **Milestone A** (brain-side + full frontend, mocked invoke)

Owner chose **"lets build" → Full CLIENT Milestone A**: the brain-side app surface + the entire travel-zoom map frontend behind a mocked `invoke` (MSVC-free). Built serial via Codex (host re-verified + committed each). **Whole tree green: Python 442 passed / mypy+ruff clean; client tsc + vitest 54 + build + eslint + cargo-fmt all 0.**

Shipped (in dep order): **CLIENT-a** brain app-auth (`9682d2d`) → **M2-c broker client dev-subset** (`13599fe`) → **CLIENT-b** app HTTP surface (`7b0ee9b`) → **CLIENT-core** Tauri foundation (`ce20f78`) → **CLIENT-theme** (`7eb8634`) → **CLIENT-world** (`bccd18b`) → **CLIENT-card** (`4ecea6c`) → **CLIENT-screens** (`8e77a21`) → **CLIENT-ask** (`f122d1d`). All 9 specs archived to `done/`.

### Decisions Made (continuation 4)
- **⚠ FORK RESOLVED (owner: "can we build it?") — the M2-c broker hole.** CLIENT-b assumed an M2-c `BrokerKeyProvider`/`BrokerClient` that was never built (M2-c had shipped only the SQLCipher shim + the bare `KeyProvider` Protocol). Determined the Swift SE broker *server* (M2-a) is genuinely Windows-impossible, but the brain-side Python broker *client* (M2-c Tasks 1–2) IS dev-buildable against a fake socket → **built it** (`13599fe`): `BrokerClient` + `BrokerKeyProvider` with a cross-platform best-effort page-locked secure buffer (VirtualLock/mlock + mandatory zeroize). CLIENT-b then extended it (pair/begin_unlock/complete_unlock). The real broker IPC round-trip + HW mlock stay Mac-gated. **This expanded scope beyond the 8 CLIENT specs — flagged for planning: M2-c is now partially built (Tasks 1–2); its Tasks 3–5 (sqlcipher wrapper already seamed, tier0 key, launchd/autologin) + Swift M2-a remain.**
- **CLIENT-a SMALL fork:** spec Task 4's prescribed gateway delegation (`handle_text → handle_text_scoped(text, OWNER_SCOPE)`) silently dropped the live M2-c lock check; restored the lock-aware unscoped path (→ "LOCKED") while keeping the scoped seams. Net security fix.
- **CLIENT-b per-domain reads built fake-by-default for ALL five** (calendar/tasks/projects "real" per the amendment, but they need a DEK + Google OAuth — both broker/Mac-gated on dev) via an injected `DomainReadSource` seam; live module composition swaps in on the Mini. Logged in the commit.
- **CLIENT-b main.py** wires the real `BrokerKeyProvider` lazily with a relay-only prover (the relay unlock flow doesn't use the injected auto-prover).
- **CLIENT-core/screens DTO drift:** the TS DTOs mirror the **as-built** CLIENT-b wire shapes (`cards`/`id`/`domain`/`w`/`h`/`version`), not the specs' stale `dto.ts` text (`placements`/`domain_id`). CLIENT-screens defines *richer* per-domain DTOs per the locked detail designs + renders against fakes; live wire-mapping deferred.
- **CLIENT-screens registry wiring:** added a 1-line `import "./screens/registry"` to `App.tsx` (the spec's file list omitted the side-effect import needed for `registerDomain` to run).
- **jsdom devDep added** for `AskPopup`'s focus-trap/Esc behavioral tests (the only DOM-env suite; the other 12 use string `renderToStaticMarkup`).

### Specialist Flags (continuation 4)
- **Security review run on the auth wall (CLIENT-a + broker + CLIENT-b):** dispatched `apex-wave-reviewer` ×2 (apex-auth + apex-security). **apex-auth = CLEAN** (counter boundary incl. 0≤0, constant-time, two-tier guard, scope-from-session, no-secret-logging, pairing verify-before-consume + rollback, domain separation all verified). **apex-security = FLAG (no BLOCK)** → hardened in-place: SSE re-checks the vault BEFORE the first chunk + wraps the generator to emit a terminal frame on exception (fail-closed, + regression test); broker base64 fields decoded `validate=True`→`BrokerError`; DeviceRegistry temp file `chmod 0600` before replace. Accepted defense-in-depth (broker/proxy trusted): unlock_complete signature passthrough, /admin/pair-code proxy advisory.
- **CLIENT-core cross-model review (Opus on Codex output):** token-in-Rust posture verified — `Zeroizing<String>` + manual Debug redaction, strict CSP (no unsafe-inline/eval, ipc-only), default-deny capabilities, invoke-only facade (no JS fetch/token). Clean; one minor note (`token()` returns a non-Zeroizing clone to build the header — acceptable).
- **`/code-review ultra` recommended before shipping the auth surface for real** (billed, owner-triggered) — the live pairing/session/unlock + OAuth paths are gated and untested on dev.

### Blocked Actions / Gated (continuation 4)
- **MSVC tail (the whole Rust compile).** The dev box is GNU-default with no `cl.exe`; `cargo check`/`clippy`/`test`, `tauri build`, `cargo audit`, and `Cargo.lock` generation are all gated. The Rust gateway, the `#[cfg(test)]` suites (token-never-leaks, zeroize-on-logout, SSE frame parsing, bearer/401/423), the CLIENT-auth FFI keystore plugin, and the global-shortcut plugin are **written but uncompiled**. Unblock: `winget install …VCTools` + `rustup default stable-msvc`.
- **CLIENT-auth not built** — its native NCrypt/TPM/SE FFI keystore plugin is MSVC + HW gated; correctly excluded from Milestone A. The frontend connection/lock state machine + transport seams exist for it to compose.

### What's Next (discoveries / follow-ups, continuation 4)
- **Milestone A is the "see the map move" target** (`cd client && npm run dev` → the map shell + domain overlays + Ask popup in a browser, invoke mocked). **Milestone B (end-to-end)** = install MSVC → build the Rust core + CLIENT-auth FFI + `tauri build`, wire a dev model (`dev-model-stack-ollama`), and run brain-side CLIENT-a/b live.
- **New planning follow-ups raised:** (1) **M2-c is now partially built** (broker client Tasks 1–2) — reconcile the spec status (Tasks 3–5 + Swift M2-a remain). (2) **client npm-audit: 5 advisories**, all dev-toolchain transitive (esbuild→vite→vitest, GHSA-67mh-4wv8-2f99 dev-server) — NOT in the shipped Tauri bundle; fix needs vite@8 (breaking). Dev-dep bump pass. (3) **CLIENT-screens** carries richer per-domain DTOs than CLIENT-b ships — a live wire-mapping/contract-reconcile pass is needed when the brain reads go live (gmail/finance backends also unbuilt). (4) **CLIENT-theme** parked: 7 draft palette cells (hand-tune + a11y-pass → flip `vetted:true`) + 16 bundled photo assets (gradient fallback ships now). (5) **CLIENT-b `/app/layout` + per-domain reads** + the contract change were ratified inline — confirm at next planning review.
- **The M3-a docling fork remains the sole critical-path blocker** for the main cluster (Gmail→Finance→sensitivity-P→CAL-d) — untouched this session.
