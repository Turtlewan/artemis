# Handoff — 2026-06-28 (owner-manual activation tails, on-box)

Session type: **ops walk-through** of `docs/technical/setup/owner-manual-activation-runbook.md` on the
enrolled Windows box, with two sanctioned planning-mode code fixes to unblock it. The activation runway
was unit-built (2026-06-27) but had **never been run end-to-end** — this session ran it for the first
time and surfaced **6 first-run gaps**. Three tails now genuinely work; two are real build gaps.

## Decisions Made
- **Fixed `artemis-brain` entry point inline (sanctioned one-off, owner-approved).** Was
  `scripts.run_brain:main` pointing at an unpackaged top-level `scripts/` file (wheel ships only
  `src/artemis`). Moved the launcher to `src/artemis/run_brain.py`, repointed the entry point to
  `artemis.run_brain:main`, deleted the orphan. The archived `win-brain-runtime` spec actually
  *specified* an in-package path — the build deviated. **Code change committed-pending** — confirm at
  next planning review (it's a one-line entry-point + file move, zero logic change).
- **Set `ARTEMIS_DATA_ROOT` to `%LOCALAPPDATA%\Artemis`** in `config/.env.dev` (local, gitignored).
  The DPAPI `WindowsKeyProvider` refuses a key store outside `%APPDATA%`/`%LOCALAPPDATA%`; the corpus
  default `/opt/artemis` fails that guard on Windows. (Stale Mac `ARTEMIS_WORKTREE_ROOT` left as-is —
  didn't block; noted in runbook.)
- **Enrolled a Windows Hello PIN** to satisfy the unlock gate (the box has no biometric). PIN counts
  as a Hello verifier — the accepted ADR-025 dev-wall downgrade. Live unlock now works.
- **Google login used the `--scope` workaround** (`gmail.readonly`) because the CLI registers no
  scopes on its own (see Blocked).
- **Did NOT fix the client-pairing or harness-robustness gaps** — those are planning→build work, not
  ops one-offs. Filed to BACKLOG instead.

## Specialist Flags
- **(security/robustness) `QuarantinedReader.read()` crashes the batch on a model transport error.**
  The `model.complete()` call is *outside* the try/except that handles parse failures, so an Ollama
  `400` (or any HTTP/timeout error) on a single untrusted email kills the whole poll. The untrusted-
  input reader should degrade-and-continue on transport errors exactly like it does on JSON-parse
  errors. `untrusted/quarantine.py:132-140` vs the `except` at `:143`. (BACKLOG, 2026-06-28.)
- **(ai-systems) Ollama ignores the OpenAI `response_format: json_schema`** — `qwen3:4b` returned
  prose for every quarantine read (7/7 → `JSONDecodeError` degrade). Needs Ollama's native `format`
  param (or prompt-coax / different reader). The `400` was likely `num_ctx` overflow on a long email.
  Exactly the tuning signal the observe harness exists to surface. (BACKLOG, 2026-06-28.)

## Blocked Actions
- **Client overlay demo (runbook Steps 3–5) — BLOCKED on an unwired pairing UI.** The Tauri client
  builds + launches clean, but boots to `connection.state="unpaired"`; `App.tsx:131` renders `null`
  until connected, and **nothing in the running app renders a pairing surface or calls `pairDevice()`**
  (`auth/pairing.ts`, referenced only by its own test). Permanent blank screen, no way to pair. Needs:
  build a pairing screen + then exercise the never-run live `auth_pair`/`auth_connect`/`auth_unlock`
  + DER-signature handshake (the gated `CLIENT-auth` Task 7 / Step 4b; possible X9.63-vs-SPKI fork).
  Planning→build. (BACKLOG, 2026-06-28.)
- **`artemis-google-auth login` registers no scopes → bails** (`"no scopes…"`). `cli.py login`
  computes `required_scopes()` but imports no connector modules. Root cause: inconsistent
  registration — `modules/calendar` registers at module import (top-level), but `modules/gmail`
  registers only inside `build_gmail_manifest()` (`register_gmail_scope()`). The standalone CLI never
  composes manifests. Fix: have the CLI trigger connector scope registration before login (and/or move
  gmail's registration to module top-level for parity). Runbook's "read-only" claim is also stale —
  calendar registers `calendar.events` (write). (BACKLOG, 2026-06-28.)

## What's Next (discoveries)
- **The activation runway was unit-built but never run end-to-end — 6 gaps in one pass.** Brain
  entry-point · Windows `data_root` · pairing UI · Google scope-registration · quarantine transport-
  error crash · local-model JSON reliability. None would survive a single live run; all passed
  mypy/pytest. **This is the strongest evidence yet for the BACKLOG item "agentic self-verification of
  the activation tails"** (the Task Executor running the runbook as a self-test) — added 2026-06-28.
- **What now works on-box (real, verified):** brain `/healthz` 200 + heartbeat; owner-private
  encryption-at-rest end-to-end (Hello-PIN unlock → DPAPI-sealed `keys/owner-private.dek` → SQLCipher
  `tokens.db` + `memory.db` + owner-private LanceDB vault); live Google OAuth + encrypted token
  storage; the email-rules harness reaching live Gmail in observe mode. The `m2-win-a` encryption-at-
  rest milestone (this branch) is demonstrably live.
- **Runbook updated** with all the first-run frictions (Step 0 `--group agentic` + `data_root`; Step 1
  Hello-at-startup fallback; Step 2 real-terminal + PIN-counts-as-Hello; Step 3 blocked banner).
- **Commit-pending:** the brain entry-point fix (`pyproject.toml`, `src/artemis/run_brain.py` new,
  `scripts/run_brain.py` deleted) + the doc updates (runbook, BACKLOG, this handoff). Branch
  `m2-win-a-encryption-at-rest`. _(Committed `e36dd58`.)_

---

## Planning sweep (later same day — "start all the planning")

After the activation run, the owner asked to plan the whole standing queue. Outcome: **8 threads
resolved, 10 build-ready specs produced** (BUILD-ORDER Group D). Done via parallel AFK drafting agents
(design briefs → ADR → specs), foreground for decisions only.

**Decisions made (durable homes updated):**
- **Unified voice+text Ask → ADR-034.** The Ask overlay becomes spoken+typed in, streamed-text +
  spoken-aloud out, from ONE brain generation teed (display SSE branch unchanged + a brain-side
  speakable branch → existing M5 TTS/sidecar). **Owner's speakable rule:** shape-aware — short answer
  spoken near-verbatim; list/long answer → a brief "it's on screen" pointer, never read aloud.
  Reuses the whole M5-a/b/c/d stack. Spec-time defaults set (shape-classifier structural; pointer
  templated from query subject; speaks-by-default + mute toggle). Brief: `docs/findings/unified-voice-ask-design-brief.md`.
- **Agentic forks (ADR-031):** only 2 of 6 were genuinely open (the other 4 were already built — ratified).
  `cloud_reasoning_enabled` **default = ON** (owner; against the brief's rec — recorded in ADR-031 parked
  items with the kill-switch + sensitivity-wall guardrails; governs only the future GEPA path).
  Sensitive-screen denylist **deferral ratified** (ADR-031 Fork-2 approach stands; list pinned at Rung-3).
- **Denial-vs-abort (m2-win-b):** dev-relaxed path now continues **locked** on a denied gesture too
  (prod still aborts) — a small code fix, spec'd in the cleanup bundle adjacent items.
- **Out-of-spec reconciliations ratified** as correct-as-built (m2-win-b fixture, gateway.rs `pub(crate)`,
  auth.rs/tests).

**10 ready specs (BUILD-ORDER Group D):** `fix-quarantine-transport-error`, `fix-local-model-structured-output`
(serial — same file), `fix-google-auth-scope-registration`, `cleanup-bundle-2026-06-28`,
`client-pairing-screen`, `voice-ask-S1..S4`, `client-pairing-live-handshake` (owner-run).

**Specialist flags surfaced during drafting:**
- **qwen3:4b root cause = thinking-model `<think>` blocks**, not the ignored schema (drives the local-model fix).
- **Voice shape-vs-streaming** resolved by an early short-circuit (emit the pointer the instant a
  list/table/code/3rd-sentence trigger appears; short answers buffer fully) — sanity-check at build.
- **`app_ask_voice` mic→brain glue** falls between voice specs → assigned to S2 (recorded in BUILD-ORDER D2).
- **Pairing-code source is BUILT** (`POST /app/admin/pair-code`), not a gap — pairing fix is a new
  `PairingScreen` + a one-line `App.tsx:131` change; the live DER/X9.63 handshake is still unproven (D3).

**Reconciliation:** M5-a-win-sidecar + agentic Wave 0 were already built (BUILD-ORDER Groups A/B) — the
earlier "ready to build" framing in Open Questions was stale; Group D is the true pending queue.

**Next:** switch to coding mode and build Group D, entry = Wave D1 (all Windows-testable). Two micro-records
were completed this session (ADR-031 §parked, BUILD-ORDER Group D); nothing left to record.
