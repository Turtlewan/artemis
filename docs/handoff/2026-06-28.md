# Handoff — 2026-06-28 (owner-manual activation tails, on-box)

Session type: **ops walk-through** of `docs/technical/setup/owner-manual-activation-runbook.md` on the
enrolled Windows box, with two sanctioned planning-mode code fixes to unblock it. The activation runway
was unit-built (2026-06-27) but had **never been run end-to-end** — this session ran it for the first
time and surfaced **6 first-run gaps**. Three tails now genuinely work; two are real build gaps.

## Decisions Made
- **Fixed `artemis-brain` entry point inline (sanctioned one-off, owner-approved).** Was
  `scripts.run_brain:main` pointing at an unpackaged top-level `scripts/` file (wheel ships only
  `src/artemis`). Moved the launcher to `src/artemis/run_brain.py`, repointed the entry point to
  `artemis.run_brain:main`, deleted the orphan. The archived `win-brain-runtime` spec actually
  *specified* an in-package path — the build deviated. **Code change committed-pending** — confirm at
  next planning review (it's a one-line entry-point + file move, zero logic change).
- **Set `ARTEMIS_DATA_ROOT` to `%LOCALAPPDATA%\Artemis`** in `config/.env.dev` (local, gitignored).
  The DPAPI `WindowsKeyProvider` refuses a key store outside `%APPDATA%`/`%LOCALAPPDATA%`; the corpus
  default `/opt/artemis` fails that guard on Windows. (Stale Mac `ARTEMIS_WORKTREE_ROOT` left as-is —
  didn't block; noted in runbook.)
- **Enrolled a Windows Hello PIN** to satisfy the unlock gate (the box has no biometric). PIN counts
  as a Hello verifier — the accepted ADR-025 dev-wall downgrade. Live unlock now works.
- **Google login used the `--scope` workaround** (`gmail.readonly`) because the CLI registers no
  scopes on its own (see Blocked).
- **Did NOT fix the client-pairing or harness-robustness gaps** — those are planning→build work, not
  ops one-offs. Filed to BACKLOG instead.

## Specialist Flags
- **(security/robustness) `QuarantinedReader.read()` crashes the batch on a model transport error.**
  The `model.complete()` call is *outside* the try/except that handles parse failures, so an Ollama
  `400` (or any HTTP/timeout error) on a single untrusted email kills the whole poll. The untrusted-
  input reader should degrade-and-continue on transport errors exactly like it does on JSON-parse
  errors. `untrusted/quarantine.py:132-140` vs the `except` at `:143`. (BACKLOG, 2026-06-28.)
- **(ai-systems) Ollama ignores the OpenAI `response_format: json_schema`** — `qwen3:4b` returned
  prose for every quarantine read (7/7 → `JSONDecodeError` degrade). Needs Ollama's native `format`
  param (or prompt-coax / different reader). The `400` was likely `num_ctx` overflow on a long email.
  Exactly the tuning signal the observe harness exists to surface. (BACKLOG, 2026-06-28.)

## Blocked Actions
- **Client overlay demo (runbook Steps 3–5) — BLOCKED on an unwired pairing UI.** The Tauri client
  builds + launches clean, but boots to `connection.state="unpaired"`; `App.tsx:131` renders `null`
  until connected, and **nothing in the running app renders a pairing surface or calls `pairDevice()`**
  (`auth/pairing.ts`, referenced only by its own test). Permanent blank screen, no way to pair. Needs:
  build a pairing screen + then exercise the never-run live `auth_pair`/`auth_connect`/`auth_unlock`
  + DER-signature handshake (the gated `CLIENT-auth` Task 7 / Step 4b; possible X9.63-vs-SPKI fork).
  Planning→build. (BACKLOG, 2026-06-28.)
- **`artemis-google-auth login` registers no scopes → bails** (`"no scopes…"`). `cli.py login`
  computes `required_scopes()` but imports no connector modules. Root cause: inconsistent
  registration — `modules/calendar` registers at module import (top-level), but `modules/gmail`
  registers only inside `build_gmail_manifest()` (`register_gmail_scope()`). The standalone CLI never
  composes manifests. Fix: have the CLI trigger connector scope registration before login (and/or move
  gmail's registration to module top-level for parity). Runbook's "read-only" claim is also stale —
  calendar registers `calendar.events` (write). (BACKLOG, 2026-06-28.)

## What's Next (discoveries)
- **The activation runway was unit-built but never run end-to-end — 6 gaps in one pass.** Brain
  entry-point · Windows `data_root` · pairing UI · Google scope-registration · quarantine transport-
  error crash · local-model JSON reliability. None would survive a single live run; all passed
  mypy/pytest. **This is the strongest evidence yet for the BACKLOG item "agentic self-verification of
  the activation tails"** (the Task Executor running the runbook as a self-test) — added 2026-06-28.
- **What now works on-box (real, verified):** brain `/healthz` 200 + heartbeat; owner-private
  encryption-at-rest end-to-end (Hello-PIN unlock → DPAPI-sealed `keys/owner-private.dek` → SQLCipher
  `tokens.db` + `memory.db` + owner-private LanceDB vault); live Google OAuth + encrypted token
  storage; the email-rules harness reaching live Gmail in observe mode. The `m2-win-a` encryption-at-
  rest milestone (this branch) is demonstrably live.
- **Runbook updated** with all the first-run frictions (Step 0 `--group agentic` + `data_root`; Step 1
  Hello-at-startup fallback; Step 2 real-terminal + PIN-counts-as-Hello; Step 3 blocked banner).
- **Commit-pending:** the brain entry-point fix (`pyproject.toml`, `src/artemis/run_brain.py` new,
  `scripts/run_brain.py` deleted) + the doc updates (runbook, BACKLOG, this handoff). Branch
  `m2-win-a-encryption-at-rest`. _(Committed `e36dd58`.)_

---

## Planning sweep (later same day — "start all the planning")

After the activation run, the owner asked to plan the whole standing queue. Outcome: **8 threads
resolved, 10 build-ready specs produced** (BUILD-ORDER Group D). Done via parallel AFK drafting agents
(design briefs → ADR → specs), foreground for decisions only.

**Decisions made (durable homes updated):**
- **Unified voice+text Ask → ADR-034.** The Ask overlay becomes spoken+typed in, streamed-text +
  spoken-aloud out, from ONE brain generation teed (display SSE branch unchanged + a brain-side
  speakable branch → existing M5 TTS/sidecar). **Owner's speakable rule:** shape-aware — short answer
  spoken near-verbatim; list/long answer → a brief "it's on screen" pointer, never read aloud.
  Reuses the whole M5-a/b/c/d stack. Spec-time defaults set (shape-classifier structural; pointer
  templated from query subject; speaks-by-default + mute toggle). Brief: `docs/findings/unified-voice-ask-design-brief.md`.
- **Agentic forks (ADR-031):** only 2 of 6 were genuinely open (the other 4 were already built — ratified).
  `cloud_reasoning_enabled` **default = ON** (owner; against the brief's rec — recorded in ADR-031 parked
  items with the kill-switch + sensitivity-wall guardrails; governs only the future GEPA path).
  Sensitive-screen denylist **deferral ratified** (ADR-031 Fork-2 approach stands; list pinned at Rung-3).
- **Denial-vs-abort (m2-win-b):** dev-relaxed path now continues **locked** on a denied gesture too
  (prod still aborts) — a small code fix, spec'd in the cleanup bundle adjacent items.
- **Out-of-spec reconciliations ratified** as correct-as-built (m2-win-b fixture, gateway.rs `pub(crate)`,
  auth.rs/tests).

**10 ready specs (BUILD-ORDER Group D):** `fix-quarantine-transport-error`, `fix-local-model-structured-output`
(serial — same file), `fix-google-auth-scope-registration`, `cleanup-bundle-2026-06-28`,
`client-pairing-screen`, `voice-ask-S1..S4`, `client-pairing-live-handshake` (owner-run).

**Specialist flags surfaced during drafting:**
- **qwen3:4b root cause = thinking-model `<think>` blocks**, not the ignored schema (drives the local-model fix).
- **Voice shape-vs-streaming** resolved by an early short-circuit (emit the pointer the instant a
  list/table/code/3rd-sentence trigger appears; short answers buffer fully) — sanity-check at build.
- **`app_ask_voice` mic→brain glue** falls between voice specs → assigned to S2 (recorded in BUILD-ORDER D2).
- **Pairing-code source is BUILT** (`POST /app/admin/pair-code`), not a gap — pairing fix is a new
  `PairingScreen` + a one-line `App.tsx:131` change; the live DER/X9.63 handshake is still unproven (D3).

**Reconciliation:** M5-a-win-sidecar + agentic Wave 0 were already built (BUILD-ORDER Groups A/B) — the
earlier "ready to build" framing in Open Questions was stale; Group D is the true pending queue.

**Next:** switch to coding mode and build Group D, entry = Wave D1 (all Windows-testable). Two micro-records
were completed this session (ADR-031 §parked, BUILD-ORDER Group D); nothing left to record.

---

## CODING SESSION 2026-06-28 — Group D built (D1 + D2, all 9 dev-buildable specs)

All nine Group D dev-buildable specs built (Codex `gpt-5.5` primary, Opus host orchestration +
reviews), host-verified (full `uv run mypy` + full `uv run pytest -q` per spec; client `tsc`/`vitest`/
`eslint`/`cargo check`), committed, archived to `done/`. Baseline green throughout (mypy 340 files;
pytest **926 passed, 6 skipped**; client vitest 79). **The Windows dev-buildable queue is now fully
drained** — every remaining `docs/changes/*.md` is Mac/SE/launchd/Swift/vision-gated, except
`client-pairing-live-handshake` (D3, owner-run).

### Decisions Made
- **L2-spec vs standing auto-commit:** the voice specs carry `autonomy_level: L2` (template default); the
  owner's standing auto-commit grant (green + scope-clean + fork-free) governed — committed all nine like
  the L5 session. Review-needed only if the L2 was deliberate. ⚠️
- **S1 post-review in-scope fixes (deviations):** widened `GatewayProtocol.handle_ask_unified` in
  `api_app.py` to `Scope | Identity` (matched the spec contract + concrete gateway; the protocol copy had
  narrowed it); added a comment at the speak-sink site flagging the S3 vault-recheck requirement.
- **spec 3 ruff I001 autofix** (blank-line/import-order) the Codex recipe missed — applied (behavior-identical).
- **spec 4 `test_health.py` deviation (SMALL, in-scope):** the spec's naive context-managed `TestClient`
  fixture would trigger the real win32 lifespan (Hello prompt + real brain compose) on entry → Codex faked
  the lifespan's OS/network parts while preserving the hardening goal. ⚠️ review-needed.
- **spec 4 `ReviewDetail.tsx` deviation:** Codex added a legacy `boolean` overload + runtime guard rather
  than edit the out-of-scope `ReviewDetail.test.tsx` — scope-preserving but speculative back-compat;
  planning may simplify to list-only + update the test. ⚠️

### Specialist Flags (from apex-reviewer; all FLAG/NIT, none blocking)
- **S1 speak-branch fail-closed gap (MED):** the speak branch does NOT apply the per-chunk vault recheck
  the display branch has. Harmless in S1 (default sink drains) — **S3's real TTS sink MUST re-check
  `is_owner_unlocked()` before speaking, or voice output is a fail-closed hole.** Commented in code.
- **S1 fire-and-forget speak task (LOW):** `asyncio.create_task(speak_sink(...))` keeps no reference;
  S3's real sink should retain it + log failures.
- **S2/S3 maintenance drift (MED):** `push_to_talk._capture_until_endpoint` is a near-copy of
  `VoiceLoop._capture_until_endpoint`; `speak_overlay_answer` reaches private back-half methods via a
  throwaway `VoiceLoop` with cast dummy `stt/gateway/settings` (safe today, fragile). Extract shared
  free-functions in a follow-up.
- **S3 barge-in re-arm (LOW, Mac):** `_watch_bargein` sends `startListening` after barge-in (correct for
  the headless loop) — in the overlay PTT model this races the PTT trigger on real hardware. Masked by
  Windows half-duplex; fix at the Mac audio bring-up.
- **S2 missing log-absence test (LOW):** S3 has a caplog assertion pinning never-log-transcript; S2 relies
  on having no logger. Add an equivalent regression test.

### Blocked Actions
- None blocked the build. The Mac/SE/launchd/Swift/vision-gated specs were correctly NOT attempted (no
  Secure Enclave / launchd / mlx-server / Swift / heavy-vision on this box).

### What's Next (discoveries)
- **Voice is built as SEAMS, not wired end-to-end.** The text Ask path is fully live. Voice needs a
  **composition/wiring spec** (planning): construct `frontend`+`tts` in `main.py`, install
  `app.state.speak_sink = compose_speak_sink(...)` (replacing S1's drain), add the `app_ask_voice` Rust
  command + a route calling `overlay_voice_turn`, then the on-box real-audio bring-up. None of this is in
  the S1–S4 file scopes (deliberate) — it's an architecture seam for planning.
- **`client-pairing-screen` unblocked D3:** the blank-screen (`App.tsx:131 → null`) is fixed; the client
  now renders `PairingScreen` unpaired. The live pairing handshake (second Hello = client device-key DER
  signature verified vs X9.63 pubkey by the brain; distinct from the already-done brain key-unlock Hello)
  is now runnable — owner-run.
- **winsdk `0xc0000374` flake** recurred in the Codex sandbox (timeout/crash in `test_windows_hello_unlock`)
  but NOT on any host run this session (all green). Pre-existing; `cleanup-bundle` hardened `test_health.py`
  for the same root cause, but `test_windows_hello_unlock.py` may want similar treatment.

### LIVE BRING-UP — first end-to-end app launch (post-build, owner-driven)
- **App launches, pairing screen renders, brain healthy on 8030** — the `client-pairing-screen` build is
  confirmed working live (no more blank window). Brain key-unlock Hello (WinRT `UserConsentVerifier`) works.
- **✅ RESOLVED (was a real bug, not a TPM problem): client keystore `map_status` mis-mapped
  `NTE_BAD_KEYSET`.** Root cause: CNG providers (the TPM Platform Crypto Provider AND the software KSP)
  return `NTE_BAD_KEYSET` (0x80090016), NOT `NTE_NOT_FOUND` (0x80090011), for a persisted key that hasn't
  been created yet. `map_status` only treated `NTE_NOT_FOUND` as KeyNotFound, so `NTE_BAD_KEYSET` fell into
  the `_ => HardwareUnavailable` catch-all → `has_key()` *errored* instead of returning `false` →
  `create_key()` was never reached → the device key was never created → every pairing failed before any
  Hello gesture, surfaced as the misleading "Can't reach your brain"/`offTunnel`. (The TPM is PRESENT +
  READY, AMD fTPM 6.32 — never the issue. Brain unlock Hello works via a different API, WinRT
  UserConsentVerifier.) Diagnosed by temporarily logging the raw HRESULT in `map_status`.
  **Fix (`tauri-plugin-keystore/src/windows.rs`, owner decision: software for Windows / hardware for Mac):**
  (1) map `NTE_BAD_KEYSET` → `KeyNotFound` (the real bug fix — would have blocked *any* provider on first
  pairing); (2) Windows dev twin uses the software Key Storage Provider for the device key (no gesture gate);
  the hardware-bound, gesture-gated key stays the Mac Secure Enclave path (`macos.rs`, ADR-025). **Verified
  headlessly** with a new `cargo test` (`software_ksp_roundtrip_create_sign_verify`) that does the full
  create → sign → **verify the DER signature against the exported X9.63 pubkey over the prehashed digest**
  (the brain's exact check) → destroy. Plugin recipe green (fmt + clippy `-D warnings` + tests).
  **⚠ Owner-run when back:** start the brain in a console (`uv run artemis-brain`, Hello unlock) → mint a
  code (`POST /app/admin/pair-code`) → Pair in the app → should now reach the full UI (no second Hello on
  Windows, by design of the software path). **Findings for planning:** (a) the `NTE_BAD_KEYSET` mapping bug
  means **hardware-on-Windows is likely viable too** now (the mapping was the real blocker, not the TPM) —
  could revisit prefer-hardware-on-Windows later, but software is the chosen dev path; (b) `offTunnel`
  conflating network + keystore failure is a misleading UX bucket — worth splitting so a keystore failure
  doesn't read as "can't reach your brain".
- **✅ FIXED (committed `1c17b38`): brain `/app/pair` 500 on Windows — `broker.pair()` on a `None` broker.**
  With the keystore fixed, the next live failure was HTTP 500 from `/app/pair`. Root cause: `main.py` leaves
  `app.state.broker_client = None` on Windows (the broker is the Mac Secure Enclave relay), but the handler
  called `broker.pair(...)` unconditionally → AttributeError → 500. Fix: guard `if broker is not None`
  (Windows custody = WindowsKeyProvider + DeviceRegistry; device registration unchanged) + regression test
  `test_pairing_succeeds_without_broker_on_windows_host`. This also *proved the full crypto chain works*:
  `_verify_pairing_signature` passed (it runs before the broker call and 401s on failure, not 500), i.e. the
  device's DER signature verified against the X9.63 pubkey on the **real brain**.
- **⛔ REMAINING (the actual "can't connect in" blockers — gated CLIENT-auth Task 7 finish-work):** live flow
  now reaches `pair 200 → session/begin 200 → session/complete 401`. Root cause = **client↔brain auth-proof
  contract divergence** (never reconciled; each side only tested against a mock of the other):
  - **(#3) session/unlock proof format + context mismatch.** Brain verifies `nonce + b"artemis-api-session"
    + counter` (raw); client signs `len(nonce)+nonce + len(ctx)+ctx + counter` with ctx `b"session"`
    (`client auth.rs proof_message`/`CONNECT_CONTEXT`/`UNLOCK_CONTEXT` vs brain
    `app_auth.py:285`/`API_SESSION_CONTEXT`). Different framing AND context string → 401. The canonical
    format is specified **nowhere** (not in `contracts.md`) — that's the root cause. Decide canonical
    (brain is the verifier → likely align the client), apply to BOTH proofs, document in `contracts.md`.
  - **(#4) Windows unlock path undefined.** `WindowsKeyProvider` has no `begin_unlock`/`complete_unlock`/
    `lock_all` (Mac broker-relay methods); on Windows the vault unlocks at brain startup via Hello, so the
    client unlock step needs defined behavior (likely short-circuit to already-unlocked). Architecture call
    (ADR-033/025).
  - **(#5) verify post-unlock domain reads on Windows** (a code comment notes readers need broker owner DEKs
    / FakePayload — may be one more gap).
  - **Prevention (high-leverage):** add a contract-conformance test (real client signer ↔ real brain
    verifier) so mock-only tests can't hide these again.
  - **Smallest path to "connect in" = #3 + #4 (+ likely #5).** Scoped spec: *"CLIENT-auth Windows bring-up:
    reconcile the proof contract + Windows unlock path."* All diagnosed to exact lines — build-ready for planning.
