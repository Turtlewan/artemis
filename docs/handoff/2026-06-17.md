# Handoff — 2026-06-17

## Decisions Made

- **tzdata dependency for Windows**: The `zoneinfo` module on Windows lacks the IANA tz database. Installed `tzdata` package as a dependency. This is Windows-only; macOS has it built-in. No fallout: `tzdata` is a lightweight data package, safe to keep across platforms.
- **M0-b main.py built as M1-c prerequisite**: The M0-b spec (launchd + deploy scripts) was partially built — only `main.py` (FastAPI health stubs) and its test were created. The Mac-specific plist templates, deploy script, and renderer are deferred to the Mini. This is consistent with the validation-slice scope.

## Specialist Flags — none

## Blocked Actions — none

## What's Next (discoveries)

- **compose_brain() always builds real adapters**: `gateway.compose_brain()` creates `OpenAIEmbeddingModel` + `OpenAIModelPort` which try to connect to the MLX server at runtime. The Router gracefully degrades to escalation when no server is running (the `ConnectError` is caught). The API tests work around this by overriding `app.state.gateway` with a `FakeBrain`-backed Gateway after lifespan. The CLI prints `ESCALATION_NOT_AVAILABLE` + a traceback. No change needed for the validation slice, but `compose_brain` could accept optional adapter overrides for a smoother offline dev experience.
- **Ruff lint issues in pre-existing code**: Three files (`brain.py`, `router.py`, `test_router_brain.py`) had import-sorting and unused-import issues from the earlier build. All fixed in this session. No structural impact.
- **`respond_stream` in Brain.respond_stream yields single chunk for tool path**: The test confirmed tool-path streaming works through the SSE API. The free-form responder path (token streaming from model) is untested in this slice — it requires a live model endpoint.

---

# Handoff — second coding session, same day

## Decisions Made
- **Slice 2a scope confirmed:** M4-a Tasks 2/4/6 only (schema + repository + golden tests). Tasks 1/3/5 (encryption-gated) left for the Mini, unpolluted by fallback workarounds.
- **sqlite-vec v0.1.9 metric syntax:** `distance_metric=cosine` must be a **column-level** option (`embedding FLOAT[4] distance_metric=cosine`), not a table-level option. Default metric is L2 — cosine was confirmed working with the column-level syntax. ⚠️ The spec's U5 fix (declaring cosine on the vec0 table) assumed table-level; the column-level approach is correct for this version.
- **`CurrentFactConflict` renamed → `CurrentFactConflictError`** (ruff N818 naming convention enforcement).
- **FactRow `subject_entity_id` field** included with `None` default so M4-d-2's column migration works cleanly.

## Specialist Flags — none

## Blocked Actions
- **M4-a Task 1** (sqlite-vec-under-SQLCipher spike) — hardware-GATED, needs the Mini and M2-c's `sqlcipher_open` seam.
- **M4-a Task 3** (`engine.py` keyed open helper) — depends on M2-c; blocked without it.
- **M4-a Task 5** (`SqliteMemoryStore` adapter skeleton) — depends on M2-b/M2-c wall symbols; blocked.

## What's Next (discoveries)
- **Second-precision timestamps are a real friction for bitemporal tests** — `as_of(tx_t=now)` can never see a row whose tx was closed in the same second. The workaround (assert `tx_to == new_row.tx_from` rather than `< now_iso()`) is correct but subtle. If sub-second ISO timestamps are needed later, ISO-8601 millisecond format extends naturally (`2026-06-17T12:00:00.123Z`) without changing the SQL string comparisons.
- **33 golden tests pass with 0 real model calls** — the deterministic-vector approach (`_embed()` based on text hash) works well for KNN round-trip testing without real embeddings.
- **Pre-Mini buildable surface is now 2 specs deep** (slice 1 = M0-a→M1-c orchestration; slice 2a = M4-a storage). Next candidates: OBS-a (pure Python logging), distill-datagen-pipeline (Windows-native), or partial M3 retrieval (needs LanceDB but no hardware gate).

---

# Coding session 3 — dev-model-auth + dev-offline-compose (2026-06-17, flash tier)

## Decisions Made

| Task | Decision | Reason | Review? |
|------|----------|--------|---------|
| dev-model-auth | `model_api_key: str \| None = Field(default=None, exclude=True)` | Secret: excluded from serialisation, read from env only. Local MLX servers send no header when unset. | No |
| dev-offline-compose | `embedder`/`model` overrides kwarg-only in `compose_brain` | Keyword prevents positional confusion; default `None` → builds real adapters (backward-compat). | No |

## Specialist Flags — none

## Blocked Actions
- **slice-3a-lancedb-vectorstore** (`coder_tier: pro`) — not buildable on flash-tier backend. Needs a Pro session. LanceDB dependency + full deterministic-vector golden test suite.

## What's Next (discoveries)
- The pre-existing `test_manifest_registry.py::TestToolRegistry::test_retrieve_tools_returns_fq_ids` test is flaky — depends on iterator ordering. Passed in the final run (112/112) but failed intermittently. May need a `sorted()` or non-order-sensitive assertion.
- Both flash specs build clean in ~2 min flat with zero post-edit surprises — spec quality is mature for low-friction execution.
- **Committed as `b3d868a`.** Remaining flash-tier work: none. Remaining pre-Mini work: slice-3a (pro). Remaining corpus: ~60 Mini-gated specs.
