# Handoff — 2026-06-26

Build session: **Wave R-comp (ADR-032) — all 7 specs built, cross-model-reviewed, committed.**
Coder = Codex (gpt-5.5), host = Opus. Wave 1 (R1·R3·R4m·R5d) ∥, Wave 2 (R2·R6c) ∥, then R-dev-email-harness.
Baseline green @ `00b7758`: full `uv run mypy` clean (269 files), ruff clean, `765 passed / 2 skipped`
(the one failure is a pre-existing date-flake — see What's Next).

## Decisions Made

- **R1 scope deviation (review needed ⚠️):** `tests/test_reactions_dispatcher.py` was NOT in R1's
  `Files to Change`, but R1's intended contract change (dispatcher now defaults to `mode="observe"`;
  effect-then-claim replaces at-most-once) broke its pre-existing tests. Reconciled in-place: added a
  `mode="live"` default param to the `_dispatcher` helper (preserves prior live-behaviour tests) and
  rewrote `test_degrade_..._claim_persists` → `test_degrade_refires_failed_reaction_without_aborting_siblings`
  to assert the new at-least-once semantics (failed reaction re-fires/never claimed; successful sibling
  claimed once). The spec should have listed this test file.

- **compose.py integration wiring (review needed ⚠️):** R1's `compose_reactions` shipped with
  `del get_linked_task_ref_fn, fetch_extract` and omitted them from the `register_*` calls, because
  R3 (`get_linked_task_ref_fn` on `register_self_reactions`) and R6c (`fetch_extract` on
  `register_comms_reactions`) didn't exist when R1's worker built compose concurrently. After R3/R6c
  landed, mypy stayed green (both params default to `None`-returning stubs) but the composition was
  functionally inert — comms reactions could never fetch, bill-lifecycle ref never resolved. Wired both
  per R1 Task 5 (`fetch_extract=cast(FetchExtractFn, fetch_extract)`, `get_linked_task_ref_fn=…`);
  committed with R6c (`0054d32`). This is R1 Task 5's specified wiring completed at integration time.

- **R5d `exc_info` (no code change):** R5d's AC literally asks `exc_info=True` on the non-usable/empty
  log paths too, but `exc_info=True` outside an `except` block is a no-op (Python drops it). The built
  code correctly applies `exc_info` only on the real exception path. Kept the code; the AC wording is
  imprecise — planning may tighten it to the failure path only.

- **Cross-model review folds applied (all in-scope, committed with their specs):** R4m — log
  entity-resolution failures (mirror `process_turn`). R1 — compose no longer auto-creates a throwaway
  observe `notice_sink` (**BLOCK**: it silently discarded the WOULD audit trail); WOULD notice emitted
  before the dedup-slot consume; `start()` sets the worker guard only after `create_task` succeeds.
  R2 — skip `EMAIL_INGESTED` emit when `extract_store=None` (no orphan whose `source_ref` resolves to
  nothing). R6c — `_missing_fetch_extract` and a lazy `_MissingMemory` now fail loud on misconfiguration.
  Harness — closed-loop `assert dispatcher._mode == "observe"`; structured-log no-raw-body check over all
  four canned emails; `https` loopback in the local-endpoint guard; `BuildKeyProviderError` on invalid
  DEK hex.

## Specialist Flags

- **Harness monkeypatches a private method (planning refinement):** `_install_dev_observe_filter`
  swaps `ReactionDispatcher._fire` via `setattr`. Works + is test-covered (the `len(notices)==3`
  assertion would catch a silent no-op), but it's fragile to a `_fire` rename. The Opus reviewer
  suggests exposing a public rule-predicate hook on `ReactionDispatcher` (R1's file) — out of the
  harness spec's scope, so deferred. Consider it when the dispatcher next gets touched.

## Blocked Actions

None — all 7 specs built and committed.

## What's Next (discoveries)

- **Pre-existing date-brittle test (not ours):** `tests/test_finance_hooks.py::test_hooks_hit_with_count_id_scalar_payloads`
  fails because `make_renewal_check` filters `today <= next_renewal <= until` with `today = now().date()`
  = 2026-06-26, while the test hardcodes `next_renewal="2026-06-25"` (now 1 day in the past). It fails
  with the R3 finance changes stashed too — this is FIN-c test brittleness from the clock rolling past
  the last-green date, NOT caused by Wave R-comp. Needs a date-stable fix (freeze the clock or use
  now-relative dates) in a FIN-c follow-up.

- **Reactions ship DORMANT:** `RuntimeConfig.reaction.reactions_mode` defaults to `"observe"` — the
  whole reactions runtime is wired but fires NO effects until the owner flips it to `"live"`. The
  observe path emits a `WOULD …: <rule>` audit stream (now guaranteed a real `notice_sink` after the
  R1 BLOCK fold).

- **R2 / harness low-pri test hardening (deferred):** explicit store-then-emit *ordering* assertion in
  `test_reactions_emit_seams.py`; consistent message-ids in the four-producer reachability test;
  explicit non-invocation counters on the harness no-op effect stubs (they currently raise-on-call,
  which already hard-fails the test). All advisory LOWs, no correctness impact.

- **Harness README runbook NOT written:** R-dev-email-harness's Documentation section lists a
  "Building email rules on the dev box" runbook (OAuth → `artemis-google-auth` → `artemis-dev-email-rules
  --once` → read WOULD + structured-extract logs → tune R5d/R6c). Deferred — it's tied to the exercise
  tail below, which isn't runnable yet. Planning/docs follow-up.

- **EXERCISE TAIL (owner/ops — unchanged):** before the harness can actually poll live mail:
  `ollama pull` a Qwen3 reader (must be TOOLLESS) + create a Google OAuth client (Desktop app, Gmail
  API, `gmail.readonly`, test user) + `artemis-google-auth` login on a throwaway account. Re-surface the
  full Google OAuth steps at exercise time (memory `dev-email-rules-oauth-at-exercise`).

---

# Build session 2 — Agentic engine (ADR-031, Group A) — COMPLETE

Coder = Codex (gpt-5.5), host = Opus. Built the full dev-buildable agentic engine: the Group C
finance fix (baseline-greener) + all 9 `AGENT-*` specs (A0→A5), serial, each host-verified with the
FULL `uv run mypy` + `uv run pytest -q` and (for every `cross_model_review:true` spec) an independent
Opus security review. Final state green @ `b09255c`: mypy clean (294 files), ruff clean,
**828 passed / 4 skipped** (the 4 skips are Windows symlink-privilege + Mac-gated guards). 11 commits.
All specs archived to `done/`; `changes/AGENT-*` empty.

## Decisions Made

- **Built `fix-finance-hooks-date-stability` FIRST (host, inline).** The date-flake the prior handoff
  flagged was a RED baseline — Codex's AGENTS.md refuses to build on a red baseline, so it blocked
  *every* agentic dispatch. Codex is structurally unable to fix it (its job is to green the very test
  the baseline-guard trips on), so the Opus host fixed the test inline (relative date offsets) and
  committed `f0be86c`. This unblocked the whole build. Group C item done as a side effect.
- **Dependency mechanism adapted (deviation, logged per-spec).** Specs said
  `[project.optional-dependencies] agentic` + `uv sync --extra agentic`, but the project uses PEP 735
  `[dependency-groups]` + `uv sync --group` (the docling precedent the specs cite). Added all agentic
  deps (`pydantic-ai`, `litellm`, `openhands-sdk`+`-tools`) to a `[dependency-groups] agentic` group.
  Base `uv sync` stays lean (verified — base strips the whole tree). All deps added **host-side** (not
  the Codex sandbox) per the Windows SID-ownership rule.
- **httpx2 ripple reconciled (out-of-spec file, review-needed ⚠️).** `pydantic-ai` (AGENT-spine Task 1)
  pulled `httpx2` into the tree, so `starlette.testclient.TestClient` returns `httpx2.Response` instead
  of `httpx.Response` → 14 mypy errors in the EXISTING `tests/test_api_app.py` (not in any spec's scope).
  A direct ripple from spine's own dep, so reconciled env-agnostically: replaced the concrete
  `httpx.Response` annotation with a structural `_JsonResponse` Protocol (+ `cast`), valid whether the
  typed `httpx` or untyped `httpx2` backs the client. Committed with spine + a follow-up `e53f924`.
- **Every `cross_model_review:true` spec got an independent Opus review** (Codex built → Opus reviews,
  ADR-026 family-aware). All CLEAN. AGENT-authority's `..`-escape assertion was hardened (it was hidden
  behind the symlink skip → untested on the Windows dev box).
- **AGENT-rung2 network-deny INDEPENDENTLY HOST-VALIDATED.** Beyond Codex's own run, the Opus host ran
  the live test as the normal user with real network: an in-AppContainer `socket.create_connection`
  to 1.1.1.1:80 fails while the host connects — the zero-capability AppContainer kernel network block
  is real, no admin.

## Specialist Flags

- **AGENT-spine — approve→graduate wiring incomplete (composition, ⚠️).** The executor calls
  `authority.graduate(pending.id)` after an inbox "yes", but `graduate` only enrols when the staged
  `PendingAction` is `ActionStatus.APPROVED` — a state set ONLY by `ActionStagingService.approve()`
  (the GATE surface). The executor doesn't drive `approve`, so against the real AuthorityGate a
  boundary step parks fail-closed ("authority graduation failed") rather than proceeding. SAFE (never
  ungated) but the approve→proceed happy path needs completing when GATE-b (Tauri-gated) lands —
  prefer moving graduation to a staging-approval listener so it never flows through the executor.
- **AGENT-inbox — deliver-count silent failure (composition, ⚠️).** `AskOwnerTool.ask` ignores the
  deliver return count; the real `NtfyDelivery.__call__` swallows publish failures and returns 0
  (doesn't raise), so a silent ntfy outage with `timeout_s=0` blocks on the waiter (recourse:
  `pending()`+`resolve()`). Check `count==0 → delivery-failure` when wiring the real delivery.
- **AGENT-spine — token budget not enforced.** `BudgetTracker.check` is always called `tokens_used=0`;
  only `step_budget`/no-progress trip. Wire pydantic-ai usage accounting.
- **AGENT-coder — supply-chain follow-up.** The optional agentic group carries inherited transitive
  CVEs (pyjwt/cryptography/idna/msgpack/pydantic-settings) from base + the pydantic-ai/litellm waves —
  NOT from openhands-sdk (its own tree pip-audit'd clean). `posthog` telemetry is pulled (dormant; no
  live run). Do a supply-chain bump pass before any live cloud-coder run. `build_workspace` maps both
  `docker` and `remote` to `RemoteWorkspace` (Mac reconcile).
- **AGENT-rung2 — icacls error handling (minor robustness).** `_grant_workspace_acl` uses
  `check=True`, so an icacls failure raises `CalledProcessError` (not `OSError`) which `run()`'s
  `except OSError` misses → unhandled rather than a fail-closed `CommandResult` (still fail-closed in
  effect). Map `SubprocessError` to an error result.

## Blocked Actions

None — all 10 specs (1 finance fix + 9 AGENT-*) built, host-verified, reviewed, committed, archived.

## What's Next (discoveries)

- **The engine is HEADLESS + behind the optional `agentic` group** (base sync stays lean). It composes
  cleanly: types → checkpoint/inbox/authority → spine (plan→act→verify + reliability) → rung0/1 tools
  → coder-router → embedded OpenHands coder → rung2 AppContainer sandbox.
- **Two composition seams are the integration glue to the owner-approval surface** (spine
  approve→graduate, inbox deliver-count) — both wait on GATE-b (currently Tauri-gated). They are the
  natural first task when the client/GATE surface is next built.
- **OpenHands embed is fake-tested only** (no live coding run on the dev box, per spec) — the live
  embed + env-var key wiring is an on-hardware/Mac exercise. **rung2's AppContainer network-deny, by
  contrast, WORKS and is host-validated on the dev box now.**
- **Remaining BUILD-ORDER queue (independent of A):** Group B voice (`M5-a-win-sidecar`→b→c→d) and
  Group C `distill-datagen-pipeline`. Gated Mac/MSVC/Tauri specs unchanged.

---

# Build session 3 — Group B (voice, partial) + Group C (distill, complete)

Coder = Codex, host = Opus. After Group A, continued the BUILD-ORDER queue.

## Decisions Made
- **Voice dep-fix (owner chose "fix deps first").** The `[voice-dev]` group didn't resolve. Three real
  spec package errors fixed (`livekit` not `livekit-rtc`; `moonshine-voice>=0.0.62` not the model's
  "v2"; `PyAudioWPatch` marked `sys_platform=='win32'`), plus `[tool.uv] environments` scoped to
  Artemis's real targets (Windows+macOS/3.12) — this drops the linux split where openwakeword pulls
  the cp312-less, linux-only `tflite-runtime`. `requires-python` unchanged; existing baseline
  re-verified green (828 passed) after the change. The group now RESOLVES.
- **distill smoke artifacts not committed** — the dry-run produced `datasets/distill/smoke/*`
  (synthetic FakeTeacher placeholders); removed as throwaway test output. The standalone
  `tools/distill/uv.lock` IS committed (project reproducibility).

## Specialist Flags
- None new beyond the blocked fork below.

## Blocked Actions
- **⛔ M5-a-win-sidecar AF_UNIX transport (owner chose "leave for planning").** `socket.AF_UNIX` is
  absent on this Windows Python 3.12.10, so the frozen M5-a AF_UNIX socket IPC can't run on the dev
  box (codec/state-machine/barge-in all tested; the e2e socket test skips gracefully). DECISION for
  planning: switch the Windows-dev twin's transport to TCP-loopback (127.0.0.1; wire FRAMING
  unchanged) and build M5-d to match — the Mac/Swift sidecar keeps AF_UNIX. Spec stays in `changes/`
  (NOT archived); full writeup in `docs/progress/M5-a-win-sidecar.md`. M5-b/c/d are unbuilt and wait
  on this decision.

## What's Next (discoveries)
- **Group C done; Group B core done (transport-forked).** The dev-buildable active queue is now
  drained except the M5-a transport decision (→ then M5-b/c/d voice continuation, increasingly
  GATED-real-model-dependent) and the gated Mac/MSVC/Tauri specs.
- **The full `uv sync --group voice-dev` heavy install** (~5GB: torch/onnxruntime/spacy/kokoro +
  espeak-ng + cu128 GPU torch) remains the GATED on-hardware exercise; the lock resolves, but the core
  builds/tests against fakes without it.

---

# Build session 4 — Voice chain (Group B) COMPLETE + dev-buildable queue drained

Coder = Codex, host = Opus. Planned the M5-a transport fork (owner: bare TCP-loopback) then built the
full voice chain. Baseline green @ `3819a92`: full mypy clean (324 files), 865 passed / 5 skipped.

## Decisions Made
- **M5-a transport fork resolved (owner: bare TCP-loopback).** Built `m5-a-win-transport`: IPCServer
  selects AF_UNIX where available else TCP on 127.0.0.1 (ephemeral, loopback-only) + `audio.port`
  rendezvous + `endpoint()`. Framing byte-for-byte unchanged; Mac keeps AF_UNIX. The e2e socket test
  now RUNS on this Windows host (the AF_UNIX skip is gone). M5-a-win-sidecar + transport archived.
- **[voice-dev] deps fixed earlier (build-session 3) now power the chain** — all M5-b/c/d build their
  port adapters with lazy MLX/SpeechBrain loaders (`# type: ignore[import-not-found]`) so they
  mypy/test WITHOUT the heavy stack installed; real models GATED on-Mac.
- **M5-c voice identity wall (security).** voice-ID routes scope but never unlocks the DEK; voiceprints
  are embeddings-only AES-GCM under the Tier-0/GENERAL key (readable while locked for routing).
- **M5-d voice loop fail-closed gate.** Codex (per instruction) made `handle_voice_stream` fail-CLOSED
  (owner Tier-1 + locked/no-key → raise NeedsPhoneUnlock); the loop plays a spoken prompt, never the
  sensitive answer; no direct `brain.respond` bypass.

## Specialist Flags
- **Two Gateway voice gates now exist (harmonize — review needed ⚠️).** `handle_voice` (M5-c,
  non-streaming) is FAIL-OPEN on an absent key_provider (the `is not None` escape); `handle_voice_stream`
  (M5-d, streaming) is FAIL-CLOSED. The live voice loop uses ONLY the fail-closed streaming path, so the
  voice path is secure — but make `handle_voice` fail-closed too, or retire it if the stream supersedes
  it. (docs/progress/M5-c-speaker-id.md + M5-d-voice-loop.md.)
- **Codex's nested-sandbox AppContainer flake.** During these builds Codex's full-suite runs sometimes
  failed `test_agent_rung2.py` with exit 3221225781 (STATUS_DLL_NOT_FOUND) — the AppContainer-inside-
  Codex's-restricted-token can't load Python DLLs. It PASSES on the host (normal user). Host-verify is
  the source of truth for the AppContainer tests; ignore the Codex-sandbox failure.

## Blocked Actions
None — the entire dev-buildable queue (Groups A agentic, B voice, C distill) is built, host-verified,
reviewed, committed, archived.

## What's Next (discoveries)
- **Dev-buildable queue is DRAINED.** Only gated specs remain in `changes/`: Mac (`M0-b/c/e/f`,
  `M2-a/c/d`, `M3-d`, `M5-a-audio-sidecar`), MSVC (`CLIENT-auth`), Tauri (`GATE-b`). They wait for the
  Mac Mini / MSVC toolchain.
- **GATED voice tails for the Mac:** real Parakeet/Whisper/Kokoro/ECAPA models, the real mic→speaker
  end-to-end + the ~750–800ms latency budget, and production AEC quality. All structured behind the
  port adapters' lazy loaders + GATED Task 5/6 in each M5 spec.
- **Planning follow-ups carried:** the 2 agentic composition seams (await GATE-b), the agentic
  supply-chain bump, the rung2 icacls error-mapping, and the harmonize-the-two-voice-gates item.

---

# Session 5 — planning drain + `voice-gate-rung2-hardening` build

Mixed planning+build session (owner ran "start planning" → drained the build follow-ups → "start build").

## Decisions Made
- **Drained the 2026-06-26 handoff to inbox-zero.** Queued `voice-gate-rung2-hardening` (Light) for the
  voice-gate + icacls fixes; **dropped the email-to-self clip** (owner — redundant with M8-d-c2 capture);
  carried the GATE-b-blocked / Mac-gated items to status.md Open Questions; pruned 6 drained
  `docs/progress/*.md` records; refreshed `overview.md` status banner + ADR index (029–032); wrote the
  **activation runway** into status.md (replaced the stale "Next step" section).
- **`handle_voice` harmonized fail-closed (not retired).** Mirrors `handle_voice_stream`'s nested-`if`
  exactly; test-only today but a symmetric public API, so retiring would churn 4 tests for a speculative
  removal. rung2 `sandbox.py run()` gained a `subprocess.CalledProcessError` arm (icacls `check=True`
  → fail-closed `CommandResult`). 2 regression tests added. Built **inline on the Opus host**, not Codex —
  see Specialist Flags. Full host verify green: ruff clean, mypy 324 files, 867 passed / 5 skipped.

## Specialist Flags
- **Codex blocked on its mandatory baseline-verify (build-process learning).** `codex exec` refused to
  start because its AGENTS.md baseline check found the baseline red: plain `uv sync` omits the optional
  `[dependency-groups] agentic` group, so `mypy` can't resolve `openhands.sdk` in the coder files; and
  `pytest -q` exceeded Codex's ~124s command timeout (the suite takes ~4 min — lazy torch/onnxruntime
  imports). Fix applied: host `uv sync --group agentic` greens mypy; for this tiny 3-edit spec the host
  built it inline (the rung2 AppContainer test must run host-side anyway). **Recurring** — see the new
  feedback memory `codex-baseline-verify-needs-agentic-sync`. For real Codex dispatches: host must
  `uv sync --group agentic` first, and the slow full-suite baseline is the friction to plan around.

## Blocked Actions
None.

## What's Next (discoveries)
- The activation runway (status.md) is the forward plan: ① MSVC → ② dev-model-stack (first live signal)
  → ③ reactions/email → ④ voice → ⑤ agentic design → ⑥ Mac Mini.

---

# Session 6 — Windows-host v1 pivot (ADR-033) + activation runway run + M2-win specced

Planning+exec session. Ran the activation runway, then **owner re-scoped the whole roadmap**.

## Decisions Made
- **ADR-033 — Windows-host v1 (owner).** Make this PC the v1 RUNNING host; Mac = later migration, OFF the
  critical path. Security wall = **lighter interim** (SQLCipher DEK sealed to DPAPI/TPM + Windows Hello;
  no phone-attested SE broker until Mac). Substitutes captured (Ollama/CUDA, voice twin, TPM/Hello). The
  runway in status.md was re-anchored around this; the old "⑥ Mac Mini is THE blocker" framing is retired.
- **M2-win Phase 1 split + research.** Research de-risked the libs: `sqlcipher3-binary` = prebuilt Windows
  wheels (the SQLCipher-on-Windows pain point is gone); Windows Hello needs an **HWND** so the headless brain
  can't prompt directly → gesture goes on a console-CLI now / the Tauri window in Phase 2. Split into
  `m2-win-a` (DPAPI custody + real SQLCipher, buildable now) → `m2-win-b` (Hello gate + main.py wiring).

## Specialist Flags
- **Planning apex-security review on both M2-win specs caught 4 BLOCKs** a fresh build would have shipped:
  DPAPI `CRYPTPROTECT_LOCAL_MACHINE` mis-naming (silent machine-scope downgrade), `PRAGMA key` leaking the
  DEK into exception text, a `verify_hello=False` auth-bypass param, and a Hello-unavailable→auto-unseal
  downgrade path. All folded (+9 FLAGs). Strong ROI on the pre-build review for auth/crypto specs.
- **Client "built ≠ runs" (C1–C4 in status Open Questions).** The Tauri client compiled+ran but blank-screened
  on a real crash (unguarded Tauri `listen()` + no error boundary — FIXED `997b68b`) the **mocked-Tauri unit
  tests never caught**; and its UI is connection-gated (`if (!connected) return null`) so it shows only the
  ambient background until a brain is wired. dev/prod CSP split still needed (reverted to strict).

## Blocked Actions
None — everything committed; specs build-ready.

## What's Next (discoveries)
- **▶ NEXT = BUILD `m2-win-a`** (coding mode) — Windows encryption-at-rest; buildable + unit-testable on this
  box now (no Hello/interaction). Then `m2-win-b` (Hello gate). Then ⑤ full compose (brain service + client
  auth wiring — the 14 dead-code IPC commands) → ⑥ live spoke OAuth. status.md headline + § Activation runway
  hold the full state.
