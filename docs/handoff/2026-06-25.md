# Handoff — 2026-06-25 (cluster wave, continuation 6)

Coding session (Codex-primary via apex-code; Opus host). Cluster wave spokes built this session:
**M3-c → DR-c → CAL-d → FIN-a → FIN-b**, each host-verified + committed + archived to `done/`.
Baseline green throughout; tree now **547 passed / 2 skipped**, full `uv run mypy` clean (214 files).
Commits: `d8b7d9e` (M3-c) · `830ca13` (DR-c) + `98c3414` (DR-c mypy fix) · `aaa0a91` (CAL-d) ·
`55325e9` (FIN-a) · `3a42b24` (FIN-b). What changed → `git log`; status/known-gates → `status.md`.

## Decisions Made

- **M3-c Task 4 (gateway `compose_brain` agentic wiring) — FLAGGED, not built.** `compose_brain`
  constructs no `AdaptiveRetriever`, so the spec's own Task 4 conditional ("STOP and FLAG rather than
  editing a third file") fired. `Brain.agentic` defaults `None` → preserves M1 behaviour; the agentic
  loop ships fully via the injected path + tests. **Review needed ⚠️:** planning must decide where the
  retriever is composed into the gateway and wire `agentic=` there.
- **CAL-d Task 4 (CAL-c hooks `pre_tick_steps` rewiring) — FLAGGED, not built.** Changing
  `build_calendar_hooks`'s return type + the two factory signatures ripples into `manifest.py:411`
  (caller) and `tests/test_calendar_overlay_hooks.py` (3 call sites) — both out of CAL-d scope — and
  the upstream `pre_tick_steps` composition target is unspecified. The `_quarantine_stub` in `hooks.py`
  is left intact (pre-existing). CAL-d's chokepoint (`quarantine_event_text`), knowledge push, and
  memory extractor all shipped; only the briefing/prep render-path laundering waits. **Review needed ⚠️.**
- **DR-c `research_orchestrator_standard` role → DeepSeek endpoint** in `config/roles.toml`. DeepSeek is
  the archived backend (ADR-027), but this is a *logical role mapping* only — off-hardware tests use
  fakes; live Standard-mode is HW-gated (Task 6). `DEEPSEEK_API_KEY` is an env-ref, never inline.
  If DeepSeek stays archived, planning may want to repoint Standard at a local/other orchestrator.
- **FIN-b `extract_email` return type changed** `list[str]` → `EmailExtractionResult(written, suggested)`
  (deviates from the spec's `-> list[str]`) per the cross-model review — removes a tool-layer N+1 and
  makes written-vs-suggested unambiguous. Downstream callers use `.written`/`.suggested` directly.
- **cross_model_review (FIN-a, FIN-b): both FLAGS-ONLY, no BLOCK** — schema/money/privacy/Seam-7/
  always-local all structurally sound; every flag fixed inline before commit (details in the two
  commit bodies). No schema-freeze concerns outstanding.

## Specialist Flags

- **apex-reviewer (FIN-b), HIGH:** the original build gated only `extract.parse_failed`, **not
  `extract.flagged_injection`** — a quarantine-flagged bank email would have reached the extraction
  model. Now gated (warn + return empty, model never called). **Pattern to carry:** any DR-a-reader
  consumer must check `flagged_injection`, not just `parse_failed`. Re-audit the Gmail extractor
  (M8-b1 `GmailMemoryExtractor`) and CAL-d's memory extractor for the same gap when next touched.
- **apex-reviewer (FIN-a), medium:** `add_transaction` did a redundant `SELECT` on *every* insert;
  now a `cursor.rowcount==0` fast path (SELECT only on an ON-CONFLICT no-op). Idempotency unchanged.
- **FIN-b instrument link** was silently dropped on `accept_fin_suggestion` (payload stored
  `instrument_hint`, accept read `instrument_account_id`). Now the FK is resolved *before* the
  suggestion payload is built. Watch this pattern (hint-vs-resolved-FK) in FIN-c/FIN-d.

## Blocked Actions

- Two under-scoped **composition-wiring** steps (M3-c gateway `agentic=`, CAL-d `pre_tick_steps`)
  need planning to (a) name the composition target and (b) scope the caller/test ripples. Both are
  additive — the capabilities ship now; only their production wiring is deferred. Not buildable
  surgically from the specs as written.

## What's Next (discoveries)

- **HOST-VERIFY LESSON (process, applied mid-session):** Codex's per-spec verify recipe runs
  `mypy --strict src tests/<the-one-test>` — file-scoped, so it **missed `tests/eval_research.py`**
  (DR-c) which was red under the full project mypy. Bare `uv run mypy` (no args) caught it (`98c3414`).
  From FIN-a on, host-verify runs the **full** `uv run mypy` + `uv run pytest -q`, not a file list.
  Bake this into the apex-code host-verify step generally.
- **FIN chain continues:** **FIN-c** (recurring detection + reconciliation hooks — this is where the
  finance module gets its first `proactive_hooks`; FIN-a/b ship `proactive_hooks=[]`) → **FIN-d**
  (finance knowledge/memory). FIN-c modifies the same FIN-a/b files + adds hooks; cross_model_review.
- **Remaining cluster spokes** after FIN: SENS-* (sensitivity ingestion gate), RXN-* (cross-module
  reactions, ADR-021), MAPS-connector / TRIP-entity, M3-d (HW-gated visual doc understanding).
  **GATE-b** needs a Tauri re-scope (Swift-era spec, not a clean build).
- No new deps added this session (docling stayed an extra; DR-c/FIN reused installed deps).
