# Handoff — 2026-06-25 (cluster wave, continuation 6)

Coding session (Codex-primary via apex-code; Opus host). Cluster wave spokes built this session:
**M3-c → DR-c → CAL-d → FIN-a → FIN-b**, each host-verified + committed + archived to `done/`.
Baseline green throughout; tree now **547 passed / 2 skipped**, full `uv run mypy` clean (214 files).
Commits: `d8b7d9e` (M3-c) · `830ca13` (DR-c) + `98c3414` (DR-c mypy fix) · `aaa0a91` (CAL-d) ·
`55325e9` (FIN-a) · `3a42b24` (FIN-b). What changed → `git log`; status/known-gates → `status.md`.

## Decisions Made

- **M3-c Task 4 (gateway `compose_brain` agentic wiring) — FLAGGED, not built.** `compose_brain`
  constructs no `AdaptiveRetriever`, so the spec's own Task 4 conditional ("STOP and FLAG rather than
  editing a third file") fired. `Brain.agentic` defaults `None` → preserves M1 behaviour; the agentic
  loop ships fully via the injected path + tests. **Review needed ⚠️:** planning must decide where the
  retriever is composed into the gateway and wire `agentic=` there.
- **CAL-d Task 4 (CAL-c hooks `pre_tick_steps` rewiring) — FLAGGED, not built.** Changing
  `build_calendar_hooks`'s return type + the two factory signatures ripples into `manifest.py:411`
  (caller) and `tests/test_calendar_overlay_hooks.py` (3 call sites) — both out of CAL-d scope — and
  the upstream `pre_tick_steps` composition target is unspecified. The `_quarantine_stub` in `hooks.py`
  is left intact (pre-existing). CAL-d's chokepoint (`quarantine_event_text`), knowledge push, and
  memory extractor all shipped; only the briefing/prep render-path laundering waits. **Review needed ⚠️.**
- **DR-c `research_orchestrator_standard` role → DeepSeek endpoint** in `config/roles.toml`. DeepSeek is
  the archived backend (ADR-027), but this is a *logical role mapping* only — off-hardware tests use
  fakes; live Standard-mode is HW-gated (Task 6). `DEEPSEEK_API_KEY` is an env-ref, never inline.
  If DeepSeek stays archived, planning may want to repoint Standard at a local/other orchestrator.
- **FIN-b `extract_email` return type changed** `list[str]` → `EmailExtractionResult(written, suggested)`
  (deviates from the spec's `-> list[str]`) per the cross-model review — removes a tool-layer N+1 and
  makes written-vs-suggested unambiguous. Downstream callers use `.written`/`.suggested` directly.
- **cross_model_review (FIN-a, FIN-b): both FLAGS-ONLY, no BLOCK** — schema/money/privacy/Seam-7/
  always-local all structurally sound; every flag fixed inline before commit (details in the two
  commit bodies). No schema-freeze concerns outstanding.

## Specialist Flags

- **apex-reviewer (FIN-b), HIGH:** the original build gated only `extract.parse_failed`, **not
  `extract.flagged_injection`** — a quarantine-flagged bank email would have reached the extraction
  model. Now gated (warn + return empty, model never called). **Pattern to carry:** any DR-a-reader
  consumer must check `flagged_injection`, not just `parse_failed`. Re-audit the Gmail extractor
  (M8-b1 `GmailMemoryExtractor`) and CAL-d's memory extractor for the same gap when next touched.
- **apex-reviewer (FIN-a), medium:** `add_transaction` did a redundant `SELECT` on *every* insert;
  now a `cursor.rowcount==0` fast path (SELECT only on an ON-CONFLICT no-op). Idempotency unchanged.
- **FIN-b instrument link** was silently dropped on `accept_fin_suggestion` (payload stored
  `instrument_hint`, accept read `instrument_account_id`). Now the FK is resolved *before* the
  suggestion payload is built. Watch this pattern (hint-vs-resolved-FK) in FIN-c/FIN-d.

## Blocked Actions

- Two under-scoped **composition-wiring** steps (M3-c gateway `agentic=`, CAL-d `pre_tick_steps`)
  need planning to (a) name the composition target and (b) scope the caller/test ripples. Both are
  additive — the capabilities ship now; only their production wiring is deferred. Not buildable
  surgically from the specs as written.

## What's Next (discoveries)

- **HOST-VERIFY LESSON (process, applied mid-session):** Codex's per-spec verify recipe runs
  `mypy --strict src tests/<the-one-test>` — file-scoped, so it **missed `tests/eval_research.py`**
  (DR-c) which was red under the full project mypy. Bare `uv run mypy` (no args) caught it (`98c3414`).
  From FIN-a on, host-verify runs the **full** `uv run mypy` + `uv run pytest -q`, not a file list.
  Bake this into the apex-code host-verify step generally.
- **FIN chain continues:** **FIN-c** (recurring detection + reconciliation hooks — this is where the
  finance module gets its first `proactive_hooks`; FIN-a/b ship `proactive_hooks=[]`) → **FIN-d**
  (finance knowledge/memory). FIN-c modifies the same FIN-a/b files + adds hooks; cross_model_review.
- **Remaining cluster spokes** after FIN: SENS-* (sensitivity ingestion gate), RXN-* (cross-module
  reactions, ADR-021), MAPS-connector / TRIP-entity, M3-d (HW-gated visual doc understanding).
  **GATE-b** needs a Tauri re-scope (Swift-era spec, not a clean build).
- No new deps added this session (docling stayed an extra; DR-c/FIN reused installed deps).

---

# Handoff — 2026-06-25 (continuation 7): RXN-emit · FIN-c · ADR-029 sensitivity wall · FIN-d

Coding session (Codex-primary via apex-code, Opus host). Built 8 specs:
**RXN-emit → FIN-c → SENS-prod-M3a → SENS-prod-M4b → SENS-prod-M8b1 → SENS-carry-M3b →
SENS-carry-M4c1 → SENS-enforce-ragcompose → FIN-d** (9 commits `4f343c8`..`db0e089`).
Each host-verified with FULL `uv run mypy` + `uv run pytest -q`. Baseline green @ `db0e089`:
606 passed / 2 skipped, mypy clean (228 files). **The ADR-029 sensitivity privacy wall is now
end-to-end** (producer → carrier → enforcer). What changed → `git log`.

## Decisions Made

- **Build-order correction (RXN-emit first):** FIN-c imports `artemis.reactions` (`DomainEvent`/
  `EventType`), which did not exist — RXN-emit (a Wave-R infra spec, all prereqs met) was unbuilt.
  Built it FIRST as a mechanical dependency-ordering resolution (not a fork). BUILD-ORDER allows
  R-infra alongside Wave S, so this is in-policy.
- **FIN-c store.py folded into scope** (M8-d-a precedent): the spec's tasks call `store.X()` for new
  repo methods but the Files-table omitted `store.py`; added the thin delegations. Also updated a
  stale FIN-a test assertion (`proactive_hooks == []` → `len == 4`) to match FIN-c's hook wiring.
- **`txn-recorded` emit NOT wired into FIN-b** (already shipped): FIN-c only *exports* the
  `txn_recorded_event` builder per its spec. Wiring the emit around FIN-b's `add_transaction`
  service call is a follow-up (needs the EventBus composed — Wave-R dispatcher). Review needed ⚠️.
- **Cross-model reviews (5 specs, all Codex-built → Opus-reviewed, ADR-026 family split):**
  M3a CLEAN; M4b/M8b1/enforcer/FIN-d FLAG. Genuine hardening applied inline (below); test-fidelity
  nits logged. All privacy-wall invariants verified correct by the reviewers.
- **SENS-prod-M4b hardening:** tombstone INSERT now carries `sensitivity`/`category` explicitly
  (was relying on the DB column DEFAULT) — code-driven, consistent with add/update.
- **Enforcer hardening (privacy wall):** replaced the `assert all(general-or-released)` guards with
  an unconditional `PrivacyWallError` raise (asserts are stripped under `python -O` — the guard is
  load-bearing); brain.py now surfaces a detected breach at ERROR and fails safe to local; the
  fallback `recall_fn` is scope-gated to `OWNER_PRIVATE` (M4-c-1 parity).

## Specialist Flags

- **GmailMemoryExtractor still gates only on `parse_failed`, not `flagged_injection`** — re-confirmed
  this continuation-6 gap while building SENS-prod-M8b1; it's a DR-a injection-gating concern,
  deliberately OUT OF the sensitivity spec's scope. A quarantine-flagged email still reaches the
  classify/enqueue path. Should be closed in M8-b1 scope.
- **Advisory test-fidelity items** (logged, none blocking): FakeExtractor doesn't exercise the real
  classify-fallback branch (M4b); spy-queues drop `source_sensitivity`/`category` (M8b1); FIN-d
  `test_finance_knowledge_push_requires_handles` mutates module globals (xdist-unsafe).

## Blocked Actions (planning must resolve)

1. **`Source.sensitivity` override — HIGH, real privacy gap (FIN-d knowledge push).** FIN-d's
   knowledge-corpus ingest cannot tag the finance doc `"sensitive"` *explicitly*: `Source` has no
   `sensitivity` field and `IngestPipeline.ingest` always re-classifies. **Safe today** (fail-closed
   to `"sensitive"` when no classifier is wired; the dev/test path), but on the fully-wired real path
   a classifier misclassifying finance text as `"general"` would let it reach cloud. Memory side is
   fully fail-safe (explicit `source_sensitivity="sensitive"`). Fix = add `sensitivity` to the
   `Source` dataclass + honour an explicit override in `IngestPipeline.ingest` (skip-classify-on-
   override). This is an **M3-a / SENS-prod-M3a ingest-contract change** (connectors.py + pipeline.py),
   a BIG fork — not patched mid-build. Would also let SENS-prod producers pass explicit tags.
2. **`retrieve_fn` / AdaptiveRetriever composition (carried from M3-c).** `compose_brain` still
   constructs no `AdaptiveRetriever`, so the enforcer's `retrieve_fn` is left `None` (runs on recalled
   facts only) and M3-c's `agentic=` is unwired. Planning must decide where the retriever is composed
   and bind both `retrieve_fn` and `agentic=` there.

## What's Next (discoveries)

- **ADR-029 wall is complete end-to-end.** Producer (M3a/M4b/M8b1) → carrier (M3b/M4c1) → enforcer.
  The only remaining seam is `retrieve_fn` (blocked-action 2) — until then the enforcer gates the
  request + recalled facts; retrieved chunks aren't yet in the cloud-bound prompt because nothing
  composes the retriever.
- **RXN-emit shipped → reaction layer's publish side exists.** Wave R remainder is next-buildable:
  RXN-rulestore → RXN-dispatcher (wires `EventBus.emit` as the subscriber, then FIN-c's 3 finance
  events + the others start flowing) → RXN-reconciler → RXN-recipes-* + TRIP-entity + MAPS-connector
  + calendar.create_from-extract. FIN-c already emits `bill-recorded`/`subscription-detected`.
- **FIN chain complete (a/b/c/d).** Finance now has its first `proactive_hooks` (FIN-c) and the
  sensitivity-tagged knowledge/memory bridge (FIN-d).
- No new deps this continuation.

---

# Handoff — 2026-06-25 (continuation 8)

Coding session (Codex-primary via apex-code; Opus host). **13 specs built, host-verified, committed,
archived** (one partial). Tree: **606 → 732 passed / 2 skipped**, full `uv run mypy` clean (258 files),
ruff clean throughout. Each Codex build was host re-verified (full `uv run mypy` + `uv run pytest -q`,
not the file-scoped subset) before commit. `cross_model_review` specs got an independent Opus review.

**Built this session (commit):** 3 composition-gap fixes — `sensitivity-ground-rules` (b6ebc10) ·
`retriever-wiring` (322a33f) · `flagged-injection-coverage` (616eefe); then the full Wave R
end-state — `M8-d-c2` (8e8ca35, unblocks the chain) · `RXN-rulestore` (50788f8) · `TRIP-entity`
(39c963e) · `MAPS-connector` (8b09c44) · `CAL-create-from-extract` (91e54d9) · `RXN-dispatcher`
(e69e267, keystone) · `RXN-reconciler` (d7cf8fa) · `RXN-recipes-planning` (42eabac) ·
`RXN-recipes-comms` **(fe00960, PARTIAL)** · `RXN-recipes-self` (7ab1c43).

## Decisions Made

- **2 composition blocked-actions RESOLVED.** retriever-wiring closed the ADR-029 `retrieve_fn=None` +
  M3-c `agentic=` gaps (AdaptiveRetriever+AgenticRetriever composed in `compose_brain`; retrieved
  chunks spotlighted). flagged-injection + sensitivity-ground-rules closed the other two.
- **Owner authorised 2 scope expansions** (mechanical, intent-preserving under-enumeration):
  (1) sensitivity Task 6 FIN-d enqueue-drop touched `finance/tools.py` + `tests/test_finance_knowledge.py`
  beyond its 2-file list (the real `memory_queue` plumbing); (2) flagged-injection's Protocol fix
  touched `obs/errors.py` + `obs/telemetry/source.py` + `tests/test_obs_core.py` (no-op methods).
- **Wave R prereq fork — owner chose full end-state.** `M8-d-c2` (CaptureService) was an UNBUILT prereq
  the status NEXT pointer didn't flag; built it first to unblock the RXN dispatcher chain.
- **Cross-model-review fixes applied inline** (Opus host, all re-verified green): flagged-injection
  Protocol-visibility BLOCK (declared `on_injection_flagged` unconditionally + no-op on every sink,
  killing a latent injection-path AttributeError); CAL-create `discard_held_event` status guard
  (don't null an APPROVED event's audit trail); RXN-dispatcher A4-routing-before-external_effect +
  `run_forever` `rules_for` guard; recipes-self B4c over-threshold test boundary; sensitivity
  `text_preview` 200-char cap.

## Specialist Flags

- **RXN-dispatcher stateful dedup is NOT atomic — but not reachable under the single-consumer drain.**
  Non-stateful path uses atomic `try_claim`; the stateful path does read-compare-then-write on
  `state_hash`. Safe today (one sequential consumer). A future MULTI-consumer drain needs a `try_claim`
  first-fire guard or a `BEGIN IMMEDIATE` read+write txn. Documented in `docs/progress/RXN-dispatcher.md`.
- **`reactions/__init__.py` uses lazy `__getattr__` exports** to break an `emit→memory→reactions`
  import cycle (Codex adaptation; standard idiom).
- **`recipes/self.py` `_register_tool` mutates ToolRegistry privates** (`_tools`/`_pending`/`_manifests`)
  — registry has no public `register_tool`. Flag for the registry owner to expose one.

## Blocked Actions

- **`reaction:gift_signal` PARKED** (RXN-recipes-comms is PARTIAL, stays in `docs/changes/`). Needs the
  unbuilt **M4-b module-fact-push amendment** (`source_kind="module"` + gift-signal category on the
  memory write path; ADR-021 dep #1). Build that amendment, then finish gift_signal. Everything else in
  recipes-comms shipped green.
- **`react_bill_paid_lifecycle` is dead-in-production** (RXN-recipes-self): reads `linked_task_ref` from
  the BILL_PAID payload, but `bill_paid_event()` never emits it → always "skipped" in prod. Cross-spec
  contract fix (where `linked_task_ref` is sourced at emit) — resolve WITH the already-known
  "txn-recorded emit not wired into FIN-b" RXN↔FIN emit-wiring follow-up.

## What's Next (discoveries)

- **Reactions layer (ADR-021) is now substantially built** — emit/rulestore/dispatcher/reconciler +
  planning/comms/self recipe packs. Remaining to fully close it: M4-b module-fact amendment →
  gift_signal; RXN↔FIN emit wiring (FIN-c/FIN-b emit points → dispatcher) + bill-lifecycle
  `linked_task_ref`; wire `register_*_reactions` into the composition root.
- **Several MEDIUM semantic flags banked in `docs/progress/*.md`** for planning: TRIP "revise-in-place"
  leg semantics (DO NOTHING vs DO UPDATE) + origin-PLACE on revise; sensitivity DOB bare-date over-fire
  (Ambiguity #3, labelled-only vs birth-year guard); B4c threshold inclusive-vs-exclusive (DECISIONS-LOG
  I-3 "above"); CAL-create `list_held_events` unvalidated status string.
- **No new PyPI deps** this session (all Wave R specs were stdlib/existing-dep).

---

## Continuation 9 (2026-06-25 night) — reactions-composition specs + dev email-rules harness (PLANNING in coding mode; NO build ran)

This continuation did spec-finalisation, not a build. Output: ADR-032 amended + **7 ready specs** in
`docs/changes/` (committed 6d58ab6, cf26ba1). **Owner: next session goes straight to coding mode to
build everything.** The `status.md` In-Flight row is the authoritative build checkpoint.

### Decisions Made
- **Re-carved the reactions build 4 → 6 specs.** Verifying ADR-032's Fork-1 ("reactions fetch the
  laundered extract via `source_ref`") against the codebase exposed a real gap: **no producer builds the
  structured email payload the shipped comms reactions read, and no fetchable laundered-extract store
  exists** (`QuarantinedReader` returns only summary/claims, transient; `GmailIngestor.ingest_message`
  emits nothing). Owner chose the **full Option-B build** → added **R5d** (email detection step on the
  LOCAL responder + owner-private `EmailExtractStore` + `fetch_extract` seam). Final: Wave 1 ∥
  R1·R3·R4m·R5d, Wave 2 ∥ R2·R6c. Old R4 superseded by R4m+R6c.
- **ADR-032 amended** with all six fork resolutions (F2 worker + F4 effect-then-claim OVERRIDE the
  built dispatcher; F1/F1b/F3/F6 refine).
- **`R-dev-email-harness` specced** (Lane B, live Gmail). Owner deferred OAuth setup to exercise-time.

### Specialist Flags (review-caught, FIXED before gate)
- Domain review (2× security + 1 data, AFK) caught **5 BLOCKs** — all fixed in the specs:
  **observe-poisons-live** (observe must NOT write the durable ledger or fire-once slots are consumed
  before `live` ever runs) · **model-output shadowing** in R5d `_coerce` (strip model `source_ref`/
  `summary`) · R5d `summary` length cap · R6c `source_ref` format-validation before store fetch ·
  R4m `object_` param name (matches `repo.add`, avoids ruff A002).
- **Build-time watch-outs for next session:** (1) R1 depth guard authority is the queue-level
  `event.depth` check (the ContextVar stamp is best-effort; handlers should direct-`await`, not
  detached `create_task`). (2) The R5d/QuarantinedReader local model MUST be toolless. (3) M8-a live
  OAuth (Task 7) is code-complete but NEVER exercised — first real consent round-trip may need a fix.

### Blocked Actions
- None — the build was **held by owner choice**, now greenlit. No permission/architecture blocker.

### What's Next (discoveries)
- **Infra for the email-rules loop is already built** (don't rebuild): M8 auth + Gmail connector +
  urgency, all CAL, all M8-d, and `dev-model-stack-ollama`. The only code left for the whole live
  email→rules loop is the **7 specs**.
- The reactions email path **does NOT need docling/M3-a** (uses `GmailMemoryExtractor.extract` →
  `QuarantinedReader` directly, not the M3 `IngestPipeline`). Removes a long-standing blocker from this path.
- Exercise tail (owner/ops, post-build): `ollama pull` Qwen3 + Google OAuth client + `artemis-google-auth`
  login (test account). Re-surface OAuth steps at exercise time (memory `dev-email-rules-oauth-at-exercise`).
