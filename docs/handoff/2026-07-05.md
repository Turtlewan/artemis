# Handoff — 2026-07-05 (session 12, fable host: agent-loop arc AL-1→AL-3 shipped + AL-4 fully specced)

One session took the ADR-047 arc from "two owner decisions pending" to: AL-1/2/3 BUILT+SHIPPED
(4 commits) and the complete AL-4 set (6 specs) reviewed+gated+ready. What changed → `git log`
(`932da44`, `d44af9a`, `e01842c`, `58e1c70`); state → `docs/status.md` (SWITCH POINT block at top).
This handoff = what the build/planning learned.

## Decisions Made
- **Owner resolved the arc's model roles live:** Sonnet drives / Haiku grunt-work; escalation =
  CROSS-FAMILY Sonnet→Codex (owner picked over the in-family recommendation — second sub's quota +
  model diversity); judge = independent no-tools Haiku. Recorded as ADR-047 Amendment.
- **Owner: delivery posture = DELIVER + VISIBLE CAVEAT** for flagged/unjudged answers (judge = quality
  gate + injection detector, NOT an enforcement gate; enforcement escalation is a future decision that
  would ride its own security review).
- **Owner: UI work goes LAST** (after arc + small fixes + open decisions); session-host routing saved
  in status.md `session_host_routing` + memory `session-host-switch-prompts`.
- **Autonomous (logged): AL-3a/3b built sequentially, not parallel-worktree** — both verify full-suite
  in one tree; worktree overhead > the win for a 2-file registry spec.
- **Autonomous: AL-4c kept as ONE spec at 5 non-test files** (over the ≤3 rule) — a thin vertical
  slice (Rust DTO → TS DTO → store → chip → render); splitting would strand unrendered fields.
- **Autonomous: eval scorer stays Opus** (not Fable, despite the ai-systems FLAG citing the model
  table) — rubric-constrained grading ≠ open critical judgment; Fable pool deliberately scarce per
  owner routing. Documented in the harness spec.
- **Autonomous: stall-induction gets BOTH reads** — harness-only `--primary-budget` knob for a
  forced-stall gating number + the natural measured-subset rate as honest secondary.

## Specialist Flags
- **The dual-review pipeline caught 6 BLOCKs pre-build across the arc** — all folded, none shipped:
  judge-reason re-injection (AL-2, second-order injection into the corrective retry); AL-3a
  test-value short-circuit (invariant path untested); judge calibration measured only false-REJECTS —
  the safety-critical false-ACCEPT direction (judge rubber-stamps hallucination) was unscored AND
  unbarred (2 BLOCKs, harness+gate); the gate had NO frozen report schema (malformed report could
  silently GO); gate threshold tests couldn't discriminate ≥ from > (off-by-one on the go-live bar);
  AL-4c caveats were visible-only (screen readers would never announce the safety-relevant
  "unverified" warning).
- **A real scope-lock fork fired and worked as designed** (AL-3a build): adding the 10th role staled
  the routes-test's hard-coded role count + collided a test binding with the new default. Codex
  refused the out-of-scope file, reported; planning amended the spec (Task 3) + a follow-up dispatch
  completed it. Lesson folded forward: the AL-4b brief pre-checked `test_model_routes.py` up front —
  and indeed needed no change.
- **Sandbox skip-count artifact recurs on every Codex build** (full-suite skips +7 inside the
  sandbox, resolves on host re-verify) — known noise, not a defect; don't chase it.
- **`_NO_TOOLS_PROVIDERS` = {claude_code, ollama}** — the owner's judge roster stays eligible after
  AL-4b; codex requires the version-pinned two-layer smoke (existing research) before admission.
- **`synth` role is call-site-only tool-less** (same posture judge had pre-AL-4b) — filed in the
  small-fix list for admission if its call sites multiply.

## Blocked Actions
- none. (One transient: opus-tier Agent dispatch failed once on model availability — retry succeeded.)

## What's Next (discoveries)
- **The switch protocol is live:** next session = OPUS build run over the 6 ready AL-4 specs (order
  in the status.md SWITCH POINT block). After the build: owner applies the roster via /app/models,
  runs the eval gate live (owner-initiated, spends quota), then makes the flag-flip decision against
  the gate artifact — which also carries the NVDA manual-pass and request-timeout checklist items.
- **The eval corpus wants owner labeling time:** judge-calibration cases need owner-labeled
  (evidence, answer, passed?) pairs, and the driver golden-set captures from the live DataStore —
  a short owner-present session once the corpus tooling builds.
- **Brain watch item:** one `fetch_oauth_mint_failed capability=calendar-sync` at brain boot
  (monitor event, 2026-07-05) — single fail-soft occurrence, likely boot-race; escalate only if it
  recurs on a scheduled (non-boot) tick.
- **UI design inventory is ready** (`docs/v2/ui-design-inventory.md`) for the owner's design-overhaul
  session — deliberately queued LAST per owner directive.
