# Handoff — 2026-06-09 (planning)

_Session: resumed → designed + specced the **M8 first-spoke-wave** AND resolved the architecture gap it
surfaced. Two background-agent waves (draft → self-review → gate, then reconcile-to-real-seams).
**Outcome: +7 specs ready** (53 total); **1 parked** (M8-b2). This doc is the non-obvious rationale the
next session needs; "what changed" = git, ready/parked state = status.md._

## Decisions Made (the WHY)

1. **Gmail = read-only mirror, split-by-depth ingestion** (`modules/gmail.md`). Metadata-index ALL mail;
   full body + attachments + memory only for signal categories (Primary/Updates/Forums). Gmail's own
   category label (kept as metadata) gives marketing-vs-real differentiation *for free* → no need to
   deep-read junk; minimises injection surface + memory pollution + Mini cost. Bounded 12-mo backfill,
   History-API sync, end-state 3-stage urgency hook. Owner chose the fullest hook.

2. **Productivity = Tasks + Projects + Areas** (`modules/productivity.md`, all forks LOCKED): full 3-level
   Calendar time-blocking (but the gap-fill + completion-check *hooks* opted out — capability stays);
   capture = **suggestion-inbox you guide → written automation recipes** (the owner's framing "I guide
   it at the start but the automation rules get written" maps exactly onto the M7 recipe loop + the
   ADR-012 recurrence→recipe bridge — the rules ARE recipes, inspectable, not a black box); **no
   Google-Tasks / no external integration at all**; **both** recurrence modes; hooks = Morning-plan +
   Overdue + Weekly-review. Habits/Goals deferred (the time-blocking rail is reserved for them).

3. **★ Gated-action staging — the architecture gap, RESOLVED → ADR-012 + GATE-a/b.** Building CAL-b
   revealed the owner-approval surface (M7-b `ReviewSurface` + CLIENT-b `/app/review/*`) is entirely
   **recipe**-based: a recipe is a reusable learned-behaviour *template* (`approve(name)` = "may auto-fire
   in future"). A gated calendar write is a one-off *instance* with bound args (`approve` = "execute this
   once"). Conflating them is a category error. **Decision (ADR-012):** one-off actions are **pending
   actions** in a dedicated `PendingActionStore` + `ActionStagingService` (stage → owner approves on the
   Review screen → execute-once via the ToolRegistry). The two surfaces are complementary — pending
   actions = *permission now*, recipes = *automate later* — and the recurrence of staged actions is the
   signal that feeds the recipe-learning loop. This was the headline blocker for the whole write path +
   every future external-effect spoke; it is now built (GATE-a backend + GATE-b client).

4. **Module-layout convention LOCKED:** domain modules under `src/artemis/modules/<name>/` (calendar,
   gmail, productivity); shared Google auth stays in `integrations/google/` (infra). M8-b1 was migrated
   from `integrations/google/gmail/` → `modules/gmail/` (auth imports preserved).

5. **AFK drafting via parallel background agents (autonomous).** Two waves; agents drafted + self-reviewed
   + ran the gate + parked-on-seam-mismatch, then a reconciliation wave bound every parked seam to the
   real (now-existing) symbol. The "park don't guess" discipline is what surfaced ADR-012 instead of a
   broken guessed binding — worth preserving.

## Specialist Flags (non-obvious gotchas a diff won't show)

- **The two-untrusted-boundary design is the spoke pattern** (M8-b1, mirrored in CAL-d): (1) raw external
  text stored VERBATIM in the knowledge corpus — untrusted-at-rest, gated at RETRIEVAL by the existing
  M3-b/brain-boundary (NOT re-quarantined at ingest, consistent with M3-a); (2) every LLM-generative path
  (memory extraction, urgency scoring, briefings) goes through DR-a `QuarantinedReader` first — privileged
  side sees only the sanitized `Extract`. Keep this split for all read/mirror spokes.
- **GATE-a `approve` executes server-side, once, re-validating args against the tool schema, under the
  live unlock gate, with TTL-expiry** — it reuses the brain's ToolRegistry dispatch (no second execution
  route to audit). The `args` payload is owner-private and never carries untrusted external text.
- **One remaining seam mismatch in the whole M8 wave** (see Blocked) — everything else reconciled to real
  symbols (CAL-d↔CAL-a `CachedEvent`, CAL-b/c↔GATE-a `stage()`, GATE-b↔GATE-a, M8-b2 `Fact` fields).

## Blocked Actions (none left for the spoke wave)

1. **✅ M8-b2 pre-flight — RESOLVED this session.** M6-c was amended in place (added an optional
   `pre_tick_steps: list[Callable[[], Awaitable[None]]]` param to `attach_to_heartbeat` +
   `compose_proactive` — one param, an await-loop, a test). M8-b2's QuarantinedReader pre-flight runs
   there; `check_ref` stays LLM-free and the full dual-LLM quarantine posture holds (raw mail stays inside
   `fetch_extracts`; only `Extract.summary` ≤500 chars reaches the scoring model). M8-b2 is now ready.
   NB: `pre_tick_steps` is global to the `compose_proactive` call — the composition root must collect all
   modules' pre-flight callables into one list (a composition-time concern; matters if a 2nd module follows
   M8-b2's pattern).

2. **✅ M8-d-c2 — RESOLVED + drafted (ready).** The capture-recipe graduation seam was the open question;
   resolved to a THIRD recipe-author: an **owner-behaviour-distilled CANDIDATE** written via
   `RecipeStore.write` (M7-a1) → M7-b's owner-gated promotion (TOUCHES_DATA → gated → PENDING → approve →
   ENABLED). **NOT M7-c** — its grounding gate requires ≥2 external web sources, structurally impossible
   for owner-derived automation; **NOT M7-a2** — that's teacher-distill for solve-a-task, wrong input.
   This generalises the recipe model: recipes have three authors (teacher / curiosity / owner-behaviour),
   all flowing through the one M7-b review gate — worth remembering if the M7 loop is touched.

## What's Next (discoveries / follow-ups)

- **✅ M8-d-b + M8-d-c1 drafted + ready this session.** M8-d-b = the time-blocking seam (`task.schedule`
  + the new `calendar.schedule_task` self-only focus-block reusing CAL-b's `block_focus_time` AUTO path +
  the Task↔Event link; **auto-cancels the old block on reschedule** so no orphan focus-blocks). M8-d-c1 =
  the 3 Tier-1 hooks (Morning-plan/Overdue/Weekly-review; payload = counts+IDs only as the M6-b injection
  boundary; `weekly_review` uses `interval_seconds=604800`+dedup, NOT a day-of-week cron, since M6-a's
  evaluator is daily-only). Only M8-d-c2 (capture) remains — see Blocked #2.
- **GATE-b modifies the ready CLIENT-b/c/e specs additively** (new `/app/actions/*` endpoints + a Review
  "Pending actions" tab + ArtemisKit DTOs). Confirm those additive edits land cleanly at build time.
- **The recurrence→recipe bridge (ADR-012 §5) now spans three places** — pending-action recurrence,
  Gmail/Calendar capture, and Productivity §G capture. If anyone touches the M7 distill/curiosity loop,
  that bridge is the load-bearing edge that turns "kept approving the same thing" into a written recipe.
- **Bring-up artifacts still owed** (status.md): `BRING-UP-RUNBOOK.md` + `SECRETS-INVENTORY.md`.
- **Hardware re-decision still pending WWDC** — doesn't block the queue.

---

# Handoff — 2026-06-09 (planning, SESSION 2)

_Session: WWDC + homelab + self-training research, Finance design, repo→GitHub, cross-module-links dive.
Heavy parallel-agent fan-out throughout. "What changed" = git; ready/parked state = status.md; this is the
non-obvious WHY._

## Decisions Made (the WHY)
1. **Hardware: WAIT for the M5 (Pro) Mac Mini → buy 64GB** (ADR-001 §Refinement 2026-06-09). WWDC was
   software-only (no M5 Mini). The owner chose to wait, NOT buy M4 Pro now. Rationale that makes this
   *free*: the Mini caps at 64GB on **both** M4 Pro and M5 Pro, so waiting costs **zero memory headroom** and
   gains M5 GPU speed; the build is fully front-loaded so the wait has no idle cost. ⚠️ The first WWDC
   research agent **falsely claimed 64GB was discontinued** — verified against apple.com it is NOT. Distrust
   that agent's other (software) claims accordingly; they were re-verified separately.
2. **WWDC software = no forced stack changes.** One watch-item (Apple on-device Foundation Model now has a
   *disputed* Python SDK — if real, a $0 model for cheap brain tasks, but Qwen3-4B already fills that role).
   One strategic note: **Siri AI** moves into Artemis's space, but the privacy/self-hosted thesis differentiates.
3. **Self-training reframed to CAPABILITY (not just personalization).** The owner wants "make it smarter."
   The legit path = **reasoning-distillation from Claude (+DeepSeek judge) into a ~14B student** + Tier-1
   inference-time levers (RAG/test-time-compute) first. Most of the pipeline is **PC-doable now** → new ready
   spec `distill-datagen-pipeline` (verified `claude -p … --output-format json` works on Windows; **batch K
   traces/call** — each call reloads ~44k tokens of context, so one-per-call would be quota-brutal).
4. **Homelab = "Artemis Cognitive Infrastructure (ACI)"**, phased + trigger-gated. **GPU box (P3) is the
   keystone lever** AND the home for serious fine-tuning (Apple can't drive NVIDIA) — integrates via a
   **LiteLLM proxy = zero Artemis code change**. Self-training is a cross-phase ACI lane.
5. **Finance spoke DESIGNED** (`modules/finance.md`): owns ledger, **email-extraction + manual, NO bank link
   ever** (SG aggregator coverage is poor + privacy), awareness-first → full-brain end-state, read-only/no
   GATE. Dedup = layered reconciliation; cross-module links via the M8-d-b pattern.
6. **Cross-module-links dive (4 agents) → `docs/research/cross-module-links.md`.** Keystone conclusion:
   **extend Memory (M4) as the entity backbone, do NOT build a Contacts module** — a registry would cross
   encrypted scopes; M4 is already a de-facto people graph in `owner-private`. The **M4 `fact_key` as the
   canonical cross-module person pointer** is the one decision that must precede Finance/Health/Comms/Travel
   (ad-hoc strings break at ~10–15 spokes). External prior-art unanimously backs a central entity graph.

## Specialist Flags (non-obvious gotchas a diff won't show)
- **AFK agents can't persist files in planning mode** (PreToolUse hook denies non-interactive writes, even
  `.md`). Working pattern this session: agents *return* the full artifact, the **main agent writes it**.
  (Saved to memory [[afk-spec-drafting-workflow]].)
- **`artemis.untrusted` boundary is re-implemented per-module** (Gmail/Calendar/Productivity/Finance each
  redo the trusted/untrusted split) — a shared-helper refactor candidate surfaced by the links audit.
- **Two structural entity gaps** the later spokes need: no **Place/Location** home, no **Goal** node (Goal
  folds into the deferred Habits/Goals rail; enables the goal-cascade prior-art pattern).
- **Camera reads as a sensor layer, not a user-facing module**; its receipt-OCR closes Finance's cash gap.

## Blocked Actions (planning must resolve)
1. **Write the cross-module-links ADR** — locks the 6 decisions in `cross-module-links.md` §Part 7 (fact_key
   pointer · `{module,entity_id}` contract · lifecycle sync · hub synthesis · bidirectional+auto-suggested ·
   extend M4 + Place + Goal). Do this **before** speccing any later spoke.
2. **launchd→Keychain `.env`-injection script** is unspecced but referenced by M8-a/M0-b/DR-b (SECRETS §P5 /
   RUNBOOK §P8) — needs a small M0-b follow-up spec.

## What's Next (discoveries / follow-ups)
- **Repo is now on GitHub** (`Turtlewan/artemis`, private, `main`). Future doc/spec changes commit+push;
  the Mini clones via a deploy key (RUNBOOK Step 2c). Steady-state may migrate origin to self-hosted Tailscale git (ACI).
- **CAP workstream:** build `distill-datagen-pipeline` (coding mode) → start the pre-Mac data-gen drip; then
  flesh out the 6-category generation prompts.
- **Camera module** still owed a dedicated discussion (BACKLOG, flagged).
- **10 prior-art cross-domain link ideas** spun into BACKLOG (gift-budget pipeline, person↔debt edge,
  unlinked-mention detection, relationship-decay, task-deadline-vs-meeting, news-on-contact brief, Goal-cascade,
  health↔productivity, camera-OCR, Place entity).
- **Open threads, none urgent:** cross-module ADR · env-injection spec · camera module · build distill pipeline
  · more spokes (Notes/Journal was the other high-value pick).
