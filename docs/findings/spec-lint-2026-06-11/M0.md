# Spec-Lint Report — M0 area — 2026-06-11

Final pre-handoff lint pass. Executor: DeepSeek V4-Flash (literal). Each finding classified BLOCK (stop handoff) or WARN (soft flag).

---

## M0-a-foundation-layout.md — WARN-only

Verdict: **WARN-only** (0 BLOCK, 3 WARN). Strong spec; per-task files + runnable done-criteria throughout. Minor under-specifications that Flash can still build, but with avoidable guesswork.

| spec:line | BLOCK/WARN | check | what's wrong | minimal fix |
|---|---|---|---|---|
| M0-a:50 | WARN | code detail | `roles.toml` model_ids `Qwen3.6-27B`, `Qwen3-Embedding-0.6B` etc. are plausibly invented HF repo ids; Flash will write them verbatim and they may not resolve as `model_path` in M0-c's YAML. Cross-spec contract risk, not blocking here (M0 doesn't pull weights). | Add a comment in Task 2 that model_ids are logical labels; concrete HF repo paths are resolved in M0-c Task 2 `model_path`. |
| M0-a:54 | WARN | task atomicity | Task 4 defines 7 distinct functions (`slot_root`, `scope_dir`, `vault_dir`, `backups_dir`, `logs_dir`, `env_file`, plus ValueError guard) in one task with a long embedded D4/B8/B9 narrative — >3 sub-steps. | Acceptable as one module, but flag: each function signature is exact, so leave as-is; noted for splitting only if it fails review. |
| M0-a:54,60 | WARN | code detail | `Settings` field list is split across Task 3 (slot, data_root, 4 ports, roles, roles_file) and Task 7 (`worktree_root`). Flash builds Task 3 first and may emit a "complete" Settings without `worktree_root`, then Task 7 must edit it. Two-touch on one file. | Move the `worktree_root` field declaration into Task 3's field list; keep Task 7 limited to the example env files + default note. |

---

## M0-b-launchd-services.md — WARN-only

Verdict: **WARN-only** (0 BLOCK, 4 WARN). Two-phase spec (flagged in-spec). Every file named, every task has a runnable check. Task 7 is dense but each behaviour step is concrete with exact git/launchctl commands.

| spec:line | BLOCK/WARN | check | what's wrong | minimal fix |
|---|---|---|---|---|
| M0-b:58 | WARN | task atomicity | Task 7 has 7 numbered behaviours (risky-detect, pipeline, UAT bootstrap, backup, tag, promote, rollback) in one task — well over the >3 sub-step flag. It is buildable (each step is concrete) but is the most likely place Flash silently drops a later sub-step (e.g. step 7 `--rollback`). | Split into M0-b2a (deploy/promote + risky-detect, steps 1-6) and M0-b2b (`--rollback`, step 7). Or at minimum add a per-step done-checklist to the done-when. |
| M0-b:58 | WARN | cross-reference | `scripts/backup.sh --slot <to>` is a placeholder call to a script created in M0-e (later spec). Flash building M0-b before M0-e will reference a non-existent file. The done-when is dry-run gated so it won't fail, but the call shape is assumed. | Keep — dry-run guard covers it; note that `backup.sh` ships in M0-e. Already stated as "placeholder call". |
| M0-b:46 | WARN | acceptance | Task 1 `/readyz` returns `{"status":"ok"|"degraded",...}` but the M0 stub "always reports ok"; done-when only checks `/healthz`. `/readyz` has no runnable assertion. | Add to done-when: TestClient `GET /readyz` returns 200 with `status=="ok"` and `checks=={}`. |
| M0-b:54 | WARN | env preconditions | `render_plists.py` reads `{RUNTIME_USER}` from env `ARTEMIS_RUNTIME_USER` and binary-path placeholders "from env with documented defaults" — the defaults are not given in the spec. Flash will invent defaults for `{MLX_LAUNCH_CMD}`/`{NTFY_BIN}`/`{AUDIO_SIDECAR_BIN}`. | Inline the default values (e.g. `NTFY_BIN` default `/opt/homebrew/bin/ntfy`, `AUDIO_SIDECAR_BIN` default path, `MLX_LAUNCH_CMD` filled by M0-c) in Task 5. |

---

## M0-c-mlx-server.md — BLOCK

Verdict: **BLOCK** (1 BLOCK, 4 WARN). One stale file-name reference that will make Flash write to the wrong file, plus version-pinned facts the executor cannot verify.

| spec:line | BLOCK/WARN | check | what's wrong | minimal fix |
|---|---|---|---|---|
| M0-c:96 | **BLOCK** | code detail / cross-ref | Performance section says resident flags are "Encoded as `resident` flags in **mlx.toml**" but the actual file is `config/mlx-models.yaml` and the field is `on_demand` (Task 2). Flash reading Specialist Context literally may create a stray `mlx.toml` or emit a `resident:` key that contradicts the `on_demand` schema in Task 2. Contradictory file/field name within one spec. | Edit line 96: "Encoded as `on_demand: false` for the responder in `config/mlx-models.yaml`." Remove the `mlx.toml`/`resident` wording. |
| M0-c:39 | WARN | env preconditions | `install_mlx.sh` runs `brew install ntfy` — Homebrew presence is a precondition not named; on a box without brew the script aborts under `set -euo pipefail`. ntfy install is arguably M0-b/bootstrap concern leaking into M0-c. | Add precondition "Homebrew installed (deploy bootstrap)"; or gate the brew step behind a `command -v brew` check. |
| M0-c:39 | WARN | code detail | done-when hedges: "(or the confirmed module name)" for `mlx_openai_server` — Flash needs ONE exact import name. If the real module differs it will pick wrong. | Pin the exact importable module name (verify against the 1.8.1 package); drop the parenthetical. |
| M0-c:43 | WARN | code detail | Task 3 hedges roles.toml endpoint: keep `{MLX_PORT}` placeholder "if Settings needs a concrete value, resolve via the slot's `mlx_port`" — two divergent behaviours offered; Flash picks one arbitrarily, possibly breaking `Settings` validation (endpoint with a literal `{MLX_PORT}` is not a URL). | Decide one: store endpoint as `http://127.0.0.1:{MLX_PORT}/v1` literal in roles.toml AND have `config.py` substitute `s.mlx_port` at load. State that single behaviour. |
| M0-c:18 | WARN | external/version | Spec pins many 1.8.1 CLI/config facts (`--tool-call-parser qwen3`, multi-model YAML keys, `on_demand_idle_timeout`) as ground truth; if any differ in the real 1.8.1 package Flash cannot detect it (gated tasks are on-hardware). | Accept (gated), but flag for build-time verification against installed 1.8.1 before wiring the YAML. |

---

## M0-d-ports-scaffolding.md — BLOCK

Verdict: **BLOCK** (1 BLOCK, 2 WARN). One internal type-definition contradiction that a literal executor will resolve inconsistently across files, breaking `mypy --strict` (the load-bearing gate for this interface-only spec).

| spec:line | BLOCK/WARN | check | what's wrong | minimal fix |
|---|---|---|---|---|
| M0-d:42,66,83,84 | **BLOCK** | code detail | `Message` is specified inconsistently: Task 1 (line 42) says `Message` frozen dataclass `{role,content}` in types.py; model.py Task 4 comment (line 83) says "`Message` is a frozen **dataclass / TypedDict**". A dataclass and a TypedDict have different construction/usage. Flash may define one in types.py and reference it as the other, or duplicate it. Under `mypy --strict` this is a hard fail, and Task 6 `__all__` exports `Message` once. | Edit line 83 to remove "/ TypedDict" — `Message` is a `frozen=True` dataclass defined once in types.py (matching Task 1). Single definition, single import. |
| M0-d:60,76,80 | WARN | code detail | `RouteDecision` (routing.py) and the `embed` method on `ModelPort` use `Vector`/`Usage`/`Message` imported from types.py, and `ModelResponse(BaseModel)` mixes pydantic BaseModel with frozen-dataclass `Usage` as a field type. pydantic v2 accepts a dataclass field but Flash may need `arbitrary_types_allowed` or a model_config; not stated → possible mypy/runtime friction. | Add a one-line note in Task 4: `ModelResponse` is a pydantic `BaseModel`; `Usage` is a stdlib frozen dataclass field — no extra config needed in pydantic v2 (or add `model_config` if required). Make the expectation explicit. |
| M0-d:98 | WARN | acceptance | Task 7 done-when asks to assert "no Protocol method has a non-`...` body" via a conforming dummy + isinstance, but Protocols are structural — an isinstance/structural check does NOT prove method bodies are `...`. The stated assertion doesn't test the stated property. | Reword done-when to what is actually checked: a conforming stub type-checks against the Protocol (mypy) and `_is_protocol` is True; drop the unprovable "non-`...` body" claim. |

---

## M0-e-isolation-backup.md — WARN-only

Verdict: **WARN-only** (0 BLOCK, 4 WARN). Two-phase spec (flagged). One Modify-table inconsistency and several on-hardware hedges, but the off-hardware testable core (backup mechanic) has exact `PRAGMA` assertions.

| spec:line | BLOCK/WARN | check | what's wrong | minimal fix |
|---|---|---|---|---|
| M0-e:39,65 | WARN | file explicitness | Files-to-Change table (line 39) lists `scripts/render_plists.py` as **modify**, but the Permissions Modify row (line 65) says "(none in M0; render_plists.py extension noted in Task 5 **may** modify...)". Contradiction: is the modify in scope or not? Flash may skip the render_plists.py edit (silently dropping the backup-plist registration, F2). | Make Permissions Modify row list `scripts/render_plists.py` unconditionally (matching the Files table + Task 5). Remove "may". |
| M0-e:44 | WARN | env preconditions | `claude-code-sandbox.json` schema is admittedly unknown ("confirmed against the installed Claude Code on the Mini"). Flash invents a JSON shape; the allowed-fs/deny-network keys it picks may not match any real Claude Code sandbox schema. done-when only checks `json.load` parses + data root excluded. | Accept (gated + explicitly drafted-now/confirm-later); flag that the JSON is a draft whose key names are verified on-hardware. Already stated. |
| M0-e:48 | WARN | task atomicity | Task 4 (backup.sh) bundles snapshot + keying-stub + immutable-flag + retention-with-uchg-clear — 4 behaviours. Buildable (each is concrete with exact commands) but dense. | Leave; each sub-behaviour has an exact command + the done-when asserts all four. |
| M0-e:42 | WARN | code detail | Task 1 `setup_build_user.sh` applies "`chmod`/`chmod +a` ACLs DENYING ... read" — the exact `chmod +a` ACL string is not given; Flash will invent the ACE syntax (e.g. `user:artemis-build deny read`). Gated on-hardware so it won't be verified off-box. | Inline the exact ACE, e.g. `chmod +a "artemis-build deny read,list,search,readattr" /opt/artemis/<slot>/owner-private`. |

---

## M0-f-env-injection.md — WARN-only

Verdict: **WARN-only** (0 BLOCK, 2 WARN). The strongest spec in the set: exact function signatures, full per-branch logic, 9 enumerated test cases each with a concrete assertion, exact CLI behaviour, and a wave plan. `cross_model_review: true` set appropriately for a secrets spec.

| spec:line | BLOCK/WARN | check | what's wrong | minimal fix |
|---|---|---|---|---|
| M0-f:62 | WARN | task atomicity | Task 1 specifies six functions (`read_keychain`, `parse_existing_env`, `build_env`, `write_env_file`, `main`, + the dataclass/constant) in one task — >3 sub-steps. Mitigated by exact signatures per function and a Wave plan, so a literal executor can build each deterministically. | Acceptable given each signature is exact; flag only for optional splitting. No change needed for handoff. |
| M0-f:65 | WARN | acceptance | Task 2 done-when relies on "a forced `inject_env.py` failure (e.g. missing required secret on a box with no Keychain entries)" — off-hardware (no `security` binary) `read_keychain` fails-closed to None for ALL items, so required-missing→exit 1 naturally occurs; but on a dev box WITH partial Keychain entries the outcome differs. The check is environment-dependent. | Make the deploy-wiring test deterministic: assert the `== inject secrets ==` line is printed and `inject_env.py` is invoked (grep the dry-run output), independent of Keychain state. |

---

## Area verdict

**NOT handoff-ready** — 2 BLOCKs (M0-c stale `mlx.toml`/`resident` reference vs the `mlx-models.yaml`/`on_demand` schema; M0-d `Message` dataclass-vs-TypedDict contradiction that breaks the `mypy --strict` gate). Both are single-line fixes. M0-a/b/e/f are WARN-only and shippable. Fix the two BLOCK lines, then the M0 area is handoff-ready.
