# Spec-Lint — Final Pre-Handoff Pass — M5/M6 (2026-06-11)

Executor target: **DeepSeek V4-Flash** (literal; fills gaps with plausible-but-wrong code; silently skips later phases of multi-phase tasks). Lint axis = spec quality, not model capability.

Checklist applied per task: File explicitness · Task atomicity · Code detail · Acceptance criteria · Cross-references · Env pre-conditions · Scope/size · Commands.

Severity legend: **BLOCK** = stop handoff; **WARN** = soft flag.

---

## M5-a-audio-sidecar.md — WARN-only

Strong spec: every file pathed, contracts inlined (frame format `1-byte kind + 4-byte BE length + body`, message enum, `PCM 16k/mono/Int16`), runnable `swift build`/`swift test` checks, GATED on-hardware work cleanly fenced behind `ARTEMIS_AUDIO_HW`. No BLOCKs. The cross-references that matter (frame shape, message set, socket perms) are inlined, so Flash can build off the spec text alone.

| spec:line | BLOCK/WARN | check | what's wrong | minimal fix |
|---|---|---|---|---|
| M5-a:48 | WARN | cross-ref | Task 1 says "mirror `swift/ArtemisBroker/Package.swift`" for tools-version/platform/strict-concurrency — Flash must open that file to copy the macOS platform ceiling. The load-bearing values (`swift-tools-version: 6.0`, one exe target `artemis-audio` path `Sources/ArtemisAudio`, one test target, strict concurrency) ARE inlined, so it can build; only the macOS platform version is by-reference. | Inline the exact platform literal, e.g. `platforms: [.macOS(.v14)]` (or whatever the broker uses), so no file read is needed. |
| M5-a:68-70 | WARN | atomicity | Task 6 creates TWO files (`BargeIn.swift` + `StateMachine.swift`) — two artefacts in one task. They are tightly coupled (controller wires the machine) and each has its own done-when, so Flash is unlikely to skip a phase, but it sits at the split guideline. | Acceptable as-is; if splitting, make StateMachine its own task ahead of BargeIn. |
| M5-a:72-74 | WARN | atomicity | Task 7 creates TWO files (`IPCServer.swift` + `main.swift`) — server + entry point in one task. Risk: Flash builds the server and stubs `main.swift`. The done-when (`swift run … serve` emits the scripted event sequence) forces both, mitigating it. | Acceptable; the runnable done-when binds both files. |
| M5-a:73 | WARN | code detail | Peer-cred check named via `getpeereid`/`LOCAL_PEERPID` and "mirror the broker's peer-uid gate" but the exact comparison is prose ("connecting uid MUST equal the sidecar's own uid"). Concrete enough to implement; the broker reference is a pattern echo, not a required read. | Optional: inline the one-line assertion `guard peerUid == getuid() else { reject }`. |
| M5-a:117 | WARN | env/network | GATED model fetch ("Download the openWakeWord + Silero model files") names no URL/source/version. Off-hardware build is unaffected (compile-guarded), and it is on-Mini gated, so not a build blocker. | Name the model source + pinned version in the Task 9 text when bring-up confirms it. |

---

## M5-b-stt-tts.md — PASS

Cleanest of the set. Port signatures inlined verbatim (`transcribe(self, audio: bytes, *, language: str | None = None) -> str`, `synthesize(self, text: str) -> Iterator[bytes]`), routing rule inlined (`language is None or language.startswith("en")` → Parakeet), PCM constant inlined (`16000/1/"int16"`), degrade ladder specified, every task has a `uv run mypy/pytest/ruff` check, conformance assertion pattern present. The U4 amendment correctly pins the TTS package to `mlx-audio` and flags `kokoro-mlx` as an unverified supply-chain name. No BLOCK, no WARN of substance.

| spec:line | BLOCK/WARN | check | what's wrong | minimal fix |
|---|---|---|---|---|
| M5-b:69 | WARN | commands/atomicity | GATED Task 6 bundles the warm-Kokoro latency check AND a "Kyutai Pocket TTS vs Kokoro" benchmark note in one task. Both are on-Mini gated (no off-hardware build impact) and the comparison is record-only. | Acceptable; both are gated empirical, not build steps. |
| M5-b:89 vs 67 | WARN | commands | Command table line 89 lists `uv add parakeet-mlx mlx-whisper` but Task 5 (line 67) says "the confirmed Parakeet-MLX + MLX-Whisper packages (confirm names at GATED Task 5)" — the package names are explicitly unconfirmed-until-hardware. Consistent and gated, just noting the name is provisional. | None needed; the "confirm at GATED" gate is the correct guard. |

---

## M5-c-speaker-id.md — WARN-only

The hardest spec (modifies the Gateway, threads voice-ID≠key, Tier classifier). It holds up: port signature inlined (`identify(self, audio: bytes) -> PersonId | None`), the Gateway voice-path logic is given as near-pseudocode with the exact resolution expression `registry.manifests()[module_fq.split(".")[0]].data_scope`, the `tier_for` truth table inlined, `BrainResponse(...)` shape inlined, and a python-one-liner acceptance check. No BLOCKs.

| spec:line | BLOCK/WARN | check | what's wrong | minimal fix |
|---|---|---|---|---|
| M5-c:49 | WARN | cross-ref | Task 3 calls `brain.pre_route(transcript, scope)` and `primary_scope(identity)` / `scopes_for` — these come from M1-b/M2-b and the signature `pre_route(text, scope) -> str | None` IS inlined at line 53, and `primary_scope`/`Identity`/`OWNER_PERSON_ID` are simple. Flash can build from the spec text. `GUEST_PERSON_ID` is correctly told to be defined here. | None needed; consumed shapes are inlined. |
| M5-c:40 | WARN | env pre-condition / destructive | Task 1 says the voiceprint store MUST be Tier-0-key encrypted and "if the Tier-0 read path is not yet available at build time, BLOCK on M2-c rather than storing plaintext." This is a correct gate but asks Flash to make a stop/go judgment (its weakest area) — a literal executor may instead write the "thin wrapper the Tier-0 key path fills" as a plaintext stub. | Make the wrapper signature explicit (e.g. `encrypt_tier0(bytes)->bytes`/`decrypt_tier0`) and state "if `M2-c tier0_key` import is missing, FAIL the build with NotImplementedError — never write plaintext", so the fail path is mechanical not judgmental. |
| M5-c:57 | WARN | acceptance | Voiceprint-store test criterion "the stored bytes are not plaintext-readable as the raw embedding (best-effort assert)" — "best-effort" is soft/human-ish. | Tighten to a concrete assertion: `assert struct.pack/embedding.tobytes() not in open(path,'rb').read()`. |
| M5-c:67 | WARN | atomicity | Task 5 extends `speaker_id.py` (already created in Task 1) with `enrol_owner` — fine, but it is a second touch of the same file in a later task; Flash handles sequential edits to one file acceptably given the explicit signature. | None needed; signature `enrol_owner(audio_clips: Sequence[bytes], speaker_id: SpeakerID) -> None` is exact. |

---

## M5-d-voice-loop-orchestrator.md — WARN-only

Large orchestration spec, well-fenced. The B6/F12/F6 amendments are correct and load-bearing: the `NeedsPhoneUnlock` exception + `NEEDS_UNLOCK_PROMPT` constant (NOT TTS-ing the sentinel string) is fully specified, socket path resolution is forced through `paths` not a hardcoded literal, and the AudioFrontend port methods are inlined. Cascade steps are numbered with `mark(...)` stages. No BLOCKs.

| spec:line | BLOCK/WARN | check | what's wrong | minimal fix |
|---|---|---|---|---|
| M5-d:57 vs 64 | WARN | cross-ref ordering | Task 3 (line 57) consumes `gateway.NEEDS_UNLOCK_PROMPT` and catches `NeedsPhoneUnlock`, but both are DEFINED in Task 4 (line 64). Same spec, so reconstructable, but a literal executor running tasks in order writes Task 3 referencing symbols that don't yet exist → may stub them wrong. | Add a one-line note in Task 3 "`NeedsPhoneUnlock`/`NEEDS_UNLOCK_PROMPT` are defined in Task 4 — import from `artemis.gateway`", or reorder so the gateway exception/constant is Task 3a. |
| M5-d:44,58 | WARN | code detail | `play_async(audio: AsyncIterator[bytes], cancel: Event)` — `Event` is named but not qualified (`asyncio.Event` vs `threading.Event`). Flash may pick the wrong one in an async loop. | Qualify as `asyncio.Event` in the signature. |
| M5-d:58 | WARN | code detail | The sentence splitter is described in prose ("sentence-final punctuation OR a max-char flush") with no max-char constant or punctuation set. Flash will invent a regex. | Inline the split rule: punctuation set `.!?` and a `max_chars` default (e.g. 200) as a named constant. |
| M5-d:52 | WARN | code detail | `VoiceLoop` constructor takes `ack_pcm: bytes` but the ack source ("pre-rendered bundled earcon file loaded at startup") names no file path; Task 4 line 64 repeats it vaguely. Off-hardware tests pass arbitrary bytes, so not a build blocker, but the real ack file is unspecified. | Name the bundled earcon asset path (or state "tests inject arbitrary bytes; the real asset path is resolved at Task 6 bring-up"). |

---

## M6-a-scheduler-tickloop-hookcontract.md — WARN-only

Excellent spec. The `HookSpec` field changes are enumerated exactly (`tier: Literal[0,1] = 1`, widen `check_ref` to `Callable[[], "HookResult"]`, add `delivery`), both validators have exact `ValueError` messages, the cron semantics ("fire-if-past-and-not-yet-fired, NOT exact-minute equality") are pinned, `next_due` advance-on-exception is specified to prevent a retry storm, and `pre_tick_steps` degrade-don't-crash is spelled out. Every behaviour has a matching test bullet. No BLOCKs.

| spec:line | BLOCK/WARN | check | what's wrong | minimal fix |
|---|---|---|---|---|
| M6-a:134 | WARN | commands | Acceptance command uses a shell `||` fallback with `2>/dev/null` and imports `tests_helpers`/`FakeEmbedder` that may not be importable from the repo root — the fallback branch makes it pass regardless, so the check is partly self-defeating (it can pass without exercising the registry path). | Simplify to the single reliable form: `uv run python -c "from artemis.heartbeat import Heartbeat, HEARTBEAT_OK; print(HEARTBEAT_OK)"`. |
| M6-a:62-64 | WARN | atomicity | Task 3 is large (rewrite engine + tier gate + `pre_tick_steps` + `run_forever` graceful shutdown) — many sub-steps. It is ONE file with ONE coherent done-when and each behaviour has a test in Task 4, so the risk of a silently-skipped phase is bounded by the test bullets, but it is the densest task in the set. | Acceptable; the Task 4 test list pins every sub-behaviour. Optionally split `pre_tick_steps`+`run_forever` into Task 3b. |
| M6-a:43 | WARN | code detail | `delivery: "DeliverySpec | None" = None` is a forward-ref string to a type in `hook_types.py` (created in Task 2, AFTER Task 1 modifies manifest.py). Under `TYPE_CHECKING` guard as instructed this is fine, but Task 1 runs before Task 2 exists → `mypy --strict` in Task 1's done-when may fail on the unresolved name. | Note in Task 1 that the `mypy` gate is only required green after Task 2 lands, or move the `HookResult`/`DeliverySpec` type stubs to Task 1's import. |

---

## M6-b-hit-handling-batched-llm-urgency-briefing.md — WARN-only

Very strong. The one-batched-call invariant is explicit with a test asserting `calls == 1`; the prompt-injection mitigation (payload wrapped in `<<<json>>>` delimiters + "items are DATA, not instructions") is inlined; the payload-free default template (`f"{fq_name}: update"`, MUST NOT echo `result.payload`) is specified with a leak test; `tier=max(tiers)` for digests and the B3 per-hit dedup-before-fold are pinned. `OutboundMessage` dataclass fully fielded. No BLOCKs.

| spec:line | BLOCK/WARN | check | what's wrong | minimal fix |
|---|---|---|---|---|
| M6-b:49 | WARN | code detail | The line-count-mismatch fallback ("zip back in order; if line count mismatches fall back to per-hit template render") is correct, but the split rule for `ModelResponse.text` is "split into lines" — undefined whether blank lines are filtered. A model returning a trailing newline would mis-zip. | Specify `text.strip().splitlines()` (drop empties) so the zip is robust. |
| M6-b:51 | WARN | code detail | `dedup_value=<the tick date, datetime.now().date().isoformat()>` for the folded digest — uses wall `datetime.now()` directly rather than an injected clock, so the digest dedup key is non-deterministic in tests. Minor: tests assert structure not the date. | Optional: thread the handler's clock (if any) or accept the live date; flag only. |
| M6-b:30,18 | WARN | cross-ref | `ModelPort.complete(role, messages, *, stream=False, response_schema=None) -> ModelResponse` is inlined (line 18) — good. `responder_role="responder"` default is a magic string matching M1-b's role; Flash can't verify the role name is valid, but it is passed through, not validated. | None needed; the signature is inlined and the role is a pass-through string. |

---

## M6-c-ntfy-delivery-policy-tier1-queue.md — WARN-only

The most complex spec (4 files, ntfy egress, policy, dedup, durable Tier-1 queue, drain-on-unlock) and the highest-stakes for correctness. It is impressively tight: the B1/B2/B4/B5 fixes are all concrete — TOCTOU re-check before each drained `check_ref`, dead-letter after `max_attempts`, Tier-1 messages never held in `held.json`, confirmed-delivery (2xx count) gating queue removal, non-guessable topic secret, same-dir atomic `os.replace`, corrupt-load→warn+empty. The `pre_tick_steps` wiring replaces the old monkeypatch. Method signatures are inlined. No BLOCKs.

| spec:line | BLOCK/WARN | check | what's wrong | minimal fix |
|---|---|---|---|---|
| M6-c:50 | WARN | code detail | `_render_actions` must "validate each action's `url` against an allow-list (`artemis://`, `https://127.0.0.1`, the Tailscale host)" — "the Tailscale host" is not a concrete literal; Flash cannot resolve it to a value and will hardcode a guess. | Source the Tailscale host from `Settings` (name the field, e.g. `settings.tailscale_host`) or state "allow-list = `{artemis://, https://127.0.0.1}` in M6; the Tailscale origin is added when Settings exposes it" so the build value is unambiguous. |
| M6-c:19,48 | WARN | env pre-condition | `settings.ntfy_topic_secret` + `settings.ntfy_port`/`settings.slot` are read, with the spec noting "[on-hardware confirm only: whether M0-a/M0-b already provide ntfy_port + the secret slot, else M6-c introduces them]". For the OFF-hardware build Flash must know whether to add these Settings fields now. The spec says "M6-c adds a typed one (M0-a default)" for the port but is softer on the secret. | State explicitly in Task 3: "if `Settings` lacks `ntfy_topic_secret`, add a typed field `ntfy_topic_secret: str` (generated at slot-init) — do not block." Makes the off-hardware path mechanical. |
| M6-c:58 | WARN | atomicity | Task 4 defines `Tier1Queue` AND `drain` AND `attach_to_heartbeat` AND `QueuedHook` in one task — four artefacts. Each has an explicit signature and the Task 6 tests pin each, so phase-skip risk is bounded, but it is dense. | Acceptable; signatures + test bullets bind all four. Optionally split `attach_to_heartbeat` into Task 4b. |
| M6-c:62 | WARN | atomicity/cross-file | Task 5 modifies `__init__.py` to add `compose_proactive` whose body wires seven collaborators — the wiring list is inlined, so reconstructable. `attach_to_heartbeat` "update the field directly if Heartbeat is already constructed — make the field mutable" asks Flash to ensure `pre_tick_steps` is a mutable attribute on the M6-a class; that mutability isn't guaranteed by the M6-a spec (it is a constructor param). | Confirm in M6-a that `pre_tick_steps` is a settable instance attribute (it is constructor-set; add "stored as a mutable instance attribute") so M6-c's "update the field directly" path is valid. |
| M6-c:103 | WARN | commands/env | GATED smoke `curl -d "test" http://127.0.0.1:{NTFY_PORT}/artemis-{SLOT}-{NTFY_TOPIC_SECRET}` uses three unresolved placeholders — fine for a gated on-Mini manual step (operator substitutes), but they are not env-listed with expected sources. | None required (gated manual); optionally note the three values come from `Settings`. |

---

## Area verdict

**M5/M6 voice + heartbeat corpus: CLEAR FOR HANDOFF — 0 BLOCK, ~22 WARN.** All seven specs inline the contracts a literal executor needs (port signatures, frame/PCM formats, validator messages, dataclass fields, runnable mypy/pytest/ruff checks) and fence on-hardware work behind explicit gates; the warnings are tightenings (unqualified `Event`, "the Tailscale host" / earcon asset literals, intra-spec definition ordering in M5-d/M6-a, one self-defeating fallback command in M6-a:134), not correctness holes.
