# Spec-lint pass — M4 memory engine (2026-06-11)

Executor: DeepSeek V4-Flash (literal; fills gaps with plausible-wrong code; silently skips later phases).
Checklist applied per task. BLOCK = stop handoff; WARN = soft flag.

---

## M4-a-store-schema-bitemporal-repo.md

**Verdict: BLOCK**

The substrate is detailed, but the load-bearing return types `FactRow` and `EpisodeRow` are named as method return types in Task 4 and consumed by M4-c / M4-d-2 with EXACT field access — yet their field lists are never given. Flash will invent a `FactRow` shape, and the downstream specs (which read `row.salience`, `row.last_access`, `row.confidence`, `row.fact_key`, `row.relation`, `row.object`, `row.subject_entity_id`, `row.valid_from`) will then fail to type-check or read wrong columns. This is the single highest-severity defect in the area.

| spec:line | BLOCK/WARN | check | what's wrong | minimal fix |
|-----------|------------|-------|--------------|-------------|
| M4-a:75 | BLOCK | CODE DETAIL | `FactRow` declared only in prose ("a frozen dataclass mirroring the row (incl. fact_key, decay inputs, provenance)") — no exact field list. M4-c Task 1/2 and M4-d-2 Task 1 access specific named fields; Flash will guess the shape and field names. | Inline the exact frozen dataclass: `@dataclass(frozen=True) class FactRow: fact_id:str; fact_key:str; person_id:str; subject:str; relation:str; object:str; confidence:float; valid_from:str; valid_to:str; tx_from:str; tx_to:str; source_turn_id:str|None; extracted_at:str|None; extractor_model:str|None; salience:float; access_count:int; last_access:str|None; keywords:str|None; contextual_description:str|None; linked_ids:str|None` (M4-d-2 adds `subject_entity_id`). |
| M4-a:81 | BLOCK | CODE DETAIL | `EpisodeRow` is the return type of `read_episodes` but is never defined anywhere. | Inline a frozen `EpisodeRow` dataclass mirroring the `episodes` columns (`episode_id, person_id, turn_id, role, text, valid_from, valid_to, tx_from, tx_to, created_at`). |
| M4-a:69,72 | BLOCK | CODE DETAIL | `compute_fact_key(self, subject, relation, object_)` excludes `person_id` from its args but the assumption (line 26) says the hash includes `person_id`. The repo holds `self.person_id`, but the prose at line 26 says `sha256(person_id, subject, relation[,object])` — the exact serialization (separator, encoding, ordering) is unspecified. Two specs depend on key STABILITY; Flash picking `f"{a}{b}"` vs `"|".join` vs json gives a different-but-internally-consistent key — only a problem if anything else recomputes it. It does not, so this is the lesser risk, but the hash-input format is still underspecified for a value asserted by golden tests. | Specify exact input string, e.g. `sha256("\x1f".join([person_id, subject, relation]).encode())` for SINGLE and `+[object_]` for MULTI. |
| M4-a:77 | WARN | CODE DETAIL | `semantic_candidates` returns `(fact_id, distance)` but the SQL join ("restricted (joined) to currently tx-open + valid rows") is described in prose only — Flash must author a vec0 KNN + bitemporal join with no snippet. | Inline the KNN SQL skeleton (the `vec0` MATCH + join to `facts` on `fact_id` with the `tx_to=SENTINEL AND valid_to=SENTINEL` predicate). |
| M4-a:48 | WARN | TASK ATOMICITY | Task 1 (GATED) bundles (a) confirm binding + finalise load calls, (b) keyed open + vec load + KNN + cipher_memory_security + wrong-key check, (c) record in handoff — 3 sub-phases. It is correctly GATED/on-hardware so acceptable, but flag for splitting if it stalls. | Optionally split confirm-binding from the round-trip proof. |
| M4-a:60,66 | WARN | ENV PRE-CONDITIONS | "fresh in-memory connection (sqlite-vec loaded)" / fallback path — the fallback open helper that loads sqlite-vec on a plain (non-keyed) sqlite3 connection is described but no signature is given for how the test obtains it. | Name the fallback helper or point at the M4-a fixture that produces it. |
| M4-a:121-122 | WARN | COMMANDS | `uv add "sqlite-vec==<pinned-pre-v1-version>"` and `(or sqlcipher3-binary per Task 1)` carry placeholders/either-or — the exact pin is resolved only at GATED Task 1. Acceptable (on-hardware), but the version literal is non-runnable as written off-hardware. | Note that the pin is filled at Task 1; off-hardware uses the unpinned `uv add sqlite-vec`. |
| M4-a:57 | WARN | CROSS-REFERENCES | `distance_metric=cosine` "Confirm supported in the pinned sqlite-vec version at Task 1" — a runtime feature assumption gated correctly, but if unsupported the schema DDL is wrong and Flash has no fallback instruction. | Add a one-line fallback (L2 distance + `cosine=1−d/2` note) if `distance_metric` is unsupported. |

---

## M4-b-write-path-audn-extraction.md

**Verdict: WARN-only**

Cleanly structured; consumes M4-a symbols that are named. The main risk is inherited from M4-a's undefined `FactRow`/`get_fact` shape (tracked under M4-a). Schemas are given as JSON-schema shapes (acceptable). Two soft flags below.

| spec:line | BLOCK/WARN | check | what's wrong | minimal fix |
|-----------|------------|-------|--------------|-------------|
| M4-b:21,44,46 | WARN | CODE DETAIL | `EXTRACTION_SCHEMA` / `DECISION_SCHEMA` are described as "a JSON-schema for {...}" in prose, not given as a literal dict. Flash will author a plausible JSON-schema; Outlines is strict about schema shape, so a malformed schema fails only at on-hardware Task 6. Off-hardware tests use Fakes so it passes CI while being wrong. | Inline the literal `EXTRACTION_SCHEMA = {...}` and `DECISION_SCHEMA = {...}` dicts (the `type/properties/required` JSON-schema). |
| M4-b:52 | WARN | CODE DETAIL | `embedder.embed([...])[0]` assumes `EmbeddingModel.embed` takes a list and returns a list; M4-a:85 uses `embedder.embed` on a single string. The two specs assume different `embed` arities. Both consume the M0-d `EmbeddingModel` but neither inlines its signature. | Inline the M0-d `EmbeddingModel.embed` signature once (e.g. `embed(texts: Sequence[str]) -> list[Vector]`) and make both specs match. |
| M4-b:24,62 | WARN | TASK ATOMICITY | Task 3 builds BOTH `MemoryWritePath.process_turn` (the 4-step pipeline) AND `MemoryWriteQueue` (enqueue/run_worker/drain) — two cohesive units in one task. Within tolerance but >3 sub-steps. | Acceptable; flag only if it stalls — could split queue into its own task. |
| M4-b:44 | WARN | ACCEPTANCE | "no inferences" extraction-prompt quality is human-judgment; only the GATED Task 6 eval checks it. Fine (gated), noted. | none |

---

## M4-c-recall-autoinject-decay-owner.md

**Verdict: BLOCK**

Self-admittedly TWO logical phases (its own split-rule comment, line 16: "TWO logical phases ... At the 2-phase / borderline-files limit"). Per the project split rule (>2 logical phases OR >3 files → split), this spec is at/over the boundary AND modifies 4 files + creates 3 (gateway.py, brain.py, store.py, __init__.py modified; decay.py, owner.py, test created = 7 files). The bigger executable defect: Task 4 modifies `Brain.respond` and `compose_brain` by PROSE description of where to inject, against a `brain.py`/`gateway.py` whose current message-building shape is not inlined — Flash cannot reliably find "the local/free-form path" without the M1-b code in front of it.

| spec:line | BLOCK/WARN | check | what's wrong | minimal fix |
|-----------|------------|-------|--------------|-------------|
| M4-c:59-62 | BLOCK | CROSS-REFERENCES | Task 4 says inject "BEFORE the responder model call on the local/free-form path" and "prepend `{"role":"system",...}` to the responder messages" — but the M1-b `Brain.respond` body (how `messages` is built, what distinguishes the free-form vs tool path) is described only in the Assumptions prose (line 20), not inlined as code. Flash cannot locate the exact insertion point and will inject in the wrong place or both paths. | Inline the relevant ~10 lines of M1-b `Brain.respond` showing the `messages=[...]` construction and the branch that selects free-form vs deterministic-tool path, with the exact line to prepend before. |
| M4-c:62 | BLOCK | CROSS-REFERENCES | `compose_brain` update relies on knowing where M1-c built the `ToolRegistry`/owner-store and the M2 `KeyProvider`/scope unlocked-check — none inlined. "when an unlocked owner keyed DB is available (from the M2 KeyProvider/scope)" is a prose pointer to code Flash must go read. | Inline the `compose_brain` signature + the owner-unlocked condition expression (e.g. `key_provider.dek_for_scope(scope)` succeeding) that gates store construction. |
| M4-c:16 | BLOCK | SCOPE & SIZE | Spec declares itself TWO logical phases and touches 7 files (4 modify + 3 create) — over the project split rule (>2 phases or >3 files → split). The spec even offers the sub-split (M4-c1 inject+decay+brain / M4-c2 owner surface). | Split into M4-c1 (decay.py + store.py inject/recall + brain.py/gateway.py wiring) and M4-c2 (owner.py surface + its tests). |
| M4-c:48 | WARN | CODE DETAIL | `inject_context` token estimate = "chars/4 heuristic over the subject relation object rendering" — adequately concrete. `recall` re-rank "cosine from the candidate distance" assumes the `1 − distance` conversion stated in M4-a:57; restate inline so Flash converts consistently. | Add `cosine = 1 - distance` one-liner in Task 2. |
| M4-c:21,59 | WARN | CROSS-REFERENCES | "the scope is an owner scope" appears as a runtime predicate in Task 4 but no function/attribute is named for the owner-scope check. | Name the predicate (e.g. `scope.is_owner` or the M2-b helper). |
| M4-c:46 | WARN | CODE DETAIL | `recall_multiplier` clamp "to ~[RECALL_DAMP_MIN, RECALL_BOOST_MAX]" — the "~" plus an additive formula `α·exp+β·access+γ·cosine` whose natural range is not [0.3,1.5]; the clamp maps an unbounded sum into the band but the mapping (raw clamp vs normalise) is ambiguous. | Specify exactly: `return max(RECALL_DAMP_MIN, min(RECALL_BOOST_MAX, I))`. |

---

## M4-d-1-entity-data-layer.md

**Verdict: WARN-only**

The strongest spec of the five: real DDL snippets inline, exact method signatures, explicit normalize/guard logic, full test list, a fixed AC contradiction (line 172) already addressed. Within the 3-file limit. Only soft flags.

| spec:line | BLOCK/WARN | check | what's wrong | minimal fix |
|-----------|------------|-------|--------------|-------------|
| M4-d-1:91 | WARN | CODE DETAIL | `resolve_or_create_entity` step (3) derives `entity_id` via `person_fact_key(...)` for PERSON — but `person_fact_key` for a name-only person returns a FRESH uuid each call; idempotency relies on the alias seeded in the SAME call being hit on the NEXT call's step (2). This is correct as written but subtle; if Flash reorders create-before-alias-seed the second call mints a new id. The ordering is stated ("then seed an alias") — low risk, flag only. | Add an explicit comment: "alias seed MUST occur in the same create branch so step (2) resolves it next time." |
| M4-d-1:24,72 | WARN | ENV PRE-CONDITIONS | Stale-DB guard is well specified, but the assumption "M4-a's facts DDL is a single CREATE TABLE IF NOT EXISTS string" (line 24) is a precondition Flash must verify by reading M4-a's emitted `schema.py`. Since M4-a is a prerequisite build, the file exists — acceptable. | none (note dependency). |
| M4-d-1:174 | WARN | ACCEPTANCE | AC line 174 reinstates the `uv run python -c` import+`person_fact_key` check that line 172 just deleted as unable to pass without sqlite-vec — but THIS one only imports `from artemis.memory` and calls `person_fact_key` (no `create_schema`, no DB), so it WILL pass. The two AC lines look contradictory at a glance though. | Add a half-line clarifying 174 is import-only (no schema build) so it is distinct from the deleted 172 check. |

---

## M4-d-2-writepath-resolve-tool.md

**Verdict: WARN-only**

Excellent executable detail — inline code blocks for the resolution try/except, the bitemporal `facts_for_entity` SQL, the Pydantic models, the manifest construction, and a 4-wave plan. Consumes M1-a `ModuleManifest`/`ToolSpec`/`ActionRisk` whose exact shape IS inlined (line 22). Soft flags only.

| spec:line | BLOCK/WARN | check | what's wrong | minimal fix |
|-----------|------------|-------|--------------|-------------|
| M4-d-2:71 | WARN | CODE DETAIL | The try/except snippet calls `logger.warning(...)` but no `logger` is defined/imported in `write_path.py` per any spec; M4-b Task 3 mentions "logged" but never defines a module logger. Flash may invent `import logging; logger = logging.getLogger(...)` (fine) or call an undefined name (NameError). | Add one line: "module-level `logger = logging.getLogger(__name__)` at top of write_path.py (add in M4-b or here)." |
| M4-d-2:86 | WARN | CODE DETAIL | `_bound_resolve_entity` builds `BitemporalRepository` + `EntityRepository` "from the store's keyed connection + owner person_id" — relies on the M4-a F11 accessors `store.repository`/`store.conn`/`store.person_id` (M4-a:84). Those ARE defined in M4-a, but this task doesn't name which accessor yields `conn` vs the repo; Flash must cross-read M4-a:84. | Name them: `repo = store.repository; conn = store.conn; pid = store.person_id; entity_repo = EntityRepository(conn, pid)`. |
| M4-d-2:60 | WARN | ENV PRE-CONDITIONS | AC requires `EXPLAIN QUERY PLAN ... shows USING INDEX idx_facts_subject_entity`. On the plain-sqlite fallback the planner may pick a different plan than keyed SQLCipher; the assertion could be flaky off-hardware. | Soften to "uses an index on subject_entity_id" or gate the EXPLAIN check to the real-binding path. |
| M4-d-2:100 | WARN | CODE DETAIL | Test builds `ToolRegistry(FakeEmbedder())` — assumes `ToolRegistry.__init__` takes an embedder; not inlined from M1-a (line 22 lists methods, not the constructor). | Inline the `ToolRegistry` constructor signature in the M1-a assumption block. |

---

## Area verdict

**M4 area: BLOCK** — 2 specs block (M4-a on undefined `FactRow`/`EpisodeRow` return types consumed downstream; M4-c on prose-only Brain/compose_brain injection points + a self-declared 2-phase/7-file over-scope split). M4-b/M4-d-1/M4-d-2 are WARN-only. Fixing M4-a's `FactRow`/`EpisodeRow` shapes is the highest-leverage change — it de-risks M4-b, M4-c, and M4-d-2 simultaneously.
