# Spec-Lint — Final Pre-Handoff Pass — M1/M2 (2026-06-11)

Executor: DeepSeek V4-Flash (literal). Reviewer focus: defects that make Flash build the WRONG thing.
Checklist applied per task. BLOCK = stop handoff; WARN = soft flag.

---

## M1-a-manifest-registry.md — WARN-only

Verdict: **WARN-only** (0 BLOCK, 4 WARN). Strong spec: every model has exact field/type/default, every callable has a runnable check, fq-id seam (`module.tool`) is inlined repeatedly. The `_execute`-twin behaviour (Task 3, D1) is the one place a literal executor could under-build, but the shape is specified and a test asserts the negative (`retrieve_tools` returns no `_execute` ids), so it is recoverable, not a BLOCK.

| spec:line | BLOCK/WARN | check | what's wrong | minimal fix |
|---|---|---|---|---|
| M1-a:51 | WARN | CODE DETAIL | Task 2 says "build a minimal `Chunk` from the stored id + metadata `text`" but `Chunk`/`RetrievedChunk` field shape is not inlined — relies on M0-d port. Flash will guess fields. | Inline the `RetrievedChunk`/`Chunk` constructor signature (fields + types) one line, or cite the exact M0-d dataclass def. |
| M1-a:54 | WARN | TASK ATOMICITY | Task 3 has 6 sub-methods (register, retrieve_tools, retrieve_tools_scored, get_tool, export_index, manifests) + the `_execute`-twin dict + `default_registry` — >3 sub-steps, one task. | Acceptable as atomic registry unit but flag: consider splitting `export_index` (I/O) from the in-memory methods if review wants leaner. |
| M1-a:54 | WARN | CROSS-REFERENCES | `_execute`-twin is "the dispatch-only twin used by `ActionStagingService.approve()`" (GATE-a) — consumer not in M1; Flash cannot verify the contract here. Shape IS inlined (dict of `{fq_id}_execute` callables), so buildable. | None required; leave as WARN — note the twin has no M1 test of its dispatch, only its exclusion. |
| M1-a:64 | WARN | ACCEPTANCE | `FakeEmbedder` "maps each input string to a small fixed-dimension vector via a hash-based bag-of-words so semantically-close strings score higher" — the "score higher" property is asserted by the retrieve test but the embedder construction is prose, not a given algorithm. Two different Flash builds could produce embedders where "what time is it" is NOT closest to the time tool, failing Task 5's retrieve assertion non-deterministically. | Specify the FakeEmbedder algorithm concretely (e.g. "token-overlap count over lowercased whitespace-split words, padded to dim N") so the retrieve assertion is deterministic. |

---

## M1-b-router-brain.md — WARN-only

Verdict: **WARN-only** (0 BLOCK, 5 WARN). Seam 1 async contract, the three router paths, the degrade-don't-crash behaviour, and the `response_format` mapping are all inlined exactly. Genuinely 2 phases (Router + Brain) but justified-and-flagged in the split-rule comment. The risk areas are render-template vagueness and threshold tuning, both recoverable.

| spec:line | BLOCK/WARN | check | what's wrong | minimal fix |
|---|---|---|---|---|
| M1-b:49 | WARN | CODE DETAIL | Tool path: "render a final answer string from the return model ... use a template `f"{return_model}"`-style deterministic render". `f"{return_model}"` renders a Pydantic repr, not the time — but Task 4/AC require `text` to *contain the fixed time*. Flash may emit `tz=... iso=...` repr; the e2e (M1-d) asserts an ISO regex which the repr DOES satisfy. Borderline. | State the exact render: e.g. `text = return_model.model_dump_json()` (contains the iso field) — guarantees the ISO regex matches. |
| M1-b:46,52 | WARN | TASK ATOMICITY | Task 2 defines `respond`, `respond_stream`, AND `pre_route` (three public async/sync methods + the BrainResponse dataclass) in one task — >3 sub-steps. | Acceptable (one class), flag only; the cross-milestone back-fills (respond_stream/pre_route) could be a separate task if leaner wanted. |
| M1-b:22 | WARN | ACCEPTANCE | Router thresholds (0.6/0.35/0.15) are "drafted defaults, GATED for empirical confirm at Task 5". Off-hardware Task 4 asserts "what time is it" routes to deterministic/local and nonsense to escalate — but whether the FakeEmbedder's cosine clears 0.6 vs 0.35 depends on the (under-specified, see M1-a:64) FakeEmbedder. | Tie to the M1-a FakeEmbedder fix; ensure the test's fake produces a top score ≥0.35 for the time query and <0.15 for nonsense, or set test-only thresholds. |
| M1-b:56 | WARN | ENV PRE-CONDITIONS | Task 3 `OpenAIEmbeddingModel.dimension` "add `embedding_dimension: int` to Settings/roles.toml — use `1024`". This MODIFIES Settings/roles.toml (M0-a/M0-c file) but those files are NOT in the Files-to-Change table (only `adapters/model_adapters.py` etc.). Flash either skips the Settings edit (then `dimension` has no source) or edits an unlisted file (scope-lock violation). | Add the Settings/roles.toml file to Task 3's file list + Files-to-Change table, with the exact key `embedding_dimension: int = 1024`. |
| M1-b:104 | WARN | COMMANDS | Network table line still lists `uv add outlines` as a package install, contradicting the body's repeated instruction (lines 20, 56, 85) to NOT `uv add outlines` in M1-b. A literal executor reading the Network table may run it. | Delete `uv add outlines` from the Network table (line 104) — keep only the OpenAI client install. |

---

## M1-c-gateway-surfaces.md — BLOCK

Verdict: **BLOCK** (1 BLOCK, 4 WARN). One genuine cross-spec contradiction: Task 1 mandates lazy embedding in `ToolRegistry.register()`, but that method was fully specified in M1-a as embedding AT registration time, and `registry.py` is NOT in M1-c's Files-to-Change. Flash cannot satisfy "register() must NOT embed" without editing an unlisted file or contradicting M1-a.

| spec:line | BLOCK/WARN | check | what's wrong | minimal fix |
|---|---|---|---|---|
| M1-c:39 | **BLOCK** | CROSS-REFERENCES / FILE EXPLICITNESS | Task 1 (B7) requires `ToolRegistry.register()` to be LAZY (embed on first `retrieve_tools*`, store pending list). But M1-a Task 3 (line 54) specifies `register()` embeds immediately, and `src/artemis/registry/registry.py` is NOT in M1-c's Files-to-Change (only gateway/api/cli/main/test). Flash will either (a) skip the lazy change (compose_brain then hits network at construction → AC line 39 fails), or (b) edit registry.py outside scope. Contradicts M1-a as-written. | Move the lazy-embedding requirement INTO M1-a Task 3 (make `register()` lazy there) and have M1-c merely rely on it; OR add `src/artemis/registry/registry.py` to M1-c Files-to-Change with the exact lazy-register diff. Resolve the M1-a/M1-c contradiction before handoff. |
| M1-c:43 | WARN | TASK ATOMICITY / COMMANDS | Task 3 offers TWO mutually exclusive implementations (console-script entry in pyproject vs `python -m artemis.cli`) then says "PREFER the `python -m` form". A literal executor may implement both or pick the dis-preferred one. | Delete the pyproject/`[project.scripts]` alternative entirely; specify only `if __name__ == "__main__": main()` + `python -m artemis.cli`. |
| M1-c:39 | WARN | CODE DETAIL | `compose_brain` "register modules (call `artemis.tools.time_tool.manifest()` ... wrap in try/except ImportError)". The exact import statement + except behaviour ("registers nothing") is prose; fine, but the `Settings`/`PersonId` types it consumes are assumed from M0. | Inline the `compose_brain(settings: Settings) -> Brain` signature (already mostly present) and the one-line import: `from artemis.tools.time_tool import manifest`. |
| M1-c:47 | WARN | CODE DETAIL | Task 5 `AskResponse(**r as dict)` — `r` is a `BrainResponse` frozen dataclass; `**r` is not valid Python (needs `dataclasses.asdict(r)` or `**vars(r)`). Flash may emit `**r` literally → runtime error. | Write the exact conversion: `AskResponse(**dataclasses.asdict(r))`. |
| M1-c:20,26 | WARN | ENV PRE-CONDITIONS | Soft dependency on M1-d's `manifest()` factory; composition tolerates absence via try/except. Good. But the test (Task 5) uses a `FakeBrain` and never exercises the real `compose_brain` network-free assertion at AC line 39 beyond "returns a Brain" — the "no network at construction" claim is asserted only by prose, not a runnable check. | Add a runnable check: construct `compose_brain(get_settings())` with no mlx server running and assert it returns without raising (the AC mentions it; make it a test). |

---

## M1-d-time-tool-heartbeat.md — WARN-only

Verdict: **WARN-only** (0 BLOCK, 3 WARN). Excellent spec — exact Pydantic models, exact manifest factory call with every field, the F13 explicit-regex acceptance form, bare-name Seam 2 guard repeated. The e2e test (Task 4) inlines the FakeModelPort return shape (`{"tz": null}`).

| spec:line | BLOCK/WARN | check | what's wrong | minimal fix |
|---|---|---|---|---|
| M1-d:50 | WARN | CROSS-REFERENCES | Task 3 instructs "document this one-line correspondence in `manifest.py`" (the DataScope→storage-scope mapping) — but `manifest.py` is an M1-a file NOT in M1-d's Files-to-Change. Flash editing it = scope-lock violation; skipping = the documented mapping never lands. | Either move the mapping-doc instruction to M1-a, or add it as a docstring in `time_tool.py` (an M1-d file). Drop the cross-file edit. |
| M1-d:54 | WARN | ACCEPTANCE | Task 4 reuses `FakeEmbedder` + `FakeModelPort` "from the M1-a/M1-b test patterns" — these are in other test files, not importable shared fixtures. Flash may re-implement divergently (and re-trip the M1-a:64 determinism gap). | Specify a shared `tests/conftest.py` or `tests/fakes.py` home for FakeEmbedder/FakeModelPort, referenced by M1-a/b/d; or inline the algorithm here too. |
| M1-d:51 | WARN | ACCEPTANCE | Heartbeat test: "the loop ran exactly 3 ticks (assert via a counter/spy or the logger)" — "or" leaves the assertion mechanism to the executor; logger-based and counter-based give different test code. | Pick one: inject a counting callback or assert on `tick` call-count via a spy; state it. |

---

## M2-a-key-broker.md — WARN-only

Verdict: **WARN-only** (0 BLOCK, 6 WARN). Large but genuinely one atomic component (the broker package), flagged. Swift signatures are given; the off-hardware/on-hardware gate split is clean; replay/counter/tamper tests are concrete. The size and the SE-shim plumbing are the main risks, not missing detail.

| spec:line | BLOCK/WARN | check | what's wrong | minimal fix |
|---|---|---|---|---|
| M2-a:75 | WARN | CODE DETAIL | Task 7: `provision-scope` must work off-hardware "using a mock/off-hardware SE shim when `ARTEMIS_SE_AVAILABLE` is unset" — but the SE shim's wrap behaviour (identity? fixed bytes?) and HOW `SecureEnclaveKeyStore` selects shim-vs-real is not specified. Task 3 separately mentions a "mock keyStore returning identity-ciphertext". Flash will invent the shim seam. | Specify the shim seam: e.g. an env-gated `KeyStoreBackend` protocol with `RealSEKeyStore` and `ShimKeyStore` (wrap = prepend a marker; unwrap = strip), selected by `ARTEMIS_SE_AVAILABLE`. Name the file it lives in. |
| M2-a:73 | WARN | TASK ATOMICITY | Task 6 builds BOTH Protocol.swift AND IPCServer.swift (wire types + socket server + peer-cred + code-sign check + dispatch) — 2 files, ≥4 sub-steps. | Acceptable (tightly coupled) but flag; could split Protocol.swift (pure Codable types) from IPCServer.swift. |
| M2-a:81 | WARN | CODE DETAIL | Task 10 VolumeMount off-hardware is "a verifiable no-op shim recording mount/unmount calls" but the real attach mechanism is explicitly undecided ("hdiutil sparsebundle vs `diskutil apfs`"). Flash building the shim is fine; flag that the real `mount()` body is a stubbed `TODO` gated to Task 9 with no concrete command. | Acceptable (gated), but state the off-hardware shim records calls in an injectable recorder so Task 10's test can assert it — the test (line 81) needs that hook named. |
| M2-a:46 | WARN | NEW FILES | Creates `docs/technical/protocol/broker-ipc.md` and the whole `swift/ArtemisBroker/` tree — parent dirs (`swift/`, `docs/technical/protocol/`) creation is implicit. | Confirm `swift/` and `docs/technical/protocol/` are created (M0 likely makes `docs/technical/`; `swift/` is new) — add "create parent dirs" note. |
| M2-a:73 | WARN | COMMANDS | Code-signing check has a dev bypass `ARTEMIS_BROKER_SKIP_CODESIGN=1`, but Task 6's "done when" test ("a request from a simulated wrong-uid peer is rejected") doesn't state whether codesign is on/off during the off-hardware test. Ambiguous → Flash may write a test that needs a real signed peer. | State: off-hardware tests run with `ARTEMIS_BROKER_SKIP_CODESIGN=1`; only peer-uid is asserted. |
| M2-a:49 | WARN | ENV PRE-CONDITIONS | `macOS platform .macOS(.v26) (or the highest the toolchain accepts — document if .v15/.v14 is the SDK ceiling)` — leaves the executor to choose a platform version; build may fail on a toolchain without the .v26 SDK. | State a concrete fallback rule: "use `.v26`; if `swift build` errors on unknown platform, drop to `.v15` and note it in handoff." (Already close — make it imperative not parenthetical.) |

---

## M2-b-scope-model-and-wall.md — WARN-only

Verdict: **WARN-only** (0 BLOCK, 3 WARN). Tight spec. Every function has an exact signature, the wall invariants are inlined, `vault_dir` vs `scope_dir` distinction (D4) is stated repeatedly with a runnable path-equality assertion. The one real risk is the `gateway.py` modify (Task 4) layering on M1-c's just-created file plus M2-b's KeyProvider — but the diff is described precisely.

| spec:line | BLOCK/WARN | check | what's wrong | minimal fix |
|---|---|---|---|---|
| M2-b:44 | WARN | CROSS-REFERENCES | Task 3 uses `paths.vault_dir(settings, scope)` and `paths.scope_dir(...)` — `vault_dir` is asserted to exist in M0-a `paths`. If M0-a did not define `vault_dir`, Flash must add it to an unlisted file. The signature is inlined (`vault_dir(settings, scope) -> Path = scope_dir/vault/`) so recoverable. | Confirm M0-a `paths.py` exports `vault_dir`; if not, add `paths.py` to Files-to-Change with the one-line helper. |
| M2-b:46 | WARN | TASK ATOMICITY | Task 4 modifies `Gateway` constructor + `_resolve_identity` + `_resolve_guest` + `handle_text` + `handle_text_stream` + `compose_brain` — 6 edit points in one modify task on a file M1-c just created. | Acceptable (one cohesive scope-attach change) but flag; ensure the M1-c `compose_brain` signature is stable so the modify lands cleanly. |
| M2-b:44 | WARN | CODE DETAIL | `VectorIndexHandle` typed as `{scope, kind: Literal["sqlite-vec"|"lancedb"], path}` — described as a frozen dataclass "where logic-free", but whether it's a dataclass, TypedDict, or Pydantic model is left open; Task 5 asserts `vector_index_handle()` returns "distinct objects". | Pin the type: `@dataclass(frozen=True) class VectorIndexHandle: scope: Scope; kind: Literal["sqlite-vec","lancedb"]; path: Path`. |

---

## M2-c-broker-client-tier0-launchd.md — WARN-only

Verdict: **WARN-only** (0 BLOCK, 6 WARN). Genuinely 2 phases (client + deployment), flagged. The mlock/zeroize/raw-hex contract is inlined with exact PRAGMAs; the fake-broker-server test is well-specified. Main risks: the deferred SQLCipher-binding choice leaves a `uv add` command non-runnable as written, and the cross-file `ScopedConnection.open()` add touches an M2-b file.

| spec:line | BLOCK/WARN | check | what's wrong | minimal fix |
|---|---|---|---|---|
| M2-c:84 | WARN | COMMANDS | Commands table lists `uv add <sqlcipher binding>` with the binding "chosen at the M4 sqlite-vec spike" — NOT a runnable command (placeholder `<...>`). Flash either picks arbitrarily or fails. The keyed open is gated to Task 6 anyway. | For M2-c off-hardware, drop the `uv add` entirely (Task 3 SKIPs the real open per line 49); add the binding only at M4. Or name a concrete default: `uv add sqlcipher3-binary`. |
| M2-c:49 | WARN | FILE EXPLICITNESS | Task 3 adds `ScopedConnection.open(key)` method to M2-b's `ScopedConnection` (in `src/artemis/data/scoped_store.py`), but that file is NOT in M2-c's Files-to-Change (only `sqlcipher.py` is). Edit = scope-lock violation; skip = the wall's keyed open never wires. | Add `src/artemis/data/scoped_store.py` to M2-c Files-to-Change (Modify) with the exact `open(self, key: SecretKey) -> Connection` body, OR move the method into M2-b. |
| M2-c:47 | WARN | CODE DETAIL | `secure_buffer` helper / `libc.mlock` via `ctypes` — the exact ctypes signature (`libc.mlock(addr, len)`, loading `ctypes.CDLL(None)` or `find_library('c')`) is prose. Flash's ctypes guess often segfaults (wrong argtypes). | Inline the ctypes setup: `libc = ctypes.CDLL(ctypes.util.find_library("c")); libc.mlock.argtypes=[ctypes.c_void_p, ctypes.c_size_t]` etc. |
| M2-c:56 | WARN | ACCEPTANCE | "render_plists.py --slot dev now emits 5 plists (the 4 from M0-b + the broker)" — assumes M0-b emits exactly 4. If M0-b's count differs, the `==5` check is wrong. | Phrase as "emits the M0-b set + 1 broker plist" and assert the broker plist specifically (`com.artemis.dev.broker.plist` exists + lints), not a hard total of 5. |
| M2-c:51 | WARN | TASK ATOMICITY | Task 4 (`provision_tier0_key`) + the long ADR-006 policy prose is one task; fine. But it depends on M2-a's key store supporting `--no-proof` (`proof_required=false`) — a cross-spec contract; if M2-a didn't ship `--no-proof`, this provision call fails. | Confirm M2-a Task 7 ships `provision-scope --no-proof` (it does, line 75); leave as WARN — note the dependency. |
| M2-c:54 | WARN | ENV PRE-CONDITIONS | `setup_autologin.sh` writes `/etc/kcpassword` + `com.apple.loginwindow` — a destructive system-config op, gated on-hardware. Off-hardware only `bash -n` (syntax) runs. Good gating, but the script edits root-owned system files with no dry-run guard. | Add a leading guard in the script: refuse to run unless `ARTEMIS_CONFIRM_AUTOLOGIN=1` (destructive-op gate), so an accidental run on a dev box no-ops. |

---

## M2-d-security-gate.md — WARN-only

Verdict: **WARN-only** (0 BLOCK, 2 WARN). This is a REVIEW gate (no production code) — the checklist's code-detail/signature items largely N/A. Acceptance criteria are concrete (five named risks + IPC wall + owner-auth + volume lifecycle, each needing an explicit verdict in the file; F13 correctly tightened AC4 to an in-file check). Appropriate for an apex-security skill invocation.

| spec:line | BLOCK/WARN | check | what's wrong | minimal fix |
|---|---|---|---|---|
| M2-d:41 | WARN | ACCEPTANCE | Task 2 "write a concrete follow-up spec stub into `docs/changes/` ... naming the exact files/changes" — the stub's required minimum sections aren't specified; a literal executor may emit an empty stub that passes the "one stub per finding" check vacuously. | State the stub must contain at least Identity + Files-to-Change + one Task, matching the planning spec template. |
| M2-d:58 | WARN | COMMANDS | "Commands" = "(invoke the `apex-security` skill)" — not a shell command; correct for a review task, but there is no runnable verification that the skill actually ran (vs the executor hand-writing the record). | Acceptable for a review gate; flag that verification is human/planning-side (the AC checks file contents, which is the right proxy). No code fix. |

---

## Area verdict

**M1/M2 — 1 BLOCK (M1-c lazy-register contradiction), 33 WARN — handoff BLOCKED until the M1-a/M1-c `register()` lazy-vs-eager contradiction + unlisted-file edit is resolved; the recurring cross-spec edits to files outside Files-to-Change (M1-b Settings, M1-d manifest.py, M2-c scoped_store.py) are the dominant WARN pattern and should be swept before build.**
