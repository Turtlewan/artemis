# Spec-lint report — M3 knowledge/retrieval specs

Final pre-handoff lint pass. Executor = DeepSeek V4-Flash (literal; fills gaps with plausible-wrong code; silently skips later phases). Classification: **BLOCK** (stop handoff) / **WARN** (soft flag).

Cross-spec facts established during the pass (from M0-d, M1-b, M1-c):
- M0-d `ModelPort.complete` is **`async def complete(self, role, messages, *, response_schema=None, ...)`** (M0-d-ports-scaffolding.md:71-73; also stated in DR-a:18, DR-c:20).
- M0-d `VectorStore.search(self, scope, query: Vector, k) -> list[RetrievedChunk]`; `Reranker.rerank(self, query, chunks, top_k) -> list[RetrievedChunk]` (M0-d:46-48).
- M1-b `BrainResponse` is a frozen dataclass `{text: str, path: str, tool_used: str|None, escalated: bool}` — `path` is `str`, so `path="agentic"` is type-valid (M1-b:46).
- M1-b `Brain.respond` is **`async def respond`** (M1-b:46).
- `Vector`, `RetrievedChunk`, `Document`, `Chunk` are M0-d types in `artemis.ports` (M0-d:33).

---

## M3-a — Ingestion pipeline

**Verdict: WARN-only**

Strong spec: every task names exact files, atomic, each `done when` has a runnable check. Consumed M0-d shapes are either named inline (Document fields in Task 2) or only round-tripped through the port (RetrievedChunk in `search`). Two internal inconsistencies around the D4 env-var removal.

| spec:line | BLOCK/WARN | check | what's wrong | minimal fix |
|---|---|---|---|---|
| M3-a:68 | WARN | env/precondition | Task 5 `done when` says `(volume_root=tmp, is_unlocked=lambda:True)`, but D4 (lines 22, 122) removed `volume_root`; the store resolves its dir via `paths.vault_dir` from `ARTEMIS_DATA_ROOT`. Flash may invent a `volume_root` ctor arg. | Replace `volume_root=tmp` with "set `ARTEMIS_DATA_ROOT` to a tmp path so `vault_dir` resolves under it" (match Task 9:84). |
| M3-a:56 | WARN | code detail | `to_document(...) -> Document` builds the M0-d `Document` but the exact M0-d `Document` field list/types are not inlined (only field names in prose). If M0-d `Document` is frozen/has extra required fields, Flash guesses the constructor. | Inline the M0-d `Document` field signature (one line) or cite `ports/types.py` Document with its exact fields. |
| M3-a:65 | WARN | code detail | `search` returns "M0-d `RetrievedChunk`s" but the `RetrievedChunk` field shape isn't inlined; Flash must construct it from a LanceDB row. M3-a only strictly needs `add`, but the task tells it to implement `search` fully. | Inline the `RetrievedChunk` constructor shape (chunk + score + provenance), or note "construct via the M0-d `RetrievedChunk` ctor — see ports/types.py". |
| M3-a:33,107 | WARN | command/env | `uv add lancedb docling trafilatura` may fail to install Docling off-hardware; spec says "may be gated/lazy" but gives no concrete fallback command if `uv add docling` errors. | Add explicit note: "if `uv add docling` fails off-hardware, `uv add lancedb trafilatura` only and keep Docling import lazy (Task 3)". |

---

## M3-b — Adaptive retriever

**Verdict: WARN-only**

Tasks atomic; signatures exact (`reciprocal_rank_fusion`, `hybrid_search`, `rerank`, `retrieve` all fully typed); `GraphModeNotImplemented` clean. Defects are the inherited D4 env-var mismatch (carried in from M3-a) and a `.score` mutation assumption on a likely-frozen type.

| spec:line | BLOCK/WARN | check | what's wrong | minimal fix |
|---|---|---|---|---|
| M3-b:56,94 | WARN | env/precondition | Task 6 seeds at "a temp `ARTEMIS_VOLUME_ROOT`" and the env table lists `ARTEMIS_VOLUME_ROOT`, but M3-a (D4) deleted that var — the M3-a writer the test reuses now resolves paths from `ARTEMIS_DATA_ROOT`/`vault_dir`. Setting `ARTEMIS_VOLUME_ROOT` has no effect; the temp seed and the store may diverge. | Replace `ARTEMIS_VOLUME_ROOT` with `ARTEMIS_DATA_ROOT` in Task 6 and the Environment table, matching M3-a D4. |
| M3-b:47 | WARN | code detail | `rerank` "attach scores ... return ... with the reranker score in `.score`". If M0-d `RetrievedChunk` is a frozen dataclass, mutating `.score` raises `FrozenInstanceError`; Flash will write `chunk.score = s`. The replace-via-ctor path isn't specified. | State explicitly: "build new `RetrievedChunk` instances with the reranker score (do not mutate; the dataclass is frozen)" and inline the field used. |
| M3-b:45 | WARN | code detail | Fallback path calls "a separate FTS query on `text`" but gives no method name on the store for FTS-only search (only `search` (dense) and the new `hybrid_search` exist). Flash must invent the FTS query call against the LanceDB table. | Name the exact LanceDB FTS call (e.g. `table.search(query_text, query_type="fts").limit(k*2)`), or add a private `_fts_search` helper signature. |
| M3-b:21 | WARN | code detail | Reranker transport is "drafted default `/v1/rerank` ... if absent, fall back to constrained-decoded scores via `/v1/chat/completions`" — the fallback request/response shape is not specified, so off-hardware it is moot (FakeReranker) but Flash may stub a wrong real path. Acceptable since `_score` is GATED (Task 5) and fakes cover CI. | Optional: note "the real `_score` HTTP shape is finalised in GATED Task 5; off-hardware uses FakeReranker only — leave `_score` raising `NotImplementedError` until Task 5". |

---

## M3-c — Agentic multi-hop

**Verdict: BLOCK**

The loop and Brain wiring are built on synchronous calls to `ModelPort.complete`, which is **async** per M0-d. A literal executor will write a sync loop that does not type-check and (worse) silently returns a coroutine where text is expected. This is the highest-severity finding in the set.

| spec:line | BLOCK/WARN | check | what's wrong | minimal fix |
|---|---|---|---|---|
| M3-c:44,45,Task1 | **BLOCK** | code detail / cross-ref | `run` calls `model.complete(role=..., messages=..., response_schema=...)` synchronously and is declared `def run`. M0-d `complete` is `async def` (M0-d:71). Flash will write a sync call → returns a coroutine, no `await`, mypy/runtime failure. `run` is never marked async. | Make `run` `async def run`; `await self.model.complete(...)` at both call sites (hop-control + synthesis); inline the async `complete` signature. |
| M3-c:52,Task3 | **BLOCK** | code detail | Brain branch: "run `result = self.agentic.run(request_text, scope)`" inside `async def respond` (M1-b:46) — must be `await self.agentic.run(...)` once `run` is async. As written Flash assigns a coroutine to `result` and reads `result.chunks` → AttributeError. | Change to `result = await self.agentic.run(request_text, scope)`. |
| M3-c:47,Task1 | **BLOCK** | code detail / seam | `as_agentic_fn` is declared to return sync `Callable[[str, Scope, int], list[RetrievedChunk]]` wrapping `self.run(...)`. Once `run` is async, the closure returns a coroutine, breaking M3-b's `agentic_fn` seam (M3-b:49 declares it sync). The two specs' seam types conflict. | Decide one: either (a) make M3-b's `agentic_fn` seam `Callable[..., Awaitable[list[RetrievedChunk]]]` and `await` it in M3-b:51, or (b) have `as_agentic_fn` run the loop via `asyncio.run`. State the chosen shape in both specs. |
| M3-c:54,Task4 | WARN | task atomicity / judgment | Task 4 contains a conditional STOP: "If `AdaptiveRetriever` is not yet constructed in `compose_brain` ... STOP and FLAG ... rather than editing a third file." A literal executor cannot evaluate "is it constructed yet" reliably and may either fabricate the wiring or skip silently. | Resolve at plan time: confirm whether M1-c's `compose_brain` already constructs `AdaptiveRetriever`; state the concrete wiring (or split the construction into a named prior step) so Task 4 has no branch. |
| M3-c:45 | WARN | code detail | `_synthesise`/`_synthesis_prompt` "instruct the model to ... cite provenance (page/url)" — the exact provenance fields read off `RetrievedChunk` aren't inlined, so Flash guesses field names when formatting citations. | Inline the provenance field names available on a chunk (page, source_id/url, bbox) as used by `_spotlight`/synthesis. |

---

## M3-d — Visual-document understanding

**Verdict: BLOCK**

The `VisualIngestPipeline` subclass (Task 5) is structurally impossible as specified: it needs `parsed`/`document`/`item` from `super().ingest()`, but M3-a's `ingest` returns only `IngestResult{document_id, chunks_written, skipped}` (M3-a:70,77) — those locals are not exposed. A literal executor will fabricate access to non-existent attributes.

| spec:line | BLOCK/WARN | check | what's wrong | minimal fix |
|---|---|---|---|---|
| M3-d:55,Task5 | **BLOCK** | code detail / cross-ref | `VisualIngestPipeline.ingest` calls `super().ingest(source)` then "check `if parsed.page_images`" and `self.visual_stage.process(parsed.page_images, document, item)`. But M3-a `ingest` returns `IngestResult` (no `parsed`/`document`/`item`) (M3-a:70). The subclass has no access to those locals; the override cannot work without re-running connector→parse→to_document or M3-a exposing them. Flash will invent `result.parsed`/`result.document`. | Either (a) re-run `connector_for/parse/to_document` inside the override (duplicate M3-a step 2 explicitly, with the exact calls), or (b) add a BLOCK-flag back to M3-a to have `ingest` return `parsed`/`document`/`item` on `IngestResult` (or expose a protected `_run_to_chunks` hook). Pick one and inline the exact code. |
| M3-d:43,Task1 | WARN | code detail / cross-ref | `embed_page(self, image: PageImage) -> list[Vector]` uses `Vector`, an M0-d type, but no import instruction is given for `Vector` (the task only says import `PageImage`/`RawItem` from `connectors`). Flash may leave `Vector` undefined or alias it to `list[float]` inconsistently. | Add "import `Vector` from `artemis.ports`" to Task 1 and inline its definition (`Vector = Sequence[float]` per M0-d). |
| M3-d:52,Task4 | WARN | code detail / cross-ref | `PageImageStore` must enforce dimension-lock "use a `_meta` sidecar ... mirror M4-a's `meta` table pattern" — a cross-ref the executor must go read M4-a to build; no ctor signature or meta-row write code inlined. Also `CrossScopeError`/`ScopeLockedError` enforcement is asserted but the `PageImageStore.add` signature isn't given. | Inline `PageImageStore`'s constructor signature and the `_meta` write (one row `{embedder_model_id, dimension}` on create; assert on open) directly; give `add(scope, id, vector, document_id, page)` signature. Do not defer to "mirror M4-a". |
| M3-d:51,Task4 | WARN | code detail | `process` builds `ChunkRecord`(s) "reuse the M3-a `ChunkRecord` shape" but the `ChunkRecord` field list (from M3-a:61) is not inlined here; Flash must cross-read M3-a chunking.py to construct it. M3-a is a dependency so the type exists, but the construction fields (chunk_id ordinal scheme for OCR chunks) are unspecified. | Inline the `ChunkRecord` field list and specify the `chunk_id` scheme for OCR/scene chunks (e.g. `f"{document_id}:visual:p{page}:{i}"`). |
| M3-d:61,Task7 | WARN | env/precondition | Test uses "temp `ARTEMIS_VOLUME_ROOT`" — same D4 inconsistency as M3-b; M3-a writer now keys off `ARTEMIS_DATA_ROOT`. | Replace with `ARTEMIS_DATA_ROOT` to match M3-a D4. |

---

### Area verdict

**M3 = BLOCK.** Two hard blockers must be fixed before handoff: M3-c's sync-vs-async `ModelPort.complete` mismatch (loop + Brain wiring + seam, 3 linked findings) and M3-d's impossible `VisualIngestPipeline.ingest` override (no access to `parsed`/`document`/`item` from M3-a's `IngestResult`). M3-a and M3-b are WARN-only — chiefly the leftover `ARTEMIS_VOLUME_ROOT` references that D4 superseded.
