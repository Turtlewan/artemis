# M1 manifest (manifest registry / router-brain / gateway / time-tool) — analyst output 2026-06-08

> KEY: no draft contains `extractor` or `Qwen3-14B` — convention #2 needs no find-replace here. M1-b's
> adapter already resolves roles generically (`base_url_for_role`/`model_id_for_role`, only `claude-cli`
> NotImplemented), so `sensitive_reasoner` is covered; just make it explicit.
> CROSS-MILESTONE BACK-FILLS to ADD in M1-b (from M5): `Brain.respond_stream`, `Brain.pre_route` (see below).

## M1-a-manifest-registry.md
1. DELETE `</content>` (line 128, stray EOF).
2. DELETE `</invoke>` (line 129, stray EOF). File ends after the Progress placeholder.
- Markers: none. Keep split-rule flag (line 13). Pre-check: clean after deletions.
- **(cross-milestone) When applying, check M1-a's `ModuleManifest` carries a per-module `sensitive: bool`
  / `data_scope` flag (M5-c reads it as the Tier source of truth). If absent, ADD `sensitive: bool` to the
  manifest model.**

## M1-b-router-brain.md
1. DELETE `</content>` (~line 123, stray EOF).
2. Marker (line 20, thresholds) → replace `[NEEDS CLARIFICATION: exact default thresholds … only the
   constants.]` with: "M1 default thresholds (drafted defaults, GATED for empirical confirm at Task 5):
   route_deterministic_threshold = 0.6, route_local_threshold = 0.35, route_escalate_floor = 0.15.
   Constructor args; do not change code shape; confirm empirically on-hardware at Task 5."
3. Add Assumption (after line 17 bullet): "The OpenAIModelPort resolves ANY openai-adapter role generically
   via the M0-c base-URL seam (base_url_for_role/model_id_for_role) — NOT hardcoded to responder/embedder.
   This includes `sensitive_reasoner` (Qwen3.6-27B, openai adapter, mlx lazy), which does sensitive
   reasoning + sensitive memory extraction. M4-b calls `model.complete(role="sensitive_reasoner", ...)`
   through this same adapter, no special-casing. The adapter raises NotImplementedError ONLY for `claude-cli`
   (teacher). → impact: Stop."
4. Task 3 (line 51) `Raise NotImplementedError for role whose adapter is claude-cli (teacher — deferred,
   mirrors M0-c).` → "Resolve any openai-adapter role generically (responder, embedder, sensitive_reasoner)
   via base_url_for_role/model_id_for_role; raise NotImplementedError ONLY for a claude-cli role. Do NOT
   hardcode an allowlist of role names."
5. **(cross-milestone back-fill, ADD) `Brain.respond_stream(text, scope) -> AsyncIterator[str]`** — yields
   text segments; responder ModelPort already supports stream=True; the tool path yields its rendered answer
   as ONE segment. Add the method to `Brain` (brain.py) + a streaming-path test. Consumed by M5-d.
6. **(cross-milestone back-fill, ADD) `Brain.pre_route(text, scope) -> str | None`** — returns the top
   candidate module BEFORE serving (reuse the Router's classification) so the Gateway can classify the Tier
   pre-serve. Add to Brain (and surface via the Gateway in M1-c). Consumed by M5-c. Add a test.
- Marker resolved: line 20 thresholds (default + existing gated Task 5). Pre-check: keep the split-rule flag
  (line 13, advisory). Clean after edits.

## M1-c-gateway-surfaces.md
1. DELETE `</content>` (line 114, stray EOF).
2. Marker (line 18, SSE) → replace `[NEEDS CLARIFICATION: confirm SSE vs a raw chunked … swap the media type
   only.]` with: "Decision: SSE (`text/event-stream` via FastAPI StreamingResponse) for M1 — matches brain.md
   streaming + the eventual voice/app surfaces (apex-realtime SSE default). No on-hardware gate (code-shape)."
3. **(cross-milestone) surface `Brain.pre_route` through the Gateway** if M1-c is the Gateway home (so M5-c's
   handle_voice can call it). Confirm when applying.
- Marker resolved: line 18 SSE → decided. Pre-check: clean (startup→lifespan is soft, draft already offers
  the alternative).

## M1-d-time-tool-heartbeat.md
1. DELETE `</content>` (line 120, stray EOF).
2. Marker (line 20, Heartbeat placement) → replace `[NEEDS CLARIFICATION: should the M1 Heartbeat run inside
   … Confirm at review.]` with: "Decision: M1 ships the Heartbeat as a standalone importable `Heartbeat` class
   with run_forever() + tick(), NOT wired into the brain process or launchd in M1 (a future spec mounts it).
   M1 only proves tick() emits HEARTBEAT_OK."
- Marker resolved: line 20 → decided. Pre-check: clean.

### GENUINE OWNER FORKS (M1): none.
