# M5 manifest (voice) — analyst output 2026-06-08

> All `[NEEDS CLARIFICATION]` markers present in raw form must be rewritten to a resolved statement (marker
> text removed) — the GATED tasks already exist, the markers just need to stop being markers. No `extractor`
> in M5. Two cross-milestone back-fills to M1 (Brain.respond_stream, Brain.pre_route) — see below.
> Factual fix: M5-b says "Kokoro-82B" → 82M.

## M5-a-audio-sidecar.md
1. Marker (line 19, openWakeWord runtime) → "DRAFTED DEFAULT: openWakeWord ('Hey Jarvis') behind a
   `WakeWordDetector` protocol + `FakeWakeWord` for off-hardware tests; concrete on-device runtime (ONNX
   Runtime Swift/C via SwiftPM primary, CoreML `MLModel` fallback) loaded + confirmed in GATED Task 9.
   On-hardware item, not an owner fork." Remove marker.
2. Marker (line 20, Silero VAD) → "DRAFTED DEFAULT: Silero VAD behind a `VoiceActivityDetector` protocol +
   `FakeVAD`; concrete runner (same ONNX-primary/CoreML-fallback) loaded in GATED Task 9; playback-state-aware
   barge-in threshold testable off-hardware against the fake." Remove marker.
3. Marker (line 23, socket) → "DRAFTED DEFAULT: a single multiplexed Unix-domain socket (1-byte kind tag +
   4-byte big-endian length + body) — one LaunchAgent endpoint, mirrors the broker. Split sockets are the
   documented fallback if on-hardware bring-up shows framing contention; only the framing module changes."
   Remove marker.
- Pre-check: clean after the 3 rewrites. (Names match brain.md: openWakeWord + Silero + VoiceProcessingIO AEC;
  <200ms barge-in already GATED Task 9; no EOF artifact; data-root uses <dataRoot>/<slot>/run/.)

## M5-b-stt-tts.md
1. Marker (line 17, STT packages) → "DRAFTED DEFAULT: packages `parakeet-mlx` (Parakeet-TDT-0.6B,
   FluidAudio/ANE) + `mlx-whisper` (Whisper-turbo) behind a _load seam; routing = `language is None or
   language.startswith('en')` → Parakeet, else Whisper. Confidence/language-signal fallback = build-time
   refinement. Exact package names/load API/Parakeet confidence signal confirmed GATED Task 5." Remove marker.
2. Marker (line 18, Kokoro deployment) → "DRAFTED DEFAULT: Kokoro-82M as an in-process MLX model resident in
   the long-lived brain process behind the `TTS` port (residency satisfies 'persistent server'; avoids a 2nd
   launchd job). A separate persistent Kokoro server is the documented swap if on-hardware shows GPU/RAM
   contention. Confirmed GATED Task 6." Remove marker.
3. (line 48) `Kokoro-82B` → `Kokoro-82M` (factual typo; brain.md line 100 + the spec title say 82M).
- Pre-check: clean after the 2 rewrites + the typo fix.

## M5-c-speaker-id.md
1. Marker (line 17, ECAPA package) → "DRAFTED DEFAULT: SpeechBrain ECAPA-TDNN (PyTorch) behind a _load_ecapa
   seam + FakeSpeakerID; MPS with CPU fallback; default match threshold cosine ≥ 0.25 (ECAPA EER placeholder),
   tuned on-hardware. Exact package/MPS acceptability/tuned threshold confirmed GATED Task 6." Remove marker.
2. Marker (line 18, voiceprint store) → "DECISION: voiceprints in a SEPARATE small store readable WITHOUT the
   owner DEK (identity routing must work while locked, to classify owner-Tier-0 + unknown→guest), encrypted
   under the Tier-0 proactive key (M2-c) — identity is a Tier-0-class need, not a Tier-1 secret. EMBEDDINGS
   ONLY (raw enrolment audio discarded after embedding), never-logged, minimised (person id + embedding).
   Anti-spoof/liveness + rotation noted-for-later (not v1)." Remove marker.
3. Marker (line 20, Tier-1 signal) → "DECISION: the Tier-1 signal = the M1-a ModuleManifest sensitivity flag
   (data_scope / a `sensitive: bool`) — a module marked sensitive (minimal stand-in set
   finance/health/journal/memory) ⇒ Tier-1, else Tier-0. Reuses M1-a manifest metadata (source of truth); no
   model. The full provenance/sensitivity router is later; keep the `tier_for(...)` seam. If M1-a lacks a
   usable flag, M5-c adds an interim `sensitive: bool` name-keyed lookup." Remove marker.
4. Marker (line 21, unknown speaker) → "DECISION: an unknown speaker (identify→None) routes to a SINGLE shared
   `guest` PersonId/scope (matches M2-b light-guest infra). Per-guest clustering is a later refinement."
   Remove marker.
5. Marker (line 53, Gateway matched-module) → "DECISION (CROSS-MILESTONE DEP): the Gateway classifies the Tier
   BEFORE serving via `Brain.pre_route(text, scope) -> str | None` (top candidate module) — added to the
   Brain/Gateway from M1. The Gateway calls brain.pre_route(transcript, scope), classifies via tier_for(...),
   applies voice-ID≠key, then (if allowed) calls brain.respond. BrainResponse.tool_used/path is too late." Remove.
6. (line 40) `if the Tier-0 read path isn't ready, store plaintext under proactive/ with a FLAG` → "the
   voiceprint store MUST be encrypted under the Tier-0 proactive key (M2-c). If the Tier-0 read path is not yet
   available at build time, BLOCK on M2-c rather than storing plaintext — plaintext voiceprints at rest are
   not acceptable (embeddings-only, encrypted, never-logged is the hardened requirement)."
- Pre-check: clean after the 5 rewrites + the plaintext-fallback hardening (edit 6).

## M5-d-voice-loop-orchestrator.md
1. Marker (line 18, ack) → "DRAFTED DEFAULT: a pre-rendered short non-speech earcon PCM cached at startup
   (cheapest, language-neutral, always fast). A spoken filler (synth once + cached) is a config swap. The ack
   MUST be barge-in-interruptible. Confirmed at on-hardware bring-up (Task 6)." Remove marker.
2. Marker (line 19, streaming brain) → "DECISION (CROSS-MILESTONE DEP): M5-d consumes
   `Brain.respond_stream(text, scope) -> AsyncIterator[str]` (yields text segments) added to M1-b — the
   responder adapter already supports stream=True; the tool path yields its rendered answer as one segment.
   This is brain.md 'stream every stage' and back-fills M1-b. The loop splits the segment stream into
   sentences and pipelines tts.synthesize per sentence." Remove marker.
3. Files-to-Change (line 36) → "(small) add the streaming voice entry `handle_voice_stream(audio, transcript)
   -> AsyncIterator[str]` consuming Brain.respond_stream (M1-b back-fill) and reusing the M5-c Brain.pre_route
   Tier gate; keep handle_voice/handle_text intact."
4. Prerequisites (line 27) `M1-b (Brain.respond; + respond_stream extension per the NEEDS CLARIFICATION)` →
   "M1-b (Brain.respond; + the Brain.respond_stream back-fill — CROSS-MILESTONE DEPENDENCY)".
5. Task 4 (line 63) → "(handle_voice_stream consuming the Brain.respond_stream M1-b back-fill, and the
   Brain.pre_route Tier gate from M5-c)".
6. Task 3 step 5 (line 56) `…if added, OR splits the final text` → "(added in Task 4, consuming
   Brain.respond_stream)" — remove the conditional fallback (single deterministic path).
7. Prereq (line 27) data root: `M0-a (paths/config — the <slot>/run/audio.sock path).` → "M0-a (paths/config —
   the data root resolves to /opt/artemis; the sidecar socket is /opt/artemis/<slot>/run/audio.sock)."
- Pre-check: clean after rewrites. Edits 2/3/5/6 collapse the (a)/(b) fork into the single decided
  Brain.respond_stream path (deterministic for the literal-execution agent).

## CROSS-MILESTONE DEPENDENCIES (M5 → M1)
1. `Brain.respond_stream(text, scope) -> AsyncIterator[str]` — text-segment stream; M1-b; consumed by M5-d.
2. `Brain.pre_route(text, scope) -> str | None` — top candidate module pre-serve; M1 Brain/Gateway; consumed
   by M5-c (+ M5-d stream path).
3. M5-c reads the M1-a ModuleManifest `sensitive: bool`/`data_scope` flag as the Tier source of truth — if
   M1-a lacks it, that's an M1-a addition (M5-c carries an interim name-keyed stopgap).

### GENUINE OWNER FORKS (M5): none. (All on-hardware items = drafted-default + GATED; voiceprint/Tier/guest
decisions resolved per the gate notes, not deferred to the owner.)
