# M3 manifest (knowledge/RAG layer) — analyst output 2026-06-08

> Visual-doc LOCKED to ColQwen2.5 Light / MPS 2.5.1; GraphRAG = gated build-time spike (agentic stays
> default); the graph spike's extraction uses `sensitive_reasoner` (Qwen3.6-27B), never `extractor`.
> The agentic loop itself uses the `responder` (correct, unchanged).

## M3-a-ingestion-pipeline.md
1. Marker (line 19, M2 volume-mount seam) → resolve to drafted-default + GATED (NOT an owner fork). Replace
   the `[NEEDS CLARIFICATION: the encrypted-volume mount is an M2 deliverable …]` with: "Decision: M3-a drafts
   against `paths.volume_vectors_dir(settings, scope) -> <volume_root>/<scope>/lancedb` (volume_root under
   /opt/artemis) + precondition `is_owner_unlocked()`. The M2 broker volume-mount step is a sequenced-with M2
   deliverable (M2-a owns it; mount point /opt/artemis/<slot>/<scope>/vault/); rename at integration if M2
   finalizes different symbol names (one-line adapter change). GATED on-hardware (Task 8): confirm the table
   is created under the broker-mounted volume and is unreadable when locked."
2. Pin data root: add a clause that the mounted-volume root is under `/opt/artemis`; off-hardware
   `ARTEMIS_VOLUME_ROOT` falls back to `data_root`. Task 1: `Settings.volume_root: Path` (env
   `ARTEMIS_VOLUME_ROOT`, default data_root); PROD default = the broker-mounted volume under /opt/artemis.
3. Marker (line 22, Docling) → resolve: "Docling behind the `DocumentParser` port with a deterministic
   `FakeParser` for CI; real Docling install + parse GATED on-hardware (Task 7). Marker/MinerU escalation
   PARKED per ADR-007." Remove marker.
- Pre-check: clean after the two markers removed (M2 mount = sequencing dep, not a blocker once reworded).

## M3-b-adaptive-retriever.md
1. Marker (line 19, hybrid) → resolve: "Decision: build `hybrid_search` to TRY LanceDB native hybrid+RRF
   first, FALL BACK to explicit dense+FTS+our-RRF on AttributeError/NotImplementedError; explicit RRF helper
   unit-tested off-hardware. GATED Task 5: record which path is primary on the installed LanceDB version (WAND
   FTS). Does not change the Retriever shape." Remove marker.
2. Marker (line 20, reranker endpoint) → resolve: "Decision: drafted default transport = `/v1/rerank` behind
   the `_score(query, texts) -> list[float]` seam; if absent, fall back to constrained-decoded scores via
   `/v1/chat/completions`. GATED Task 5: confirm the live Qwen3-Reranker-0.6B endpoint shape. Off-hardware =
   FakeReranker." Remove marker.
3. (line 19) append to the LanceDB hybrid assumption: "(LanceDB FTS uses WAND — fast; sqlite-vec is pinned per
   brain.md but is NOT used for the doc corpus, only memory)."
- Pre-check: clean after markers removed. Graph stays a clean `GraphModeNotImplemented` deferred stub (the
  spike lives in M3-c).

## M3-c-agentic-multihop.md  (fix b — reconcile GraphRAG stance)
1. Identity (line 10) append after `…replacing the M1 ESCALATION_NOT_AVAILABLE stub …):`: "Agentic multi-hop
   is the SHIPPED DEFAULT for complex/connect-the-dots queries. The knowledge-graph alternative (LightRAG vs
   agentic on a personal gold-set, behind the unchanged retrieve(query,mode) port) is a GATED BUILD-TIME
   SPIKE — NOT a v1 deliverable — and only displaces agentic if the spike proves the graph earns its
   extraction cost (ADR-007 §Build-time spikes / §Refinement)."
2. Simplicity-check (line 25) `…graph deferred).` → "the graph is now a gated build-time spike (LightRAG vs
   agentic on a gold-set behind the port; agentic stays the default until the spike proves the graph earns
   its extraction cost), NOT a hard defer and NOT a v1 build."
3. ADD Task 5b (GATED — build-time spike, NOT v1): "GraphRAG-vs-agentic eval — (no repo files; behind the
   existing retrieve(query,mode) port) — on the Mini, build a small personal gold-set of connect-the-dots
   queries; evaluate LightRAG (MIT; fast-graphrag only if it ships a verified v1.0) using the
   `sensitive_reasoner` role (Qwen3.6-27B) for relation extraction (constrained decoding guarantees relation-
   JSON validity) against the agentic-multi-hop default. Wire any graph path behind mode='graph' (the M3-b
   deferred stub) ONLY for the eval; do NOT ship. Agentic REMAINS the default unless the spike proves the
   graph earns its cost. LazyGraphRAG is non-OSS — do not wait for it. — done when: gold-set result (graph vs
   agentic quality/cost) + keep-agentic-or-adopt recommendation recorded in handoff; no graph code ships in
   v1 unless the spike says so."
4. (line 19) confirm the loop uses `responder` (correct; no change); the spike's extraction = `sensitive_
   reasoner`, never `extractor`.
5. Marker (line 22, router path) → resolve: "Decision: NO router change. M3-c triggers the agentic loop on the
   EXISTING path=='escalate' branch (try-agentic-first; grounded answer → return, else the
   ESCALATION_NOT_AVAILABLE stub). A distinct 'complex' router path is an optional later M1-b follow-up." Remove.
6. Marker (line 20, loop defaults) → resolve: "Decision: ship max_hops=4, per_hop_k=5, max_total_chunks=20,
   dedup by chunk_id as config-tunable knobs. GATED Task 5: tune empirically (ADR-007 agentic-loop-tuning
   spike). Loop shape unaffected by values." Remove.
- Pre-check: clean after markers removed AND the graph stance reconciled (edits 1–3). Before: FAILED fix b.

## M3-d-visual-document-understanding.md  (fix d — LOCK ColQwen2.5 Light / MPS 2.5.1)
1. Title (line 8) `ColPali-style visual retrieval)` → `ColQwen2.5 Light visual retrieval)`.
2. Identity (line 10) `a ColPali-style page-image visual-retrieval seam … not hard-picked.` → "a ColQwen2.5
   Light (PyTorch MPS 2.5.1, NOT 2.6.0) page-image visual-retrieval seam — all swappable behind the port. The
   visual-retrieval model is now LOCKED to ColQwen2.5 Light / MPS 2.5.1 (ADR-007, 2026-06-08; Lance v2.2 Blob
   V2 makes patch-vector storage practical). Only resident-vs-lazy load sizing (48GB contention) and whether
   ColQwen2.5 ships in v1 remain a GATED build-time sizing spike — the model choice is fixed."
3. (line 11 →why) `ColPali visual retrieval).` → `ColQwen2.5 Light / MPS 2.5.1 visual retrieval — locked in
   ADR-007 §Ingestion + §Refinement).`
4. Marker (line 19, vision model) → resolve: "Decision: scene description = Qwen3-VL via the `vision` role;
   visual retrieval = ColQwen2.5 Light (PyTorch MPS 2.5.1, NOT 2.6.0) via the `visual_embedder` role — both
   LOCKED (ADR-007). Only resident-vs-lazy load policy + whether ColQwen2.5 ships in v1 remain GATED (Task 6).
   Code shape unchanged by the sizing outcome." Remove marker.
5. (line 21) `the actual ColPali model …owner-judgment (brain.md "multimodal/ColPali in v1 or later").` →
   "the visual embedder is ColQwen2.5 Light (PyTorch MPS 2.5.1), behind the `visual_embedder` role; only its
   v1-inclusion + resident-vs-lazy sizing are gated (ADR-007)." (Remove the owner-judgment framing for the
   model choice.)
6. Marker (line 21, multi-vector storage) → resolve: "Decision: store ColQwen2.5 Light patch-vectors in a
   SEPARATE `page_images_{scope}` LanceDB table with late-interaction/MaxSim search (Lance v2.2 Blob V2 makes
   patch-vector storage practical — ADR-007), keeping the main text table's dimension-lock untouched. MaxSim
   detail + v1-inclusion GATED (Task 6). Pooling-to-single-vector rejected as lossy." Remove marker.
7. Simplicity (line 25) reword: model locked (ColQwen2.5 Light for retrieval; Qwen3-VL for scene desc); only
   load policy (resident-vs-lazy) is the gated 48GB-contention spike — not model identity.
8. Files table (line 36) keep class name `ColPaliRetriever` (stable id) + note "backed by ColQwen2.5 Light /
   MPS 2.5.1 (locked)".
9. roles.toml task (line 54) model `ColPali` → model `ColQwen2.5-Light`, comment `# LOCKED: ColQwen2.5 Light,
   PyTorch MPS 2.5.1 (NOT 2.6.0) — ADR-007 2026-06-08; resident-vs-lazy + v1-inclusion = gated sizing spike
   (Task 6)`.
10. Task 3 (line 46) `the real model is the spike` → `the real model is ColQwen2.5 Light / MPS 2.5.1 (locked);
    only the load policy + v1-inclusion are the spike`.
11. Task 6 (line 56) `IF ColPali is chosen for v1: real ColPali page embed` → `IF ColQwen2.5 Light is included
    in v1: real ColQwen2.5 Light page embed (PyTorch MPS 2.5.1 — pin this, NOT 2.6.0)`; `ELSE record ColPali
    as deferred-but-seamed.` → `ELSE record ColQwen2.5 Light as deferred-but-seamed (model still locked; only
    v1-inclusion deferred).`
12. Marker (OCR bridge) → resolve: "Decision: OCR bridge = a tiny Swift CLI `artemis-ocr` under swift/ invoked
    via subprocess (matches the M2 broker precedent; clean Python adapter). Build of artemis-ocr GATED
    on-hardware (Task 6)." Remove marker.
13. **Qwen3-14B → Qwen3.6-27B sizing arithmetic:** Performance (line 109) + Task 6 (line 56 `(b)`) +
    Assumptions sizing note: replace `lazy Qwen3-14B ~9GB` / `Qwen3-14B` with `lazy Qwen3.6-27B
    sensitive_reasoner ~18GB 4-bit`. brain.md line 18: the 27B fits 48GB with ~23GB headroom — restate the
    spike's RAM arithmetic against the 27B.
- Pre-check: clean after the three markers removed + ColQwen2.5/MPS-2.5.1 lock + the 14B→27B sizing update.

### GENUINE OWNER FORKS (M3): none.
