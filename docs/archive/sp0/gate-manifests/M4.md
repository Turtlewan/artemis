# M4 manifest (memory engine) — analyst output 2026-06-08

> Role: extraction + A.U.D.N. run on the ONE local-heavy role `sensitive_reasoner` (`Qwen3.6-27B`), NOT a
> separate `extractor` role, NOT the cloud teacher. Substantive additions: relation-cardinality registry
> (Option 2), composite forgetting score (retrieval-time re-rank multiplier), A-MEM metadata columns,
> Graphiti 4-timestamp confirm, fold `bump_access`+`purge` into M4-a.

## M4-a-store-schema-bitemporal-repo.md
1. Marker (line 25, keying) → resolve to ADR-004 Option 2 cardinality registry. Replace the
   `[NEEDS CLARIFICATION: should "same logical fact" …]` block with: "Resolved per ADR-004 cardinality
   refinement (Option 2): keying is cardinality-aware via a `relation → SINGLE|MULTI` registry (Task 2a).
   SINGLE: `fact_key = sha256(person_id, subject, relation)`; MULTI: `fact_key = sha256(person_id, subject,
   relation, object)`. Registry default = MULTI (fail-safe: never overwrite). Table shape unchanged;
   `compute_fact_key` inputs + the partial-unique invariant both consult the registry." Remove marker.
2. Marker (line 21, binding) → downgrade: replace leading `[NEEDS CLARIFICATION:` with `[RESOLVED — GATED
   Task 1:`, keep body, keep closing `]` (APSW drafted default + sqlcipher3 alternative; binding confirmed
   on-hardware at Task 1). No longer blocking.
3. ADD relation-cardinality registry (Task 2a / schema). Insert after line 57 (facts_fts bullet):
   "- relation-cardinality registry `relation_cardinality(relation TEXT PRIMARY KEY, cardinality TEXT NOT
   NULL CHECK(cardinality IN ('SINGLE','MULTI')), source TEXT NOT NULL DEFAULT 'seed')`. Seed SINGLE rows
   for `lives_in, birthday, name, age, employer, home_address, phone_number, email`; all others default
   MULTI. `source ∈ 'seed'|'teacher'|'owner'`. Module constants `SEED_SINGLE_RELATIONS: frozenset[str]`,
   `DEFAULT_CARDINALITY = 'MULTI'`."
4. ADD A-MEM columns on `facts` (line 54 DDL): append `, keywords TEXT, contextual_description TEXT,
   linked_ids TEXT` before the closing `)`. Prose: "keywords (space/JSON list), contextual_description
   (short prose 'why noted'), linked_ids (JSON list of related fact_keys for multi-hop) — A-MEM
   structured-note metadata (ADR-004); all nullable, populated on write by M4-b."
5. Registry accessors (Task 4): `compute_fact_key(self, subject, relation, object_) -> str` (cardinality-
   aware: SINGLE excludes object_, MULTI includes it); `cardinality_of(self, relation) -> str` (returns
   SINGLE|MULTI; unknown → insert DEFAULT_CARDINALITY, return it); `set_cardinality(self, relation,
   cardinality, *, source) -> None` (UPSERT).
6. `add`/`update` respect cardinality (Task 4): `add` computes fact_key via `compute_fact_key(subject,
   relation, object_)` (MULTI: different object → different key, coexist; SINGLE: object excluded →
   same key → idempotency/UPDATE). `update` is meaningful only for SINGLE; MULTI supersession = tombstone+add
   (decided by M4-b).
7. Golden tests (Task 6, the ~2 affected): the lives_in London→Paris test asserts SINGLE keying +
   `cardinality_of("lives_in")=="SINGLE"`; the likes/tea idempotency test gains a coexisting likes/coffee
   row (MULTI) + `cardinality_of("likes")=="MULTI"`; ADD a registry test (seed SINGLE; default MULTI persists;
   owner override; SINGLE excludes object vs MULTI includes).
8. Fold `bump_access` + `purge` into repository.py (Task 4, insert after `semantic_candidates` bullet):
   "`bump_access(self, fact_id) -> None` (increment access_count, set last_access=now; M4-c CALLS it).
   `purge(self, fact_key) -> int` — the ONLY hard-delete primitive: permanently DELETE all version rows for
   fact_key + matching facts_vec/facts_fts; returns rows removed; irreversible, owner-only; every other
   'delete' is tombstone. M4-c CALLS it."
9. Files table (line 42 repository.py note) append: " bump_access, purge (only hard-delete),
   compute_fact_key/cardinality_of/set_cardinality (cardinality-aware keying);"
10. Files table (line 40 schema.py note) append: " + the relation_cardinality registry table (seed SINGLE)
    + the A-MEM keywords/contextual_description/linked_ids columns on facts."
11. Graphiti 4-timestamp confirm (line 23) append: " (This is the Graphiti four-timestamp bitemporal schema —
    ADR-004 reference: valid_from/valid_to = event-time t_valid/t_invalid; tx_from/tx_to = ingestion-time
    t'_created/t'_expired.)" (Schema is already 4-timestamp; no column change.)
12. Acceptance (line 150) append before `all pass.`: ", cardinality registry (SINGLE vs MULTI keying),
    bump_access (access_count/last_access updated), purge (all rows + vec/fts removed) ".
- Runtime pin: Commands line 111 `uv add sqlite-vec` → `uv add "sqlite-vec==<pinned-pre-v1-version>"` + note "pin per ADR-004 (pre-v1 API instability)".

## M4-b-write-path-audn-extraction.md
1. Marker (line 19, role) → REMOVE. Replace block with: "Resolved per SHARED CONVENTION: extraction +
   A.U.D.N. run on the ONE local-heavy role `sensitive_reasoner` (model_id `Qwen3.6-27B`, openai adapter,
   mlx-served lazily) — NO `extractor` role, NO local-teacher role. M4-b calls
   `model.complete(role="sensitive_reasoner", ...)`. Sensitive memory extraction is LOCAL — never the cloud
   claude-cli teacher." Edit the lead-in sentence accordingly.
2. Identity (line 10) `on the local teacher` → `on the local `sensitive_reasoner` role (`Qwen3.6-27B`)`.
3. Task 1 (line 43) `FactExtractor(..., *, role: str = "extractor")` → `role: str = "sensitive_reasoner"`
   (+ comment "# the one local-heavy role; there is no separate 'extractor' role").
4. Task 2 (line 45) `AudnDecider(..., *, role: str = "extractor")` → `role: str = "sensitive_reasoner"`.
5. Task 3 (line 47) `extractor_model_id: str = "extractor"` → `extractor_model_id: str = "Qwen3.6-27B"`
   (provenance value = model id).
6. Cardinality-aware A.U.D.N. (Task 2, decide): before deciding, look up `repo.cardinality_of(new_fact.
   relation)`. SINGLE → mem0 rubric as-is (same (subj,rel)+diff obj → UPDATE). MULTI → diff object = ADD of a
   coexisting value (DELETE/tombstone-then-ADD only on explicit supersession/negation). Pass cardinality into
   the decision prompt. Unknown relation → MAY one-shot-classify via `model.complete(role="sensitive_reasoner")`
   + persist `repo.set_cardinality(relation, result, source="teacher")`; default-MULTI until classified.
   `AudnDecider.__init__` gains `repo`: `(model: ModelPort, repo: BitemporalRepository, *, role=...)`.
7. A-MEM metadata on write: `ExtractedFact` gains `keywords: tuple[str,...] = ()`, `contextual_description:
   str | None = None` (+ EXTRACTION_SCHEMA optional fields). ADD apply (line 55): `repo.add(..., keywords=
   ef.keywords, contextual_description=ef.contextual_description)`. `linked_ids` written empty/NULL (later
   multi-hop pass). Requires M4-a `add`/`update` to accept these kwargs (cross-ref M4-a edit 4 — note in M4-a
   Task 4).
8. Apply mapping MULTI supersession (Task 3 step d): UPDATE applies to SINGLE; MULTI = ADD new coexisting, or
   tombstone+ADD on explicit supersession — never in-place UPDATE that loses a coexisting value.
9. Prereq (line 31) `must carry a served local extractor (teacher) role` → "M0-a roles.toml already defines
   `sensitive_reasoner` (model_id Qwen3.6-27B, openai adapter, mlx lazy) — M4-b reuses it; NO new role."
10. Task 6 (line 77) `the served local extractor MLX model` → `the served local sensitive_reasoner
    (Qwen3.6-27B) MLX model`; append "(the locked reasoner is already 27B; the eval validates its A.U.D.N.
    judgment, not a role choice)."
11. Env/Network (lines 108, 113): `extractor/teacher role endpoint` → `sensitive_reasoner role endpoint`;
    `the served extractor MLX model` → `the served sensitive_reasoner (Qwen3.6-27B) MLX model`.
12. Security (line 117) `the **local** teacher/extractor` → `the **local** `sensitive_reasoner` role`.
- Factory (Task 4, line 63) `build_write_path`: construct `AudnDecider(model, repo)` (pass repo).

**Marker resolved:** line 19 → sensitive_reasoner / Qwen3.6-27B; no extractor role, no roles.toml follow-up,
never cloud teacher. (Remove the "FLAG as a small M0-a follow-up" too.)

## M4-c-recall-autoinject-decay-owner.md
1. Marker (line 19, Brain injection-point) → downgrade: `[NEEDS CLARIFICATION:` → `[RESOLVED — drafted
   default:`, keep body; append "Confirmed: M1's deterministic tool-render path is NOT injected; the
   free-form/local responder path IS. No on-hardware gate (code-shape only)."
2. Marker (line 21, decay constants) → downgrade: `[NEEDS CLARIFICATION:` → `[RESOLVED — drafted defaults,
   tuned at GATED Task 6:`, append "weighting per ADR-004 composite forgetting score (edit 3); half_life_days
   /inject_threshold/tombstone_floor are placeholders tuned on traces at Task 6 (code-shape unchanged)."
3. Composite forgetting score (Assumption line 21) append: "Additionally, per ADR-004's brain-sweep
   absorption, recall on the SEMANTIC store applies the composite forgetting score
   `I(m,t)=α·exp(−λΔt)+β·access_count+γ·cosine(m,query)` as a retrieval-time re-rank MULTIPLIER (recently-
   accessed ×1.5, stale ×0.3) over sqlite-vec similarity — NOT eager deletion. The EPISODIC store stays
   TTL-only. Distinct from the inject-time decay score: inject ranks current facts by recency×salience×access;
   recall re-ranks vector candidates by the composite I(m,t) multiplier."
4. decay.py (Task 1) ADD `recall_multiplier(*, last_access, valid_from, access_count, cosine, now,
   half_life_days=HALF_LIFE_DAYS, alpha=1.0, beta=0.1, gamma=1.0) -> float`: composite I clamped to ~[0.3,1.5]
   (recent ×1.5, stale ×0.3); semantic store only; pure fn, no DB. Constants RECALL_BOOST_MAX=1.5,
   RECALL_DAMP_MIN=0.3, ALPHA=1.0, BETA=0.1, GAMMA=1.0. Extend done-when.
5. recall applies multiplier (Task 2, line 45) `re-rank the candidates by decay_score` → `re-rank the
   candidates by decay.recall_multiplier applied as a MULTIPLIER on sqlite-vec similarity (the ADR-004
   composite forgetting score; recent ×1.5 / stale ×0.3; cosine from the candidate distance), NOT eager
   deletion` + "(semantic store only; episodic recall stays TTL/age-ordered)."
6. bump_access CALLED not defined (line 45 inject_context): replace the "add this small method … prefer
   adding BitemporalRepository.bump_access and note the M4-a touch" with "CALL `repo.bump_access(fact_id)`
   (DEFINED in M4-a's repository.py; M4-c only calls it, no M4-a touch here)."
7. purge_fact CALLS the M4-a primitive (Task 3, line 53): replace "Add BitemporalRepository.purge(...)" with
   "CALL `repo.purge(fact_key) -> int` (DEFINED in M4-a; the only hard-delete; M4-c only calls it)."
8. Files to Change (line 83) remove `…/memory/repository.py, ` from M4-c's Modify list; remove from Git add
   (line 97) too (bump_access/purge live in M4-a now).
9. A-MEM linked_ids note (end of Task 2): "the semantic FactRow now carries A-MEM keywords/
   contextual_description/linked_ids (added M4-a, populated M4-b); recall MAY expand candidates one hop
   through linked_ids — OPTIONAL follow-on; M4-c's required behaviour is the composite-multiplier re-rank,
   linked_ids exposed for a future multi-hop pass."
10. Files-to-Change decay.py note (line 36) append: " + recall_multiplier (composite forgetting score as a
    retrieval-time re-rank multiplier, semantic store only)."
11. Test (Task 5, line 62) ADD composite-recall-multiplier test (recent ×1.5 / stale ×0.3; recall re-ranks;
    nothing deleted). Acceptance (line 128) append ", composite recall multiplier (recent ×1.5 / stale ×0.3,
    no deletion) ".

**Markers resolved:** lines 19 + 21 downgraded to RESOLVED (drafted defaults; Task 6 gated tuning).
**Pre-check:** Edits 6–8 align M4-c to NOT modify repository.py. With both markers downgraded → clean.

### GENUINE OWNER FORKS (M4): none.
(Design tension noted, NOT forked: agent-memory research suggests injecting recall as a user-turn block for
prompt-injection hardening, but ADR-004 + brain.md LOCK system-prompt injection — left as-is per "don't
re-open decided things." Revisiting would be an ADR-004 amendment, not a gate edit.)
