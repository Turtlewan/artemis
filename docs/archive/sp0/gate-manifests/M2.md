# M2 manifest (security wall) — analyst output 2026-06-08

> Fork resolutions (see README): volume-mount → fold into **M2-a** (option A), path contracted in M2-c;
> Tier-0 boot-unwrappable proactive key → explicit `proof_required=false` policy in **M2-a**, scoped to
> the `proactive` corpus only. Both ADR-grounded (ADR-005 line 36; ADR-006 lines 14/24).

No stray EOF artifacts in the M2 files. Volume-mount step is ABSENT from all four drafts → add it (M2-a owns
the native mount; M2-c contracts the path; M3-a consumes). `is_owner_unlocked()` already exists in M2-b
(`KeyProvider.is_owner_unlocked`) + M2-c (`BrokerKeyProvider.is_owner_unlocked` via broker `status`).

## M2-a-key-broker.md
1. Title (line 8) `session lifecycle + zeroization, local IPC server)` → `…local IPC server, per-scope encrypted-volume mount-on-unlock)`.
2. Identity (line 10) `enforces session lifetime + zeroization, and exposes a local Unix-socket IPC` → `enforces session lifetime + zeroization, MOUNTS the per-scope encrypted volume on a verified unlock (ADR-007 — the volume holds the scope's SQLCipher memory DB + LanceDB doc index; one unlock opens the whole per-scope vault), and exposes a local Unix-socket IPC`.
3. (line 11) append after `…SE mechanics + threat model).` → ` · docs/technical/adr/ADR-007-knowledge-layer.md (the broker mounts the per-scope encrypted volume on unlock — refines ADR-005).`
4. New Assumption (insert after line 20 DEK assumption): "On a verified unlock the broker also MOUNTS the per-scope encrypted volume (ADR-007) at `/opt/artemis/<slot>/<scope>/vault/` (holds the scope's SQLCipher memory DB + LanceDB doc index). Mount attempted only after a fresh proof verifies and before the DEK is returned; on session lock/idle/process-exit the broker UNMOUNTS it. The volume key is a separate SE-wrapped key (same `.userPresence` policy as the DEK). M3 consumes the mounted path. → impact: Stop (M3 cannot open the LanceDB index unless the broker mounts here; the mount-point path is a frozen contract for M3-a)."
5. Marker (line 19, `.userPresence` degradation) — resolve + remove (see Markers).
6. Marker (line 21, fresh-proof/SE-signed-keypair) — resolve + remove (see Markers).
7. Files table (line 34) `SE key gen (.userPresence), ECIES wrap/unwrap of a 32-byte DEK` → `…DEK + the per-scope volume-mount key`. Add table row after line 38: `| /Users/artemis-build/artemis/swift/ArtemisBroker/Sources/ArtemisBroker/VolumeMount.swift | create | mount/unmount the per-scope encrypted volume at /opt/artemis/<slot>/<scope>/vault/ on verified unlock / lock |`.
8. New Task (insert between Task 5 and Task 6; renumber consistently): "Task (volume mount): implement per-scope encrypted-volume mount/unmount — files: …/VolumeMount.swift — `struct VolumeMount`: `mountPoint(scope:) -> URL` = /opt/artemis/<slot>/<scope>/vault/; `mount(scope:) throws` attaches the scope's encrypted APFS volume/sparsebundle (key SE-unwrapped, same `.userPresence` policy) at that mount point; `unmount(scope:) throws`; `isMounted(scope:) -> Bool`. `getDEK` mounts on successful proof BEFORE returning the DEK; lock/idle/lockAll/process-exit unmount. Concrete attach (hdiutil sparsebundle vs `diskutil apfs` encrypted volume) = ADR-007 mount-lifecycle build-time spike → real attach GATED on-hardware (fold into Task 9). Off-hardware the mount is a verifiable no-op shim recording mount/unmount calls. — done when: `swift build` passes; an off-hardware test asserts `getDEK` triggers `mount(scope)` before returning the DEK and `lock` triggers `unmount(scope)`; real attach gated on-hardware."
9. Task 9 done-when (line 75) append: "; the real encrypted-volume attach/detach at /opt/artemis/<slot>/<scope>/vault/ succeeds and survives a lock/unlock cycle (ADR-007 mount-lifecycle spike)."
10. Acceptance (add after line 138): "Run the volume-mount test → verify: a verified getDEK invokes mount(scope) before returning the DEK; lock invokes unmount(scope) (real attach gated on-hardware)."
11. Marker (line 69, IPC code-signing): strip the `[NEEDS CLARIFICATION: …]` wrapper, keep the prose (decision: dev bypass flag `ARTEMIS_BROKER_SKIP_CODESIGN=1` now, PROD identity at packaging milestone).
12. **(Fork-2 consequence) Task 2 (SecureEnclaveKeyStore):** signature `wrappingKey(forScope:create:proofRequired:)` — when `proofRequired == false` build the SE access-control WITHOUT `.userPresence` (boot-unwrappable, `proactive` scope ONLY); default `true`. **Task 7 `provision-scope`:** accept `--no-proof` (sets `proof_required=false`). (Generated by resolving M2-c marker-e; the no-proof policy lives in the broker per ADR-006.)

**Markers resolved (M2-a):**
- (line 19) `.userPresence` degradation marker → drafted default + GATED Task 9. Replace bracket with marker-free prose: "Fallback if `.userPresence` degrades on-hardware: gate unwrap SOLELY on a verified phone proof with the SE key created WITHOUT a presence requirement (phone assertion = sole human factor); code shape identical (the proof gate already wraps every unwrap) — only the `SecAccessControl` flags change. Whether degradation occurs is the GATED on-hardware check (Task 9); must NOT silently degrade to login-unlock."
- (line 21) SE-signed-keypair marker → decided (ADR-005 line 18 + FINALIZATION-NOTES M2). Replace with: "M2 ships the SE-backed-signing-keypair verifier as the concrete `UnlockProofVerifier`; App Attest is the documented alternative behind the same protocol (a separate gated spike). Fully testable, no Apple-server dependency."

**Pre-check:** task renumbering after the inserted volume-mount task — keep cross-refs to "Task 9" resolving. Strip the line-69 marker wrapper. Else clean.

## M2-b-scope-model-and-wall.md
1. Marker (line 19) → resolve + remove (below).
2. (line 21) append after `…engine init is later).`: " The LanceDB doc-corpus index for owner scopes lives inside the per-scope encrypted volume mounted by the broker (ADR-007); in M2-b the handle is just a locator under `scope_dir`, the actual mounted-volume path is contracted by M2-a/M2-c."

**Markers resolved (M2-b):**
- (line 19) owner-auth-token marker → decided: adopt drafted model. Replace with: "M2 binds 'the text surface is owner' to 'an owner session is currently unlocked at the broker' (unlocked-owner-session ⇒ owner scope; else LOCKED). A per-request auth token is deferred to the client milestone. This is the M2 owner-auth model the M2-d gate reviews (no unauthenticated path may reach owner scope)."

**Pre-check:** clean.

## M2-c-broker-client-tier0-launchd.md
1. Title (line 8) `+ owner auto-login config` → `+ owner auto-login config + per-scope volume-mount path contract`.
2. Marker (line 23, Tier-0) → resolve + remove (below, fork-2).
3. Marker (line 19, SQLCipher binding) → resolve + remove (below, fix-d).
4. New Assumption (insert after line 23): "The per-scope encrypted volume is mounted by the broker (M2-a / ADR-007) at /opt/artemis/<slot>/<scope>/vault/ on a verified unlock; the brain-side `sqlcipher_open` and (M3) LanceDB open their files UNDER that mounted path. `BrokerKeyProvider.is_owner_unlocked()` (via broker `status`) is the seam M3-a uses to know the volume is mounted/unlocked; M2-c does NOT itself attach the volume (broker's native job). → impact: Caution (mount path + `is_owner_unlocked()` seam are the frozen contract M3-a consumes)."
5. Task 8 (line 65, clause b) append: " AND that the per-scope encrypted volume at /opt/artemis/<slot>/<scope>/vault/ is NOT mounted until the proof verifies, and unmounts on lock/idle (ADR-007 mount-lifecycle spike, cross-ref M2-a Task 9)."
6. Commands (line 82) `uv add sqlcipher3-binary (or APSW + sqlite3mc per ADR-004)` → `uv add <sqlcipher binding> — binding chosen at the M4 sqlite-vec spike (APSW+sqlite3mc vs sqlcipher3); M2-c installs whichever the M4 spike selects, behind the sqlcipher_open wrapper`. Line 109 likewise `uv add <sqlcipher binding> (binding per M4 spike)`.

**Markers resolved (M2-c):**
- (line 19) SQLCipher-binding marker (fix-d) → binding DEFERRED to the M4 sqlite-vec spike (the decider); do NOT pick here. Replace with: "The keyed-open sits behind a thin swappable `sqlcipher_open(path, key_hex)` wrapper; the concrete Python SQLCipher binding (APSW+sqlite3mc vs sqlcipher3) is decided at the M4 sqlite-vec-under-SQLCipher spike and is NOT chosen in M2-c. The mlock/zeroize/raw-hex logic is binding-independent."
- (line 49, Task 4, Tier-0) (fix-e / fork-2) → boot-unwrappable policy is an explicit broker policy in M2-a. Replace with: "The Tier-0 `proactive` key uses a distinct broker key policy `proof_required=false` (boot-unwrappable, no phone proof) — defined as an explicit policy in M2-a's key store (M2-a Task 2/7 edits); M2-c PROVISIONS the `proactive` scope via the broker `provision-scope --scope proactive --no-proof` path and reserves the empty corpus dirs. Boot-unwrap CONSUMPTION lands at M6. The `proactive` key protects ONLY the minimised, mostly-derived corpus (ADR-006); it must never gate any owner-private/general data."

**Pre-check:** the `provision-scope --no-proof` flag must exist in M2-a's CLI (M2-a edit 12) for M2-c Task 4 to call it — land both together. Else clean.

## M2-d-security-gate.md
1. Assumption (line 18) append after `…before any sensitive store is built").`: " The build-order prerequisite is explicit: M2-d is a hard predecessor of every M3 and M4 spec (deepest focus = prompt-injection DEK exfiltration)."
2. Prerequisites (line 23) append: " This gate is a blocking prerequisite OF M3-* and M4-* (they must list M2-d as a prerequisite; M3/M4 build order may not begin until this gate's verdict is PASS or CONDITIONAL-PASS)."
3. Task 1 (line 38) `Also confirm the IPC wall … no unauthenticated path to owner scope.` → `Also confirm (i) the IPC wall (peer-uid + code-signing, M2-a) rejects the build-agent user; (ii) the M2-b owner-auth model (unlocked-session ⇒ owner) has no unauthenticated path to owner scope; (iii) the per-scope encrypted-volume mount (M2-a/ADR-007) mounts ONLY after a verified proof and UNMOUNTS on lock/idle/exit — a mounted volume left attached after lock is a wall breach.`
4. Acceptance (line 93) add the volume-mount/unmount lifecycle to the addressed list.
5. Acceptance (add after line 95): "The verdict record states explicitly that M3-* and M4-* remain BLOCKED until this gate is PASS/CONDITIONAL-PASS, and is referenced as a prerequisite by the M3/M4 specs."
6. (line 16) add `encrypted-volume mount/unmount lifecycle (ADR-007)` to the gated-on-hardware-tasks list.
7. (line 94) add encrypted-volume mount/unmount to the cited spikes; the word "five spikes" is now stale → drop the count / say "each spike" (do NOT touch "FIVE ADR-005 residual risks" at lines 32/93 — different list).
8. **(Fork-2) add a gate criterion:** audit proactive-key scope creep (ADR-006 line 25) — the `proof_required=false` proactive key decrypts ONLY the minimised Tier-0 corpus, never owner-private/general.

**Markers resolved (M2-d):** none present.
**Pre-check:** avoid over-replacing "five" (keep it for the residual-risks list). Else clean.
